# AGENTS.md
# ==========
# This file is for Codex (Codex.ai).
# Paste the contents of this file at the start of any Codex conversation
# to instantly bring Codex up to speed on the project state.
#
# Update the CURRENT STATE section after each work session.

## PROJECT
`mytbot` — open-source autonomous multi-asset trading system under AGPL v3, published by Emfasys Labs, a division of Emfasys Ltd.
GitHub: https://github.com/kvcom/mytbot.git
Owner: UK-based, trading stocks, bonds, ETFs, forex, crypto.
Primary broker: IBKR Pro. Crypto: Kraken + Binance.

## ARCHITECTURE IN ONE PARAGRAPH
**One-button system.** `python run.py` starts everything — orchestrator brings up
Docker (Postgres, Redis), auto-discovers available brokers (skipping unavailable),
runs the trading loop and data pipeline, and exposes an API+WebSocket for the UI.
The UI has a single ON/OFF button (`POST /system/start`, `POST /system/stop`).
All brokers implement a single abstract interface in `brokers/base.py`.
Signals pass through an optional **Signal Accumulation Engine** (`signals/accumulator.py`) that maintains time-decayed, multi-source conviction per asset (quant + news + macro) before the signal engine emits a unified Signal.
The signal engine aggregates strategy outputs into a Signal (with legacy point-in-time AI modifier and optional accumulated overlay).
Every Signal passes through the risk engine (unconditional veto power).
Approved signals go to the execution engine which routes to the best broker.
Everything is logged. Runtime broker permissions support graceful fallback routing.
Risk parameters are managed by `ParameterManager` with layered overrides
(regime > AI > defaults) and bounded, auditable changes.
AI is local-first (rules → FinBERT → local LLM → optional paid fallback) — never places orders.

## KEY FILES
- `run.py`                   — **THE entry point** (`python run.py` starts everything)
- `system/orchestrator.py`   — state machine: OFF → STARTING → RUNNING → STOPPING → OFF
- `system/dependency_manager.py` — auto-start Postgres/Redis via Docker
- `system/broker_manager.py` — auto-discover and connect available brokers
- `system/trading_loop/`    — controllable async trading loop package (`TradingLoop`, `helpers.py`)
- `brokers/base.py`          — the adapter interface (FROZEN: backward-compatible optional fields only)
- `brokers/registry.py`      — add new brokers here (one line)
- `brokers/ibkr/adapter.py`  — IBKR + single-leg options (chain / qualify / `Order.instrument_metadata`)
- `brokers/ibkr/universe.py` — config-backed curated IBKR execution/discovery seed
- `brokers/ibkr/qualification.py` — persisted IBKR contract qualification cache
- `core/instruments.py`      — `OptionContractSpec` (options as structured instruments)
- `risk/options_env.py`      — `ENABLE_OPTIONS` / `OPTIONS_*` env merged into risk config
- `brokers/bybit/adapter.py` — Bybit V5 (spot / USDT linear)
- `brokers/_template/`       — copy this to add any new exchange
- `risk/engine.py`           — risk checks, kill switch, D115 FX + equity-index cluster caps
- `risk/intraday_derisk.py`  — D115 graduated portfolio-level derisk decision layer
- `risk/parameters.py`       — parameter manager (defaults + overrides + expiry)
- `signals/accumulator.py`   — stateful time-decayed conviction per symbol (optional; YAML-gated)
- `signals/anti_churn.py`    — D115 dedup / cross-strategy contradiction / post-fill cooldown gate
- `signals/engine.py`        — signal aggregation + accumulator integration + anti-churn wiring
- `strategies/momentum.py`   — first strategy (momentum breakout)
- `execution/engine.py`      — order placement
- `execution/router.py`      — smart order routing
- `storage/models.py`        — database schema
- `api/server.py`            — FastAPI: `/system/start`, `/system/stop`, `/system/status`, `/dashboard/snapshot`, `/pnl` (week/month)
- `api/pnl_periods.py`       — calendar week/month rollups over `daily_pnl` for `/pnl`
- `system/dashboard_publish.py` — persists allocator snapshot (`dashboard.snapshot` in `ControlState`) for the UI
- `config/risk_limits.yaml`  — all risk thresholds (editable without code change)
- `config/m8_micro_live.yaml` — optional micro-live profile (symbol/strategy/notional caps when `APP_ENV=live`)
- `config/fundamentals.yaml` — parameter defaults, absolute bounds, AI policy
- `config/broker_permissions.yaml` — runtime broker permission/fallback map
- `config/ibkr_universe.yaml` — curated IBKR priority instruments; qualification still required before orders
- `config/data_pipeline.yaml` — M2 symbols, intervals, News/FRED toggles
- `config/ai.yaml`          — local-first AI config: providers, escalation policy, no daily caps
- `config/session_exit_policy.yaml` — broker-aware pre-close hold/trim/close policy by horizon and mode
- `config/profile_modes.yaml` — D015 mode coefficients + emergency safety_bounds (allocator)
- `config/allocation.yaml`  — D015 global opportunity replacement policy (validated by `config/models.py`)
- `config/loaders.py`       — `load_profile_modes()` / `load_allocation()`
- `core/models_runtime.py`  — runtime types: Opportunity, RegimeState, AllocationDecision, ExecutionPlan
- `core/session_exit_policy.py` — pre-close session-exit decision layer (`hold`, `trim`, `close`, `defer`)
- `signals/volume_anomaly.py` — D015 volume/flow features, detection vs scoring, YAML-weighted component
- `signals/opportunity_engine.py` — D015 opportunities; blends momentum proxy + volume component from M2 JSON + metadata
- `risk/regime_state.py` — D015 market/regime context for allocator (stub)
- `portfolio/allocation_engine.py` — D015 global replacement allocator (stub)
- `execution/planner.py` — D015 AllocationDecision → ExecutionPlan
- `data/regime_metrics.py` — cross-section feature fetch + aggregates for regime
- `data/feature_lookup.py` — latest `feature_snapshots.features` per symbol
- `portfolio/d015_hold_switch.py` — hold score + switching-cost penalty
- `signals/d015_weights.py` — profile/regime dynamic coefficient resolver
- `signals/opportunity_components.py` — momentum/news/liquidity/structure component scores
- `system/d015_shadow.py` — optional per-signal D015 vs legacy log (env `ALLOCATOR_D015_SHADOW`)
- `core/signal_math.py` — bounded_sigmoid, tanh_clip, normalize_zscore (Decimal)
- `run_pipeline.py`          — M2: yfinance → features → Postgres; NewsAPI + FRED
- `scripts/qualify_ibkr_universe.py` — no-order IBKR contract qualification/cache warmer
- `ai/router.py`             — local-first AI router (drop-in for NewsClassifier)
- `ai/providers/`            — provider implementations (rules, FinBERT, Ollama, Codex fallback)
- `ai/escalation.py`         — necessity-based escalation engine (no hard daily caps)
- `ai/schemas.py`            — shared AI types (ProviderResult, EscalationContext)
- `ai/news_classifier.py`    — legacy Codex classifier (kept for backward compat)
- `ai/pipeline.py`           — M6 orchestration: symbol news score + macro regime
- `docs/DECISIONS.md`        — architectural decision log
- `docs/BUILD_PLAN.md`       — milestones M1–M10 + task history
- `docs/NEW_MACHINE_SETUP.md` — new PC / full reinstall (Python, Docker, Ollama, UI, `.env`)
- `docs/M8_MICRO_LIVE.md`    — micro-live guardrails (`APP_ENV=live`, YAML profile)
- `alembic/`                 — DB migrations (URL from POSTGRES_* in env.py)
- `tests/`                   — pytest smoke tests
- `.cursorrules`             — Cursor AI alignment rules

## CURRENT STATE
<!-- Update this section after each work session -->
- Milestone: Publication readiness ✅
- Last completed task: **D149 — Drawdown open-lock stops becoming a permanent deployment block**. Investigated why the dashboard remained around `48%` cash deployed with the capital slider at `100%`. Live evidence showed the slider was correctly restored to `1.0`, all brokers were connected, and the global-edge allocator was proposing fresh opens, but `strategy_candidate_log` had repeated `risk_rejected | Failed: drawdown_open_lock` rows. Root cause: the intraday-derisk monitor refreshed the 15-minute open-lock every monitor tick whenever day P&L remained below the configured tier, before/independent of successful derisk execution. A realised-loss day could therefore keep fresh opens blocked indefinitely and strand the dashboard under-deployed. Fixed `system/orchestrator.py` so recovery still clears the lock, but activation/extension happens only after a reduce-only derisk order actually fills exposure reduction. Added `risk.drawdown_governor.derisk_execution_reduced_exposure()` and regression coverage in `tests/test_intraday_derisk.py`; corrected the misleading `config/risk_limits.yaml` tier comment (`trigger_tier_idx: 1` is the sorted `-1.00%` tier). Verification: `python -m py_compile risk/drawdown_governor.py system/orchestrator.py tests/test_intraday_derisk.py`; `python -m pytest tests/test_intraday_derisk.py tests/test_dynamic_scaling_stress.py -q` (`22 passed`); adjacent risk/fill/accounting slice `python -m pytest tests/test_d126_fills_ledger.py tests/test_post_fill_snapshot_from_ledger.py tests/test_orchestrator_post_fill_persist.py tests/test_risk_engine.py -q` (`55 passed, 1 skipped`); `git diff --check`.
- Last completed task: **D148 — Fills-ledger post-fill quantity drives PositionLog snapshots**. Investigated the BCH-USD question: the 2026-06-05 `05:46:13 UTC` Bybit sell `124.78247192 @ 236.30000305` did **not** fully close BCH; the clean fills ledger says it partially closed an old Bybit long with average cost `344.29793769`, realised `-13476.24924694`, and left `position_qty_after=39.59613075` long. The UI showed it as a fresh `short` because the legacy post-fill `PositionLog` snapshot reapplied the order delta to stale snapshot state rather than trusting `FillLog.position_qty_after`, immediately writing `quantity=-124.78247192`. Fixed `execution/engine.py` to attach the fills-ledger `position_qty_after`, `avg_cost_basis`, and realised P&L to the `OrderResult`; updated `run_m3._apply_signal_to_portfolio_state()` to use those ledger values when present; wired both trading-loop and orchestrator monitor post-fill persistence paths to pass `result` through. Added `tests/test_post_fill_snapshot_from_ledger.py` covering the exact BCH-style partial close and a full ledger flat/tombstone case. Verification: `python -m py_compile execution/engine.py run_m3.py system/trading_loop/loop.py system/orchestrator.py tests/test_post_fill_snapshot_from_ledger.py`; `python -m pytest tests/test_post_fill_snapshot_from_ledger.py tests/test_d126_fills_ledger.py tests/test_orchestrator_post_fill_persist.py tests/test_position_mark_to_market.py -q` (`31 passed`); D147 PnL/derisk regression slice (`30 passed`); `git diff --check`.
- Last completed task: **D147 — PnL truthfulness + dynamic per-position loss governor**. Fixed the loss-triage issues without temporary paper-mode safety measures. `/pnl` now preserves ledger facts (realised P&L, fees, trades) during partial/offline provider coverage and only suppresses point-in-time full-book unrealised/period marks that would be paired with a partial NAV denominator. `_upsert_daily_pnl()` now reconciles `strategy_breakdown.risk_daily_loss_accumulated` from the canonical realised fill ledger when the runtime state is stale, so a day with realised losses cannot persist `risk_daily_loss_accumulated: '0'`. Added dynamic Tier 0 intraday derisking: `risk/intraday_derisk.py` now resolves the per-position loss threshold and trim fraction from the configured Tier 0 anchor, observed position volatility, NAV concentration, portfolio volatility scalar, and the existing `single_name_notional.max_pct_nav` reference; `trim_pct: 0.25` is now a floor, not a hard-coded final action. `system/orchestrator.py` passes position metadata and the single-name cap into the evaluator; `config/risk_limits.yaml::intraday_derisk.position_loss_tier.dynamic.enabled=true`. Verification: `python -m py_compile api/server.py run_m3.py risk/intraday_derisk.py system/orchestrator.py tests/test_intraday_derisk.py tests/test_pnl_periods.py tests/test_upsert_daily_pnl_partial_coverage.py`; `python -m pytest tests/test_intraday_derisk.py tests/test_pnl_periods.py tests/test_upsert_daily_pnl_partial_coverage.py -q` (`30 passed`); adjacent risk suite (`86 passed, 1 skipped`); adjacent API/PnL/MTM suite (`43 passed, 1 skipped`); `git diff --check`. Note: the earlier emergency stop/capital-zero action was reversed after the user clarified this is paper mode; capital allocation was restored to `100%`, and no restart was performed in D147.
- Last completed task: **D146 — Loss triage evidence**. User reported the paper system was making losses. Evidence gathered: `/pnl` while stopped/partial coverage masked realised P&L to `0`, but `daily_pnl` for `2026-06-05` shows realised `-9107.37610658`, unrealised `-4556.44637537`, fees `36.14863049`, trades `34`, NAV `1235050.36202196`. Open-position loss is concentrated in crypto: AAVE/ETH/SOL longs are down roughly `-$6k`, partly offset by BCH/BTC shorts. Largest realised loss found: `2026-06-05 05:46:13 UTC` Bybit `BCH-USD` sell `124.78247192 @ 236.30000305`, strategy `momentum_breakout`, realised `-13476.24924694`, closing an older high-cost long. Today strategy realised by fill ledger: `momentum_breakout -13476.24924694`, `intraday_derisk_monitor -43.87022177`, `profit_harvest_monitor +319.30729143`, `volume_flow +1491.91410262`, `mean_reversion +2601.52196808`. Red flags found before D147: `DailyPnL.strategy_breakdown` showed `risk_daily_loss_accumulated: '0'` despite large realised losses; `/pnl` masked realised losses under partial/off coverage; drawdown open lock appeared only after the large loss was already realised; profit-harvest handled winners, but loss containment/stop-loss did not prevent the old crypto loser from reaching a large realised loss.
- Last completed task: **D145 — Reconnect settling hold + NAV/history mismatch guard**. Fixed the remaining 5–10 second dashboard nonsense during IBKR reconnect where the UI could briefly compare active-provider NAV against full-book historical equity and show scary return/sharpe/drawdown. `system/broker_manager.py` now tracks `BrokerStatus.balance_ready_since` and requires a configurable dashboard-ready hold (`BROKER_DASHBOARD_READY_HOLD_SEC`, default `8s`) before a freshly balance-ready broker rejoins `BrokerReport.included_names`; coverage reports `balance snapshot settling` during this hold. `system/portfolio_equity.py` clears `balance_ready_since` when a live NAV poll demotes a broker back to warming. Added regression coverage in `tests/test_broker_coverage.py`. `ui/src/app/redesign/dashboard.tsx` now also detects a NAV/history base mismatch (current NAV far below latest historical NAV while daily P&L is flat) and rebases the performance strip / deployment presentation for that frame even if coverage has not caught up yet. Verification: `python -m py_compile system/broker_manager.py system/portfolio_equity.py tests/test_broker_coverage.py`; `python -m pytest tests/test_broker_coverage.py tests/test_broker_balance_ready.py tests/test_live_portfolio_value.py -q` (`46 passed`); `npm run build` in `ui/`; `git diff --check`. Restarted live `run.py` (PID `46160`) and verified `/dashboard/snapshot` stays `active_providers` with IBKR excluded during the settling window.
- Last completed task: **D144 — Partial-scope deployment wording**. Clarified the dashboard semantics when active-provider capital at work exceeds active-provider NAV while a provider is excluded. The live payload confirmed the `~145%` was not an IBKR leak: active NAV was `~$256k` and active-provider positions on `alpaca/binance/bybit/kraken` summed to `~$371k`. Updated `ui/src/app/redesign/dashboard.tsx` so, during partial-provider coverage, the hero Deployment card caps the headline at `100%` and moves the true overage into the sublabel as `145% active exposure` instead of implying deployable allocation exceeds the slider ceiling. Updated `ui/src/app/redesign/capital.tsx` so the gauge and info row label this condition as `active exposure` rather than plain `at work`. Verification: `npm run build` in `ui/` clean.
- Last completed task: **D143 — Scoped capital-at-work display**. Fixed a dashboard consistency issue where the hero deployment badge and Capital Allocation panel recalculated `capital at work` client-side from positions/orders, which could show misleading values such as `144%` during partial provider coverage even though `/dashboard/snapshot` had already computed scoped `portfolio.cash_deployed`. `ui/src/app/redesign/useLiveSystem.ts` now exposes `live.capitalAtWork`, preferring backend-scoped `snapshot.portfolio.cash_deployed` and falling back to the old positions/orders calculation only for older/missing snapshots. `ui/src/app/redesign/dashboard.tsx` and `ui/src/app/redesign/capital.tsx` now use that shared scoped value, keeping numerator and NAV denominator in the same active-provider universe. Verification: `npm run build` in `ui/`; `git diff --check`; live `/dashboard/snapshot` currently reports full coverage with `cash_deployed_pct=0.6202`.
- Last completed task: **D142 — Dashboard reconnect warm-up guard**. Fixed the brief post-reconnect dashboard nonsense window where IBKR could be socket-connected but its account/balance snapshot had not warmed yet. `system/portfolio_equity.py::live_portfolio_snapshot()` now demotes a broker's `balance_ready` flag when a live NAV poll finds no usable current balance and no valid cached fallback, marking it as `Balance snapshot warming up after reconnect`; this keeps that provider excluded from dashboard/accounting scope until a fresh balance probe succeeds. `/dashboard/snapshot` now polls live NAV before deriving coverage and filtering positions, preventing even the first response after reconnect from treating stale coverage as full coverage. Added regression coverage in `tests/test_live_portfolio_value.py` for a stale included IBKR balance being demoted immediately. Verification: `python -m py_compile api/server.py system/portfolio_equity.py tests/test_live_portfolio_value.py`; `python -m pytest tests/test_live_portfolio_value.py tests/test_broker_coverage.py tests/test_broker_balance_ready.py tests/test_api_dashboard_extras.py -q` (`64 passed, 1 skipped`); `git diff --check`. Restarted live `run.py` and verified `/dashboard/snapshot` reports full scope with all five brokers when all balances are ready.
- Last completed task: **D141 — Active-provider dashboard scope**. Implemented dashboard/accounting-only provider scoping for partial broker coverage. `/dashboard/snapshot` now publishes explicit `dashboard_scope` / portfolio scope metadata (`active_providers` vs `all_configured_providers`, active brokers, excluded brokers, and note text) while preserving the existing risk/strategy model that still tracks last-known offline-provider positions. Updated `ui/src/app/redesign/dashboard.tsx` so active-scope labels, warnings, and banner copy appear only when at least one provider is disconnected; when all configured providers are connected, the dashboard renders as before. During partial coverage, the headline NAV label becomes `Active portfolio NAV`, the footer names excluded providers, and performance cards use the current active-provider NAV as their baseline to avoid mixing whole-book history with connected-provider NAV. Verification: `python -m py_compile api/server.py system/dashboard_publish.py`; `npm run build` in `ui/`; `git diff --check`.
- Last completed task: **D140 — Low-deployment live paper audit after Ollama upgrade**. Confirmed Ollama has `qwen3.5:9b`, `llama3.1:8b`, and `qwen2.5:7b`; switched `config/ai.yaml::providers.local_reasoning.model_name` to `qwen3.5:9b` and verified `/system/status` reports the new local model. Diagnosed why deployment stayed near 25%: the LLM was not the blocker. The actual chain was (1) coordinator sizing unaware of the unconditional 5%-NAV single-name rail, wasting slots on at-cap dust; (2) regime/meta/risk gates not applying deployment pressure consistently; (3) execution preflight dropped Alpaca/IBKR candidates after one venue failure and paper extended-hours IBKR book depth/spread checks were too brittle. Fixes: `system/trading_loop/loop.py` now passes the single-name cap into coordinator sizing, retries alternate eligible brokers on execution-precheck rejection, stamps the preflight-surviving broker into coordinator action metadata, and computes deployment pressure from `dashboard.snapshot.portfolio.cash_deployed_pct` so it matches the UI. `system/trading_loop/candidate_collection.py` relaxes the post-regime confidence floor under deployment pressure. `risk/engine.py` applies bounded `deployment_pressure_weight` relief to dynamic confidence/trade-quality thresholds from `config/risk_limits.yaml`. `execution/engine.py` logs detailed execution-limit metadata and uses paper-only `liquidity_check.paper_mode` overrides for extended-hours paper rehearsal: lower visible-depth multiple, wider spread cap, and valid bid/ask with zero visible size treated as unknown rather than no-liquidity. Verification: focused suite `python -m pytest tests/test_global_edge_preflight.py tests/test_risk_preflight_capacity.py tests/test_d125_risk_caps.py tests/test_arbitrage_loop_integration.py tests/test_execution_engine.py tests/test_meta_label_dynamic_threshold.py -q` (`85 passed`). Live process restarted; all five brokers connected; local LLM ready as `qwen3.5:9b`. Deployment moved from ~25.1% cash deployed to 35.1% after execution-gate fixes, then to 41.4% after dashboard-aligned deployment pressure engaged. Recent filled orders included `USO`, `ABBV`, `AMGN`, `SOL-USD`, and `AAVE-USD`. Remaining gap is now mostly stale-price drift, minimum-order/dust normalization, and candidate breadth/quality, not Ollama or broker connectivity.
- Last completed task: **D139 — Trading Engine Profit Optimization & Loss Minimization**. Implemented four critical improvements: (1) Kelly Criterion position sizing as a selectable option in `system/adaptive_sizing.py` and `signals/engine.py` using strategy rolling performance statistics queried from the `fills` table; (2) Self-tuning adaptive slippage adverse drift thresholds in `execution/engine.py` scaling the stale price gate dynamically based on rolling average fills slippage; (3) Asymmetric EWMA market state score smoothing in `risk/regime_state.py` persisting smoothed score in `ControlState` via the `CommandBus` to de-risk instantly ($\alpha=0.20$) and recover cautiously ($\alpha=0.05$); (4) Smart order routing fee and borrow cost optimization in `execution/router.py` sorting venues by net transaction costs (spread + taker fees + slippage + borrow costs by hold days). Added comprehensive unit and integration tests in `tests/test_engine_improvements.py` and verified 100% success rate across all 1701 unit tests and verified the live startup sequence.
- Last completed task: **D138 — TimescaleDB PriceHistory hypertable compatibility**. Modified `PriceHistory` database model in `storage/models.py` to use a composite primary key consisting of `(timestamp, symbol, timeframe, broker)` instead of the auto-incrementing integer `id`. This satisfies TimescaleDB's requirement that any unique index on a hypertable must include the partitioning column (`timestamp`). Added an Alembic migration (`c7d7fd4e679c_price_history_composite_pk.py`) to safely deduplicate rows, drop the `id` column/constraint, and apply the composite primary key on existing databases. Verified that `price_history` is successfully registered as a TimescaleDB hypertable in PostgreSQL and all 1685 unit tests pass.
- Last completed task: **D137 — Risk clamping and liquidity check optimization**. Resolved two critical trading loop flaws: (1) Updated single-name and daily-add checks in `risk/engine.py` to use signed quantities, allowing position-reducing trades to pass through unconditionally rather than being clamped to remaining cap room or rejected. Stamped `risk_position_reducing` on signals to skip recording them as daily adds on fill in `execution/engine.py`. (2) Capped instantaneous order book depth checks dynamically at `max(5x order notional, $20k)` in `execution/engine.py` to resolve false-positive rejections on highly liquid ETFs/stocks (GLD, TLT, ADBE, USO, etc.) caused by mismatch against the $1M daily-volume threshold. Added unit test coverage in `tests/test_d125_risk_caps.py`. Verification: `python -m pytest tests/test_d125_risk_caps.py tests/test_execution_engine.py tests/test_paper_wallet.py tests/test_ibkr_market_data_mode.py tests/test_execution_marketable_limits.py -q` (`114 passed`).
- Last completed task: **D136 — IBKR FX close/pre-check fallback**. Diagnosed why positions were not closing: profit-harvest was correctly issuing reduce-only EURUSD/AUDUSD exits and risk was allowing them, but execution pre-checks rejected them because `IBKRAdapter.get_order_book()` required market-depth rows and IBKR FX depth/top-of-book DOM was empty. Updated `brokers/ibkr/adapter.py::get_order_book()` to fall back from empty/failed `reqMktDepth` to a one-shot bid/ask snapshot via `reqMktData`, preserving spread/slippage checks while avoiding a DOM-depth entitlement dependency for FX reduce/close orders. FX snapshots that omit bid/ask size receive a conservative synthetic size for liquidity checks. Added regression coverage in `tests/test_ibkr_market_data_mode.py`. Verification: `python -m py_compile brokers/ibkr/adapter.py tests/test_ibkr_market_data_mode.py`; `python -m pytest tests/test_ibkr_market_data_mode.py tests/test_execution_marketable_limits.py tests/test_execution_engine.py -q` (`54 passed`).
- Last completed task: **D135 — IBKR market-data subscription hardening**. Diagnosed the IBKR log storm as error `10089` real-time API market-data entitlement failures followed by `300` cancel-after-rejected-snapshot noise. Updated `brokers/ibkr/adapter.py` so `IBKR_MARKET_DATA_TYPE=auto` defaults paper sessions to delayed market data (`reqMarketDataType(3)`) and live sessions to real-time (`reqMarketDataType(1)`), with explicit env overrides for real-time/frozen/delayed/delayed-frozen. Applied the selected market-data type before one-shot price reads, option market-data snapshots, streaming subscriptions, and after connect/rescue paths. Removed `cancelMktData` calls for one-shot snapshot requests so rejected/auto-ending snapshots no longer produce secondary `Can't find EId with tickerId` floods; streaming subscriptions still cancel on cleanup. Added `tests/test_ibkr_market_data_mode.py`. Verification: `python -m py_compile brokers/ibkr/adapter.py tests/test_ibkr_market_data_mode.py`; focused IBKR tests `12 passed`; execution/price/stale-gate slice `64 passed`.
- Last completed task: **D134 — Production deployment-stage manager + activation UI**. Implemented first-class deployment stages (`paper` → `micro_live` → `live`) via `config/deployment.yaml` and `system/deployment.py`, with persisted `ControlState` overrides, promotion evidence, explicit env/M8/live-arming/full-live checks, startup blocking before broker connect, and API endpoints `GET /deployment/readiness`, `GET /deployment/stage`, `POST /deployment/promote`, `POST /deployment/demote`. Added a redesign Activation screen showing the current stage, runtime mode, days left, evidence metrics, stage cards, promotion progress, blockers, and checkbox-style completed/pending promotion checks; wired it into navigation, command palette, API types, and polling. Added focused deployment tests. Verification: `python -m py_compile system/deployment.py api/server.py system/broker_manager.py system/orchestrator.py tests/test_deployment.py`; `python -m pytest tests/test_deployment.py -q` (`3 passed`); `npm run build` in `ui/` clean. Browser plugin was unavailable in this session, so visual verification was limited to the production build.
- Last completed task: **D133 — Final publication, licensing and contribution policy documentation**. Updated project documentation and metadata to state that myTbot is open source under AGPL v3, published by Emfasys Labs, with separate commercial licences available from Emfasys Ltd for organisations requiring terms outside AGPL v3. Added/updated `LICENSE`, `README.md`, `CONTRIBUTING.md`, `COMMERCIAL.md`, `DISCLAIMER.md`, `SECURITY.md`, `docs/LICENSING.md`, GitHub Issue/PR templates, package licence metadata, and public-facing architecture/spec wording. Policy now welcomes GitHub Issues but states outside Pull Requests are not currently accepted and should be closed unmerged with contributors redirected to Issues.
- Milestone: M10 — Local-First AI Architecture ✅
- Last completed task: **D132 — Bleed-stopper safety rebuild for fully deployed trading**. Implemented the seven requested fixes after the trading-engine review. `portfolio/allocation_engine.py` no longer lets `capital_pct=1.0` interpolate drawdown throttle, execution quality, volatility overlays, or Wave 8 safety to `1.0`; `config/allocation.yaml::gross_exposure.unleash` now governs only partial opportunity-shape relaxation. Added `risk/drawdown_governor.py` plus a persisted `RiskEngine` open lock so intraday derisk tier-2 can block fresh opens while allowing reduce-only exits/hedges. `execution/engine.py` no longer exempts top-ups from Wave 9; genuine top-ups get a scaled cost cushion, and D125 single-name clamps no longer stamp `sizing_topup_existing`. Paper mode now enforces spread/liquidity/slippage pre-checks instead of filling `PAPER FILL (limits bypassed)`. `risk/engine.py` now adapts confidence and trade-quality thresholds from market state + rolling win-rate inside hard bands, and FX/equity-index/crypto cluster caps scale the configured base cap by `market_state_score`. Updated config, tests, and `docs/DECISIONS.md` D132. Verification: `python -m py_compile ...`; focused suites `62 passed`, `3 passed`; broader suites `92 passed, 1 skipped` and `49 passed`.
- Last completed task: **D125.4 — Live deployment constraints cleared through execution/session routing**. Continued the post-D126 low-deployment audit after the UI still showed ~20% at work. Live logs showed the bottleneck had moved from the original D125 cap veto to four downstream constraints: clamped existing-position top-ups were still rejected by `theme_uniqueness`; canonical `*-USD` crypto kept hard-routing to Kraken after Kraken's synthetic paper deploy room was zero; after Binance filled its same-cycle venue reservation, later crypto orders still skipped instead of falling through to Bybit; and IBKR/Alpaca equities/ETFs were treated as closed during the real 04:00–20:00 ET extended-hours window. Fixes: `risk/engine.py` marks single-name clamped top-ups and exempts them from theme uniqueness; `execution/router.py` falls back from exhausted Kraken to Binance spot then Bybit for USD-quoted crypto; `brokers/binance/adapter.py` and `brokers/bybit/adapter.py` map canonical `*-USD` crypto to `*USDT` venue books; `execution/engine.py` reroutes no-native-paper crypto orders when same-cycle venue reservations exhaust the chosen venue; `core/market_session.py` plus `config/market_hours.yaml` add broker-aware `by_asset_class_extended` tradeability for IBKR/Alpaca while keeping session-exit close management anchored to the regular close unless `MARKET_SESSION_EXTENDED=1`. Verification: focused suite `193 passed, 1 skipped`. Live verification after restart: first extended-hours cycle executed 12/20 selected actions and moved at-work from ~20% to 60.1%; next cycle moved it to 69.3%, with gross exposure ~$1.142M on ~$1.224M NAV (~93%). Remaining UI gap is now candidate breadth / cash-factor math, not the original execution/routing wall.
- Last completed task: **D125.3 — IBKR crypto registry guard**. Continued the low-deployment investigation after D125.2. Live logs still showed repeated IBKR API errors like `Stock(symbol='BTC-USD', exchange='SMART', currency='USD')` plus account-summary request pressure, meaning canonical crypto symbols from registry/qualification and market-data paths were leaking into IBKR as stock contracts. Fixed `instruments.canonical.canonical_to_broker()` so IBKR only exposes known PAXOS crypto bases as bare symbols (`BTC`, `SOL`, etc.) and returns `None` for unsupported crypto such as `AAVE-USD`; fixed `brokers/ibkr/adapter.py` so canonical supported crypto (`BTC-USD`) builds a `Crypto(..., "PAXOS", "USD")` contract while unsupported canonical crypto fails/skips locally for qualification, order, snapshot-price, and stream paths instead of hitting TWS. Added coverage in `tests/test_instruments_canonical.py`, `tests/test_instruments_availability.py`, and `tests/test_ibkr_universe_qualification.py`. Focused verification: `python -m py_compile instruments/canonical.py brokers/ibkr/adapter.py tests/test_instruments_canonical.py tests/test_instruments_availability.py tests/test_ibkr_universe_qualification.py`; `python -m pytest tests/test_d125_risk_caps.py tests/test_paper_wallet.py tests/test_execution_engine.py tests/test_instruments_canonical.py tests/test_instruments_availability.py tests/test_ibkr_universe_qualification.py -q` (`96 passed`). Documented as `docs/DECISIONS.md` D125.3.
- Last completed task: **D125.2 — Crypto paper-wallet room clamps instead of skips**. Continued the low-deployment audit after D125.1. Live post-restart state showed D125.1 working (6 new IBKR FX fills around the 5%-NAV cap, 8 positions total), with the remaining execution gap concentrated in `crypto_venue_capital_exhausted` plus expected pre-market US equity `market_closed` skips. Root cause: no-native-paper crypto venues use per-venue synthetic wallets (`system/paper_wallet.py`, default $50k seed), but post-risk crypto orders were clamped to ~5% of total NAV (~$61k), larger than Kraken's remaining venue room after existing shorts. `execution/engine.py` now clamps opening Kraken/Binance/Bybit paper crypto orders down to `venue_deploy_room` instead of skipping when room is positive, stamps `crypto_venue_room_clamped` / `crypto_venue_room` metadata, and still skips only for true zero-room/no-price cases. Added an in-process per-venue reservation keyed to the current wallet-room snapshot so multiple orders in the same loop cannot each consume the same stale room before the heartbeat refreshes. Added focused coverage in `tests/test_execution_engine.py`; verification: `python -m py_compile execution/engine.py tests/test_execution_engine.py`; `python -m pytest tests/test_d125_risk_caps.py tests/test_paper_wallet.py tests/test_execution_engine.py -q` (`67 passed`). Documented as `docs/DECISIONS.md` D125.2.
- Last completed task: **D121 — Dynamic slider unleash + risk-bypass interpolation**. Added a smooth unleash scaling factor $u = (\text{capital\_pct} - 0.9) / 0.1$ for $\text{capital\_pct} > 0.9$. Linearly interpolated all gross exposure dampeners (aggression, market state shaping, execution/drawdown throttles, vol targeting overlays) and dynamic position weight ceilings (risk parity vol cap, liquidity constraints, crash/panic/volatile regime multipliers) towards $1.0$ (or mode-based `nuclear_max`). When the capital slider is pushed to 100%, all risk dampeners are completely bypassed to fulfill the loss minimization + profit maximization mandate. Added target metadata tracking for `unleash_u` in allocation decisions. Updated `tests/test_adaptive_sizing.py` and created `tests/test_allocation_unleash.py` with comprehensive coverage. Verified full regression test suite (1478 passed, 0 failures).
- Last completed task: **D120 — Forced data-pipeline discovery on under-allocation**. Added a thread-safe `pipeline_wake_event` (`asyncio.Event`) to `Orchestrator` enabling immediate interruption of the discovery pipeline sleep loop via `asyncio.wait_for`. Implemented exposure calculation and target deployment checks at the end of each `TradingLoop` iteration. When unallocated capital exceeds a dynamic threshold (NAV * `default_position_pct`, clamped between 1% and 10% of NAV) and the loop interval cooldown has elapsed, the trading loop triggers the wake event to force a universe rebuild. Added comprehensive unit coverage in `tests/test_forced_discovery.py` covering exposure tracking, cooldown, and dynamic thresholds. Verified full regression test suite (1476 passed, 0 failures).
- Last completed task: **D119 — Dynamic order quantization + 100% dynamic risk & execution limits**. Added broker-specific `quantize_quantity` and `quantize_price` rounding across all broker adapters (Alpaca, IBKR, Bybit, Binance, Kraken, and `_template`). Refactored `_normalize_order_for_broker` in `execution/engine.py` to be `async` and utilize these functions prior to submission. Scaled `min_liquidity_usd` in `execution/engine.py` dynamically by `symbol_vol_scalar`. Scaled `max_consecutive_losses` in `risk/engine.py` dynamically based on `market_state_score` and `market_volatility_scalar`. Loaded `portfolio_state` regime metadata in `_run_stop_loss_tick` inside `system/orchestrator.py` to dynamically scale stop thresholds (`max_loss_per_trade_pct` and `position_stop_pct`) by the ratio of `market_state_score / market_volatility_scalar`. Added robust fallback parameters for missing telemetry. Patched `MARKET_SESSION_GATE` in all stop-loss tests to be timezone-independent. Ran full test suite (1469 tests passed, 0 failures).
- Last completed task: **D118 — Self-tuning priority pre-filter + 6-stage universe funnel**. Replaced stratified-random candidate selection with a deterministic, self-tuning rule that scores every `unique_normalized` symbol (microseconds per symbol; no I/O) and picks the top-N. Both the per-component **weights** and the scoring **budget N** are self-tuning, with **no operator-tunable numbers** — only master kill switches and fixed safety bounds. New modules: `data/universe_score_ages.py` (atomic JSON `symbol -> {last_scored_at, last_score, score_count, first_seen_at}`, deterministic LRU eviction), `data/universe_prefilter.py` (pure `compute_priority_scores()` over 6 bounded components — `liquidity_prior`, `anchor_pin`, `freshness_bonus`, `registry_availability`, `asset_class_balance`, `region_balance` — plus `top_n_by_priority` with anchor pinning), `data/universe_weight_learner.py` (online logistic regression with AdaGrad + EWMA decay + per-weight `[0.05, 0.50]` clamping + sum-to-1.0 re-normalisation; labels each picked symbol by whether it actually entered `watching` and steps weights to maximise that labelled likelihood; persists weights + sparkline history), `data/universe_budget_controller.py` (AIMD throughput control + utility-saturation detection; AIMD adds +25 on under-budget cycles and ×0.75 on over-budget cycles; saturation shrinks toward the deepest watching rank when extra scoring is no longer yielding new watching members; hard `[200, 800]` clamps; exposes the currently *binding* constraint `aimd_grow/aimd_shrink/utility_saturation/floor/ceiling/stable`), `data/universe_transitions.py` (ring buffer with `(ts, symbol, from_tier, to_tier, reason, score_delta)`). `data/universe_builder.py::build_tiered_universe` now accepts `priority_scores`, `target_budget`, `anchors`, and a `BuildTelemetry` collector; falls back to legacy stratified sampling when no scores are passed (full backward compat). `system/orchestrator.py::_pipeline_runner` loads state → computes priority scores → derives budget → builds tiers → updates `ScoreAges`/`WeightLearner`/`BudgetController` → diffs and records tier transitions, all atomically. `universe/snapshot_service.py` now publishes a 6-stage funnel (`unique_normalized` → `priority_ranked` → `scored` → `watching` → `promoted` → `active_reps`; the raw broker-listing count is a debug tooltip on `unique_normalized`, not a stage), plus `priority_rule` (live learned weights + history + budget state + binding constraint + score-age summary), `transitions`, and `asset_class_coverage`. Per-symbol rows now carry `last_scored_at`, `last_score`, `score_count`, and `priority_breakdown`. UI: extended `ui/src/app/lib/api.ts` (new `UniversePriorityRule`, `UniverseTransition`, `UniverseAssetClassCoverage` types; `UniverseSymbolRow` gains score-age + breakdown fields); `ui/src/app/redesign/universe/UniverseScreen.tsx` renames funnel stages, adds a `PriorityRuleCard` with weights bar chart + per-component sparklines + budget meter + binding-constraint label, new `CoverageTab` (asset-class progress chips), new `TransitionsTab` (filterable activity stream), and a `last scored` column with coloured age stripe + `priority_breakdown` in the native tooltip on InstrumentsTab. Config surface is intentionally minimal — `config/data_pipeline.yaml::dynamic_universe.ranking.priority_score` has only `enabled`, `weight_learning_enabled`, `budget_self_tune_enabled`, and `state_dir`. Tests: `tests/test_universe_score_ages.py`, `tests/test_universe_prefilter.py`, `tests/test_universe_weight_learner.py`, `tests/test_universe_budget_controller.py`, `tests/test_universe_transitions.py`, `tests/test_universe_builder_priority_selection.py`, `tests/test_universe_snapshot_d118.py`. Existing `tests/test_universe_intelligence.py` updated to the `active_reps` label. UI `npm run build` clean. See `docs/DECISIONS.md` D118.
- Last completed task: **D117 — Adaptive universe-tier sizing (regime + signal pressure + cluster-aware + anti-churn)**. Replaced the pre-D117 static `max_symbols=300 / core_max=50 / scan_max=250 / max_candidates_to_score=400` caps with a per-tick adaptive policy that widens the discovery funnel in `risk_on/volatile/trend_up` (multipliers `1.25/1.30/1.15`) and narrows it in `risk_off/crash/range` (`0.80/0.65/0.90`), plus a signal-pressure axis (`batch_candidate_count >= 8 → 1.20`, `<= 2 → 0.80`) and a cluster-aware floor `watching >= max(150, 3 * active_reps)`. All resolved caps are clamped to YAML bounds (`candidates 200–800`, `watching 150–600`, `core 25–100`, `scan 75–500`). Added anti-churn hysteresis: a symbol that was in the previous watchlist must miss `3` consecutive rebuilds before it actually drops to `light`, preventing single-liquidity-blip feature-ingest churn. Files: `universe/adaptive_caps.py` (pure policy + churn helper), `universe/adaptive_context.py` (async loader from `dashboard.snapshot` + `universe_intelligence.json`), `universe/adaptive_state.py` (atomic JSON persistence at `data/runtime/universe_adaptive_state.json`), `config/data_pipeline.yaml::dynamic_universe.adaptive`, `data/universe_builder.py::UniverseBuilder.update_caps()`, `system/orchestrator.py::_pipeline_runner` (compute context → resolve caps → update builder → apply hysteresis → re-save tiers → persist state), `universe/snapshot_service.py::_pipeline_caps()` + `_adaptive_caps_block()` (overlay resolved caps + new `adaptive` snapshot block), `ui/src/app/lib/api.ts` (extended `IntelligenceUniverseResponse.adaptive` type), `ui/src/app/redesign/universe/UniverseScreen.tsx` (new `adaptive Nx ↑ widen / ↓ focus` badge with reasons tooltip + base-vs-resolved caption). Risk / order routing untouched: this is a discovery-layer control only. Backward compatible: set `adaptive.enabled: false` (or omit the block) and the builder uses static caps exactly like pre-D117. Verification: `python -m py_compile universe/adaptive_caps.py universe/adaptive_context.py universe/adaptive_state.py data/universe_builder.py system/orchestrator.py universe/snapshot_service.py`; `python -m pytest tests/test_universe_adaptive_caps.py -q` (`26 passed`); regression run `python -m pytest tests/test_universe_intelligence.py tests/test_universe_tiers.py tests/test_universe.py tests/test_universe_adaptive_caps.py tests/test_orchestrator_stop.py tests/test_api_dashboard_extras.py -q` (`71 passed, 1 skipped`); UI typecheck via `ReadLints` clean.
- Last completed task: **D116.1 — Universe page wired to the D116 instrument registry**. Closed the "I don't see any changes" gap reported after D116 went live: the registry payload was populated, but the Universe page UI was still showing pre-D116 numbers because (a) the IBKR `ibkr_supported_symbols_use_registry` feature flag in `config/instrument_registry.yaml` was still off and (b) the redesign Universe screen did not render the new `registry_known_count` / `registry_covered_count` fields. Fixes: flipped `ibkr_supported_symbols_use_registry: true` (orders still call `qualifyContractsAsync` so trade safety is unchanged); updated `universe/snapshot_service.py::_build_coverage` to derive the IBKR `source` / `note` from real raw counts (`curated_seed` pre-flag, `curated_seed+registry` post-flag with descriptive note); extended `IntelligenceUniverseResponse.coverage.by_broker` typing in `ui/src/app/lib/api.ts` with `registry_known_count` / `registry_covered_count`; updated `ui/src/app/redesign/universe/UniverseScreen.tsx::OverviewTab` to (1) replace the stale `ibkr 61 curated` substring with a dynamic `curated` vs `curated+registry` tag plus a `registry NNN known / NNN covered` summary, and (2) render a per-broker chip row showing `<name> · <raw> listed · <covered>/<known> registry` colored green when fully covered, blue when partially covered, amber when zero covered. Verification: `python -m py_compile universe/snapshot_service.py`; `npm run build` in `ui/`; `python -m pytest tests/test_universe_intelligence.py tests/test_universe.py tests/test_universe_tiers.py tests/test_asset_class_routing.py tests/test_ibkr_universe_qualification.py tests/test_ibkr_supported_symbols_from_registry.py -q` (`55 passed`); full `python -m pytest -q` (`1323 passed, 3 skipped, 5 warnings` — same pre-existing torch + AsyncMock warnings as before). Restarted the supervised `run.py` and verified the live `/intelligence/universe` payload: `broker_listing_count` rose from `16,442` to `17,980`, `unique_normalized_count` from `14,828` to `15,319`, IBKR raw from `61 (curated_seed)` to `1,599 (curated_seed+registry)`, active reps from `88` to `92`. Per-broker registry coverage: `alpaca 899/903`, `ibkr 13/1615` (the 1,602 balance are `requires_qualification` and will flip to `available` as IBKR `qualifyContractsAsync` warms the cache via `scripts/qualify_instrument_registry.py`), `binance 0/1003`, `kraken 0/1415`, `bybit 0/1615` — the 0-covered crypto venues are an accurate reflection that the seeded D116 sources (Wikipedia + iShares + static FX/futures) contribute equity-heavy canonicals; adding a CoinGecko-style crypto source is a clean follow-up. Live system: `state=running`, `pipeline_running=true`, `capital_pct=1.0`, all five brokers connected, `coverage.full=true`.
- Last completed task: **D116 — Instrument Registry + Cross-Broker Availability Resolver**. Replaced hand-maintained per-broker symbol lists (especially IBKR's 61-line curated YAML seed) with a self-updating master instrument registry. Schema: `instrument_registry` (canonical PK, asset class, region, exchange, currency, sector/industry, ISIN, FIGI, timestamps, retired_at, metadata) + `instrument_source_membership` + `instrument_broker_availability` (status enum `unknown/available/unavailable/requires_qualification/blocked`) + `instrument_source_runs` audit log; migration `alembic/versions/d116a1b2c3d4_instrument_registry.py`. Canonical symbol form in `instruments/canonical.py` (yfinance conventions: `AAPL`, `HSBA.L`, `7203.T`, `BTC-USD`, `EURUSD=X`, `ES=F`). Source adapters in `instruments/sources/`: `wikipedia.py` (~20 indices), `ishares.py` (~40 ETF holdings CSVs), `openfigi.py` (ISIN/FIGI bulk mapping), `static_fx.py`, `static_futures.py`, `broker_catalog.py` (wraps `BrokerAdapter.get_supported_symbols()` per connected broker). All HTTP through `instruments/sources/http.py` (User-Agent, per-host rate limit, ETag/Last-Modified, retry with jitter). `instruments/availability.py::resolve_broker_availability()` walks the registry, attempts `instruments.canonical.canonical_to_broker` translation, falls back to OpenFIGI alternates, marks IBKR-only unknowns as `requires_qualification`. `instruments/builder.py` orchestrates refresh + availability with retire policy (min 5 consecutive misses across min 2 independent sources; nothing ever hard-deleted). `instruments/scheduler.py` runs four background tasks (constituents refresh, broker availability, OpenFIGI enrichment, broker-connect consumer); `system/broker_manager.py::register_connect_callback()` triggers immediate re-evaluation on every (re)connect. `brokers/ibkr/adapter.py::get_supported_symbols()` now returns the union of curated YAML seed + qualification cache + registry rows behind `IBKR_SUPPORTED_SYMBOLS_USE_REGISTRY` env / YAML feature flag (default off until populated); failures fall back silently to curated YAML and `place_order()` still calls `qualifyContractsAsync` before submission. `universe/snapshot_service.py` exposes `registry_known_count` + `registry_covered_count` per broker. New API endpoints: `GET /intelligence/instruments`, `GET /intelligence/instruments/{canonical}`, `GET /intelligence/instrument-sources`. CLI: `python scripts/build_instrument_registry.py --sources=all --dry-run`, `python scripts/qualify_instrument_registry.py --broker=ibkr --limit=100`. Config: `config/instrument_registry.yaml`. Docs: `docs/INSTRUMENT_REGISTRY.md`, `docs/DECISIONS.md` D116, `docs/ARCHITECTURE.md`. What did NOT change: `brokers/base.py`, `risk/engine.py`, `execution/engine.py`, `signals/*`, `strategies/*`, `portfolio/*`. Tests (59 passing): `tests/test_instruments_canonical.py` (13), `tests/test_instruments_registry.py` (7), `tests/test_instruments_sources_wikipedia.py` (4), `tests/test_instruments_sources_ishares.py` (4), `tests/test_instruments_sources_openfigi.py` (4), `tests/test_instruments_sources_broker_catalog.py` (5), `tests/test_instruments_availability.py` (8), `tests/test_instruments_builder.py` (6), `tests/test_instruments_scheduler.py` (3), `tests/test_ibkr_supported_symbols_from_registry.py` (4). Added `lxml>=4.9,<7` to `requirements.txt` (required by `pandas.read_html`). Verification: `python -m py_compile` for every new module; `python -m pytest tests/test_instruments_*.py tests/test_ibkr_supported_symbols_from_registry.py -q` → `59 passed`.
- Last completed task: **D115 — Anti-churn + cluster-aware risk + intraday derisk + stale-price gate**. Five-tier rectification after the 2026-05-19 paper P&L bleed audit (224 fills / $6.0M turnover / $1.18M NAV / ~$5k drag / no individual stop fired). Tier 1: `signals/anti_churn.py::AntiChurnGate` wired into `SignalEngine.process()` and `raw_to_signal_candidate()` BEFORE meta-label, with engine.record_fill() called from `system/trading_loop/loop.py` on every confirmed fill — stops same-strategy 90s dedup, cross-strategy long/short contradiction with 5-min tombstone, and mode-aware post-fill cooldown (hunter 120s / trader 180s / defender 600s). Tier 2: `risk/engine.py::_check_fx_cluster_exposure` (15% NAV signed-USD cap with xxxUSD vs USDxxx orientation rules) and `_check_equity_index_cluster_exposure` (20% NAV signed cap on SPY/QQQ/IWM/DIA/VTI/VOO/IVV/MDY/TQQQ/SQQQ/SPXL/SPXS) — reduce-only and neutralising legs never blocked. Tier 3: `risk/intraday_derisk.py` graduated tiers (-0.5% → 20% trim of worst 2, -1.0% → 50% trim of worst 4, -1.5% → full close of worst 6) wired as `Orchestrator._intraday_derisk_loop` / `_run_intraday_derisk_tick` — sits before `max_daily_loss_pct`, all reduce-only, still passes through risk + execution. Profit-harvest peak persistence already in place via `Orchestrator._persist_profit_harvest_peaks`. Tier 4: `execution/engine.py::_simulate_fill` rejects opening paper fill when `signal.suggested_price` drifts against trade direction by more than `stale_price_gate.max_adverse_drift_bps` (default 25 bps) — returns REJECTED OrderResult with `filled_quantity=0`, sets `last_skip_reason`; reduce-only exempt. Strategy-side hygiene (item 10) superseded by Tier 1 engine dedup. Tier 5: `scripts/flatten_orphaned_remnants.py` operator housekeeping tool, paper-only, dry-run-first, filters by notional ceiling / symbol / broker / loss-pct. Backtest harness disables anti-churn + stale-price gates for the duration of the run (wall-clock semantics do not apply when replaying historical bars in ms). Config: `config/strategies.yaml::signal_engine.anti_churn` + `config/risk_limits.yaml::fx_cluster` / `equity_index_cluster` / `intraday_derisk` / `stale_price_gate`. Verification: `python -m py_compile signals/anti_churn.py signals/engine.py risk/engine.py risk/intraday_derisk.py execution/engine.py system/orchestrator.py system/trading_loop/loop.py scripts/flatten_orphaned_remnants.py backtest/harness.py`; full `python -m pytest -q` → `1235 passed, 3 skipped, 1 warning` (warning is pre-existing AsyncMock fixture leak in `test_profit_harvest.py`); new suites `tests/test_anti_churn_gate.py` (17), `tests/test_fx_cluster_exposure.py` (9), `tests/test_equity_index_cluster.py` (6), `tests/test_intraday_derisk.py` (11), `tests/test_stale_price_gate.py` (7), `tests/test_flatten_orphaned_remnants.py` (5).
- Last completed task: **D114 — Session-exit policy embedded in market-session intelligence**. Added broker-aware session close timing helpers in `core/market_session.py` (`session_close_at`, `minutes_to_session_close`) and a governed `core/session_exit_policy.py` layer backed by `config/session_exit_policy.yaml`. The policy evaluates each position into `hold_through_close`, `trim_before_close`, `close_before_close`, or `defer_action` using broker/asset session, time-to-close, strategy horizon, overnight permission, profile mode, and unrealised return. Wired `GlobalEdgeCoordinator.propose_session_exit_actions()` and `TradingLoop._run_global_edge_tick()` so qualifying decisions become normal reduce-only `trim_symbol` actions that still pass through SignalEngine, RiskEngine, router, and ExecutionEngine; swing/position theses default to holding through close, intraday/no-overnight positions close near the bell, and Defender can bank profitable swing exposure. Added regression coverage in `tests/test_session_exit_policy.py` and extended `tests/test_market_session.py`. Verification: `python -m py_compile core/market_session.py core/session_exit_policy.py portfolio/global_edge_coordinator.py system/trading_loop/loop.py tests/test_market_session.py tests/test_session_exit_policy.py`; `python -m pytest tests/test_market_session.py tests/test_session_exit_policy.py tests/test_global_edge_coordinator.py -q` (`62 passed`).
- Last completed task: **D113 — Connect Hub connector taxonomy and safer controls added**. Refined Connect Hub so connector categories are no longer treated as equal. Marked `rules` in `config/connectors.yaml` as `core_required` / `non_disableable` with notes; UI now labels the section **AI pipeline**, hides disable/delete for core components, and shows Rules as an always-on baseline rather than a removable AI model. Added broker exposure awareness to `POST /connect/enable` and `/connect/delete`: disabling/deleting brokers now checks live positions/open orders, refuses destructive removal unless the caller explicitly chooses `block_new_only`, and applies the risk-layer broker gate for immediate new-routing block. UI disable for brokers now confirms the block-new-only behavior instead of implying silent liquidation; a dedicated flatten/disconnect workflow is still needed for automatic position closure. Hid disabled/unconfigured treasury placeholders from the card grid while keeping **Add connector** available, and changed connector grids to fixed-width cards so adding/removing connectors does not stretch cards unpredictably. Updated `docs/CONNECT_HUB.md`. Restarted supervised backend; runtime returned `state=running`, `pipeline_running=true`, active brokers `alpaca/binance/bybit/ibkr/kraken`, `connect_hub=true`. Verification: `python -m py_compile api/server.py system/connect_hub.py system/broker_manager.py tests/test_connect_hub.py`; `python -m pytest tests/test_connect_hub.py tests/test_connector_contracts.py tests/test_broker_balance_ready.py -q` (`23 passed`); UI `npm run build`.
- Last completed task: **D112 — Connect Hub enable/disable/delete controls added**. Added guarded connection lifecycle controls for any connector. Backend: added `POST /connect/enable` and `POST /connect/delete` in `api/server.py`; added `delete_connector_manifest()` in `system/connect_hub.py`; made `system/broker_manager.py` respect disabled/deleted broker manifests on future starts. Disabling/deleting a broker now also applies an immediate risk-layer broker gate (`RiskEngine.disable_broker`) so new orders cannot route there while the current process is still running; enabling re-opens that risk gate. AI enable/disable also updates matching `config/ai.yaml` provider rows where present. Delete removes the connector from Connect Hub but intentionally leaves `.env` secrets untouched for manual audit/removal. Frontend: Connect cards now expose **Configure**, **Enable/Disable**, and **Delete** actions, with delete confirmation and control-token retry prompt. Updated `docs/CONNECT_HUB.md`. Restarted the supervised backend child so the new endpoints are live; runtime returned `state=running`, `pipeline_running=true`, active brokers `alpaca/binance/bybit/ibkr/kraken`, `connect_hub=true`. Verification: `python -m py_compile system/connect_hub.py system/broker_manager.py api/server.py tests/test_connect_hub.py`; `python -m pytest tests/test_connect_hub.py tests/test_connector_contracts.py tests/test_broker_balance_ready.py -q` (`23 passed`); UI `npm run build`.
- Last completed task: **D111 — Premium LLM fallback enabled in Connect Hub and AI router**. Investigated why the Connect screen showed Premium LLM fallback as `off` despite an Anthropic API key being present in `.env`: both switches were still disabled (`config/connectors.yaml::ai_providers.premium_fallback.enabled=false` and `config/ai.yaml::providers.premium_fallback.enabled=false`). Enabled both. This does **not** make paid LLM calls unconditional; escalation remains governed by `config/ai.yaml::escalation` necessity gates and emergency keywords. Restarted the supervised backend child so `.env` and AI config reloaded. Runtime verification: `/system/status` returned `state=running`, `pipeline_running=true`, active brokers `alpaca/binance/bybit/ibkr/kraken`, `connect_hub=true`; `/connect/hub` now reports `premium_fallback` as `enabled=true`, `configured=true`, `connected=true`, `healthy=true`, `state=ready`, provider `anthropic`, model `claude-sonnet-4-5`. Verification: `python -m py_compile system/connect_hub.py api/server.py`; `python -m pytest tests/test_ai_pipeline.py tests/test_connect_hub.py -q -k "premium or connect_hub or ai_yaml"` (`7 passed, 9 deselected`).
- Last completed task: **D110 — Add Connector wizard for brand-new parties added**. Extended Connect Wizard beyond existing cards. Added `POST /connect/add` in `api/server.py`, guarded by `X-Control-Token`, which creates a new non-secret connector manifest row for brokers, information feeds, AI providers, or treasury accounts. Added `system/connect_hub.py::add_connector_manifest()` plus broker adapter scaffolding from `brokers/_template` into `brokers/<connector_id>/adapter.py` for brand-new trading platforms; the scaffold is intentionally not auto-registered for live trading until implemented and added to `brokers/registry.py`. Added UI **Add connector** button/modal on the Connect screen, letting the user choose category, display name, connector id, auth type, and env var names. Added tests for custom manifest creation. Updated `docs/CONNECT_HUB.md`. Restarted the supervised backend child so `/connect/add` is live; runtime returned `state=running`, `pipeline_running=true`, active brokers `alpaca/binance/bybit/ibkr/kraken`, `connect_hub=true`. Verification: `python -m py_compile system/connect_hub.py api/server.py tests/test_connect_hub.py`; `python -m pytest tests/test_connect_hub.py tests/test_connector_contracts.py -q` (`7 passed`); UI `npm run build`. Scope note: unknown broker protocols still need adapter implementation because the system cannot safely place orders against an API it does not know; the wizard now creates the tracking/scaffold path instead of leaving users stuck.
- Last completed task: **D109 — Connect Wizard credential save flow added**. Added the first operator-facing Connect Wizard so users no longer need to edit `.env` manually for declared connectors. Backend: added guarded `POST /connect/configure` (`X-Control-Token` / `API_CONTROL_TOKEN`) in `api/server.py`; it validates the selected connector against `config/connectors.yaml`, accepts only manifest-declared env vars, upserts them into local `.env` via `system/connect_hub.py::update_env_file()`, optionally enables the connector manifest, enables matching `config/ai.yaml` provider rows for AI connectors, never echoes secret values, and returns a fresh Connect Hub snapshot plus next-step guidance. Frontend: each Connect card now has **Configure**, opening a modal wizard with only the connector's required fields plus optional control-token entry; saving calls `api.configureConnector()` and refreshes live state. Added tests for safe `.env` upsert and endpoint validation. Restarted the supervised backend child so `/connect/configure` is live; runtime returned `state=running`, `pipeline_running=true`, active brokers `alpaca/binance/bybit/ibkr/kraken`, `connect_hub=true`. Verification: `python -m py_compile system/connect_hub.py api/server.py tests/test_connect_hub.py`; `python -m pytest tests/test_connect_hub.py tests/test_connector_contracts.py -q` (`6 passed`); UI `npm run build`. Scope note: this wizard works for connectors declared in the manifest; truly unknown broker protocols still require an adapter, OAuth flows are not implemented yet, and treasury movement remains disabled.
- Last completed task: **D108 — Connect Hub runtime made live in dashboard**. Fixed the initial Connect screen showing empty categories because the long-running backend process was still serving the pre-D107 API (`/system/status` had no `connect_hub`, and `/connect/hub` fell through to the SPA HTML). Added a resilient UI fallback in `ui/src/app/redesign/useLiveSystem.ts`: refresh now calls `api.getConnectHub()` directly and, if the endpoint is absent/old, derives a useful Connect Hub view from existing `/system/status` broker/news data plus local AI defaults. Restarted the supervised `run.py` child so the real backend endpoint is active. Runtime verification: `/system/status` now has `connect_hub=True`; `/connect/hub` returns `application/json`; summary shows brokers `5/5` connected (`ibkr`, `kraken`, `binance`, `bybit`, `alpaca`), information feeds `5/5` connected (`newsapi`, `alphavantage`, `finnhub`, `marketaux`, `fred`), enabled AI providers `3/3` connected (`rules`, `fin_sentiment`, `local_reasoning`), treasury `0/0` enabled and no auto-transfer. Verification: UI `npm run build`; `python -m pytest tests/test_connect_hub.py tests/test_connector_contracts.py -q` (`4 passed`). Local UI dev server remains available at `http://127.0.0.1:5173/`.
- Last completed task: **D107 — Connect Hub plugin fabric foundation added**. Added the first adaptive Connect Hub slice for onboarding brokers, information feeds, AI providers, and treasury accounts. New non-secret `config/connectors.yaml` declares connector manifests with category, auth type, required env vars, capabilities, roles, docs, and treasury safety policy. Added `system/connect_hub.py`, a read-only capability snapshot builder that merges broker runtime status, news/feed ingest telemetry, AI provider config, and treasury manifest state without exposing secret values. Added `connectors/base.py` contracts for future information-feed, AI-provider, and treasury adapters; treasury transfer quoting defaults to approval-only and route-disallowed. Added `GET /connect/hub` and embedded the same payload into `/system/status` as `connect_hub`; UI API types now include `ConnectHubResponse` and `api.getConnectHub()`. Added a redesign **Connect** route/screen showing trading platforms, feeds, AI providers, treasury accounts, capability flags, missing env vars, and next actions. Added `docs/CONNECT_HUB.md` as the operator/developer runbook. Treasury is deliberately metadata-only: no credential writes, OAuth, transfer, withdrawal, or auto-funding path exists yet. Documented the architecture as `docs/DECISIONS.md` D107. Verification: `python -m py_compile connectors/base.py system/connect_hub.py api/server.py tests/test_connect_hub.py tests/test_connector_contracts.py tests/test_api_dashboard_extras.py`; `python -m pytest tests/test_connect_hub.py tests/test_connector_contracts.py -q` (`4 passed`); `python -m pytest tests/test_connect_hub.py tests/test_connector_contracts.py tests/test_api_dashboard_extras.py -q -k "connect_hub or system_status_includes_connect_hub or system_status_includes_snapshot_published_at"` (`5 passed, 19 deselected`); UI `npm run build`; local dev server smoke returned HTTP `200` on `http://127.0.0.1:5173/`. Browser plugin tooling was unavailable in this session, so visual inspection via in-app browser could not be completed.
- Last completed task: **D104 — Phase E demand-shadow evidence report added**. Added `scripts/report_phase_e_demand_shadow.py`, a read-only evaluator for the learned relational demand graph. It loads the latest Phase E graph artifact, reconstructs historical per-bar learned shadow scores from `feature_snapshots`, compares them with the existing static cross-asset anchor score, labels forward panel returns over a configurable horizon, and writes both a governance JSON summary plus timestamp-level CSV rows under `reports/models/phase_e_demand_graph/`. Added `build_shadow_evidence()` test coverage in `tests/test_phase_e_relational_demand_graph.py`. Verification: `python -m py_compile scripts/report_phase_e_demand_shadow.py tests/test_phase_e_relational_demand_graph.py`; `python -m pytest tests/test_phase_e_relational_demand_graph.py tests/test_demand_meta.py -q` (`10 passed`). Real-data evidence run: `python scripts/report_phase_e_demand_shadow.py --run-id phase_e_shadow_evidence_20260517 --horizon 1 --scale 30 --signal-threshold 0.001` evaluated `8,423` hourly observations from artifact `phase_e_relational_h1_shadow002_20260516`; learned IC was `0.019845`, static cross-asset IC was `0.022477`, learned-minus-static IC was `-0.002633`, mean absolute learned score was only `0.000046`, with `1` actionable row at the threshold. Recommendation is `do_not_promote` (`low_information_coefficient`). Conclusion: Phase E is now measurable end-to-end, and the current learned graph should remain shadow-only until more data or stronger features produce evidence better than the static anchor model.
- Last completed task: **D103 — Phase E learned relational demand graph shadow added**. Built the first shadow-only upgrade path for the existing cross-asset demand graph. Added `system/relational_demand_graph.py`, which learns directed lagged cross-asset edges from historical close-price panels (`source return at t-1` vs `target return at t`), packages them as JSON `RelationalDemandGraphArtifact`s, and evaluates a learned shadow demand component without changing the live `DemandEngine.score`. `system/demand_engine.py` now optionally loads `config/strategies.yaml::demand_engine.learned_graph_shadow` and adds `learned_cross_asset_shadow_*` fields into demand components only; live demand weighting remains the existing static anchor/news/macro blend. Added `scripts/build_phase_e_relational_graph.py` to build artifacts from `feature_snapshots` and `scripts/report_phase_e_relational_graph.py` to inspect graph quality/top edges. Updated `config/strategies.yaml` to point shadow loading at `artifacts/models/demand_graph/latest_phase_e_relational_graph.json`. Verification: `python -m py_compile system/relational_demand_graph.py system/demand_engine.py scripts/build_phase_e_relational_graph.py scripts/report_phase_e_relational_graph.py tests/test_phase_e_relational_demand_graph.py`; `python -m pytest tests/test_phase_e_relational_demand_graph.py tests/test_demand_meta.py -q` (`9 passed`). Real-data build: strict threshold `min_abs_lag_corr=0.08` found `0` edges over `14` symbols / `11,492` hourly bars; relaxed shadow threshold `0.02` produced `26` weak candidate edges (`max_abs_weight=0.0564`, `avg_abs_weight=0.0296`), with top examples including `EURUSD=X -> BTC-USD`, `EURUSD=X -> ETH-USD`, `SPY -> TLT`, and `QQQ -> TLT`. Restarted supervised `run.py`; `/system/status` returned `state=running`, `pipeline_running=true`, `capital_pct=1.0`, active brokers `alpaca/binance/bybit/ibkr/kraken`. Conclusion: Phase E infrastructure is active in shadow metadata, but current learned lag edges are weak and must not influence allocation without stronger evidence.
- Last completed task: **D102 — Phase D status bundle added**. Added read-only `scripts/phase_d_status_bundle.py`, which runs the Phase D microstructure shadow report and execution outcome join in one command. Added unit coverage in `tests/test_phase_d_microstructure_shadow.py` to ensure both reports are included. Verification: `python -m py_compile scripts/phase_d_status_bundle.py tests/test_phase_d_microstructure_shadow.py`; `python -m pytest tests/test_phase_d_microstructure_shadow.py tests/test_execution_engine.py -q` (`44 passed`). Ran `python scripts/phase_d_status_bundle.py`: current historical orders still show `shadow_rows=0` for both reports because they predate D100/D101; future executions should populate the bundle.
- Last completed task: **D101 — Phase D execution outcome join added**. Added read-only `scripts/report_phase_d_execution_outcomes.py`, which scans recent `orders` rows containing `microstructure_*` metadata, computes side-aware realised slippage bps when a defensible reference price exists (`limit_price` vs `avg_fill_price`), groups outcomes by `microstructure_shadow_label`, and reports fill rate, average slippage, sample counts, and fee sums. Added pure-function tests for side-aware slippage and label-group summaries in `tests/test_phase_d_microstructure_shadow.py`. Verification: `python -m py_compile scripts/report_phase_d_execution_outcomes.py tests/test_phase_d_microstructure_shadow.py`; `python -m pytest tests/test_phase_d_microstructure_shadow.py -q` (`6 passed`). Ran `python scripts/report_phase_d_execution_outcomes.py --limit 100`: scanned orders but found `shadow_rows=0`, expected until post-D100 executions are persisted.
- Last completed task: **D100 — Phase D microstructure execution-quality shadow started**. Added a read-only execution-boundary microstructure shadow layer. `config/microstructure.yaml` now has `microstructure.shadow` enabled for observational metadata only (no blocking, rerouting, resizing, or order changes). Added `execution/microstructure_shadow.py`, which converts broker `OrderBook` snapshots into `data/orderbook_features.py` features, scores spread/toxicity/fragility/staleness into `microstructure_shadow_risk`, labels rows `normal`/`caution`/`high_risk`, and returns metadata fields only. Wired `execution/engine.py` to best-effort stamp this metadata on orders after broker resolution and before paper/live persistence, including the local paper crypto path; failures are metadata-only and cannot block execution. Added read-only `scripts/report_phase_d_microstructure_shadow.py` to summarize recent order rows containing Phase D shadow metadata. Verification: `python -m py_compile execution/microstructure_shadow.py execution/engine.py scripts/report_phase_d_microstructure_shadow.py tests/test_phase_d_microstructure_shadow.py`; `python -m pytest tests/test_phase_d_microstructure_shadow.py tests/test_execution_engine.py -q` (`41 passed`). Ran `python scripts/report_phase_d_microstructure_shadow.py --limit 50`: scanned historical orders but found `shadow_rows=0`, expected because existing order rows predate D100. Restarted the supervised `run.py` child; `/system/status` returned `state=running`, `pipeline_running=true`, `capital_pct=1.0`, active brokers `alpaca/binance/bybit/ibkr/kraken`. Future executions should now carry `microstructure_*` shadow metadata.
- Last completed task: **D099 — Phase C status bundle added**. Added read-only `scripts/phase_c_status_bundle.py`, a single operator command that runs the standard Phase C shadow check sequence: readiness, latest transition report, calibration evaluator, policy sweep, and latest CSV/JSON history export. Added unit coverage in `tests/test_phase_c_regime_transition.py` to ensure the bundle includes all expected Phase C scripts. Verification: `python -m py_compile scripts/phase_c_status_bundle.py tests/test_phase_c_regime_transition.py`; `python -m pytest tests/test_phase_c_regime_transition.py tests/test_dashboard_publish_serializers.py -q` (`17 passed`). Ran `python scripts/phase_c_status_bundle.py`: Phase C currently has `5` history rows, `0` mature rows for the default `4h` horizon, `next_ready_at=2026-05-17T02:04:43.341399+00:00`, latest transition `stress_transition` at probability about `0.5530`, and fresh exports at `reports/models/phase_c_regime_transition/latest_phase_c_shadow_history.csv` / `.json`. Existing Timescale hypertable warnings during report scripts are the known local `price_history` primary-key/timestamp incompatibility warning and do not affect Phase C report generation.
- Last completed task: **D098 — Phase C shadow-history export added**. Added read-only `scripts/export_phase_c_shadow_history.py`, which exports the rolling `regime_transition.shadow_history` control-state list to CSV and JSON files under `reports/models/phase_c_regime_transition/` for durable audit/evidence capture. The export includes transition fields plus D097 policy-shadow marker fields (`policy_shadow_enabled`, trigger, multiplier, throttle-applied flag, and policy reason). Added unit coverage in `tests/test_phase_c_regime_transition.py` for CSV/JSON writing. Verification: `python -m py_compile scripts/export_phase_c_shadow_history.py tests/test_phase_c_regime_transition.py`; `python -m pytest tests/test_phase_c_regime_transition.py tests/test_dashboard_publish_serializers.py -q` (`16 passed`). Ran `python scripts/export_phase_c_shadow_history.py --prefix latest_phase_c_shadow_history`, producing `reports/models/phase_c_regime_transition/latest_phase_c_shadow_history.csv` and `.json` with `5` rows.
- Last completed task: **D097 — Phase C shadow policy marker added**. Added `config/regime_models.yaml::transition_policy_shadow`, a strictly shadow-only candidate policy (`enabled=true`, `trigger_probability=0.55`, `exposure_multiplier=0.50`) used for reporting/history only. `system/dashboard_publish.py` now reads this policy and stamps future `regime_transition.shadow_history` rows with `policy_shadow_enabled`, `policy_trigger_probability`, `policy_exposure_multiplier`, and `policy_throttle_applied` when a transition probability is present; this still has zero live allocation/risk/execution effect. Added serializer coverage in `tests/test_dashboard_publish_serializers.py` to prove rows above the trigger receive the hypothetical multiplier. Verification: `python -m py_compile system/dashboard_publish.py tests/test_dashboard_publish_serializers.py`; `python -m pytest tests/test_dashboard_publish_serializers.py tests/test_phase_c_regime_transition.py -q` (`15 passed`). Runtime remains healthy after restarting the supervised `run.py` child: `/system/status` returned `state=running`, `pipeline_running=true`, `capital_pct=1.0`, active brokers `alpaca/binance/bybit/ibkr/kraken`. Existing history rows predate D097 and do not have policy marker fields; future rows will.
- Last completed task: **D096 — Phase C evaluation readiness helper added**. Added read-only `scripts/phase_c_readiness.py`, which inspects `regime_transition.shadow_history` and reports total rows, timestamped rows, mature rows for the selected future horizon, readiness against a minimum mature-row threshold, oldest/newest row, and the next time a row becomes evaluable. Added unit coverage for maturity counting and next-ready timestamp handling in `tests/test_phase_c_regime_transition.py`. Verification: `python -m py_compile scripts/phase_c_readiness.py scripts/sweep_phase_c_allocator_policy.py tests/test_phase_c_regime_transition.py`; `python -m pytest tests/test_phase_c_regime_transition.py tests/test_dashboard_publish_serializers.py -q` (`14 passed`). Runtime readiness currently reports `history_rows=5`, `mature_rows=0`, `ready=False`, and `next_ready_at=2026-05-17T02:04:43.341399+00:00` for the default `4h` horizon. Latest Phase C shadow row remains `regime_label=mixed`, `label=stress_transition`, probability about `0.5530`, threshold `0.45`, model `phase_c_transition_h4_thr045_20260516`.
- Last completed task: **D095 — Phase C allocator-policy threshold sweep added**. Added read-only `scripts/sweep_phase_c_allocator_policy.py`, which enriches Phase C shadow-history rows with future panel returns and ranks combinations of trigger probability (`0.45,0.50,0.55,0.60,0.65` by default) and throttle multiplier (`0.25,0.50,0.75` by default) by simulated return impact, avoided loss, and missed gain. The sweep reuses D094's simulator and does not alter live allocation. Added pure-function coverage in `tests/test_phase_c_regime_transition.py` to ensure positive-impact policies rank ahead of no-action policies. Verification: `python -m py_compile scripts/sweep_phase_c_allocator_policy.py tests/test_phase_c_regime_transition.py`; `python -m pytest tests/test_phase_c_regime_transition.py tests/test_dashboard_publish_serializers.py -q` (`13 passed`). Runtime sweep currently sees `history_rows=5` but `evaluated=0` because no row has reached the default `4h` future-return horizon yet; all policy impacts are therefore correctly zero for now.
- Last completed task: **D094 — Phase C allocator-impact simulator added**. Added read-only `scripts/simulate_phase_c_allocator_impact.py`, which consumes `regime_transition.shadow_history`, looks forward over the same configurable cross-asset panel as the calibration evaluator, and simulates a simple exposure throttle (`--trigger-probability`, default `0.55`; `--throttle-multiplier`, default `0.50`) without changing any live allocation. The simulator reports baseline return sum, simulated return sum, net impact, throttled count, avoided loss, and missed gain so Phase C can be judged on actual allocator tradeoffs before any sizing hook is considered. Added pure-function coverage in `tests/test_phase_c_regime_transition.py` to verify avoided-loss vs missed-gain accounting. Verification: `python -m py_compile scripts/simulate_phase_c_allocator_impact.py tests/test_phase_c_regime_transition.py`; `python -m pytest tests/test_phase_c_regime_transition.py tests/test_dashboard_publish_serializers.py -q` (`12 passed`). Runtime report now shows Phase C shadow history is populating: `4` rows, latest snapshot `regime_label=mixed`, `label=stress_transition`, probability about `0.5561`, threshold `0.45`, model `phase_c_transition_h4_thr045_20260516`. Immediate impact simulation has `history_rows=4` but `evaluated=0` because the default `4h` future horizon has not elapsed yet, so realised panel returns are not available; this is expected and prevents false early scoring.
- Last completed task: **D093 — Phase C shadow capture wired into the live global-edge path**. Fixed the D091 observability gap where the live loop could publish `global_edge` dashboard snapshots with `regime=None`, so Phase C history remained empty even after a completed global-edge tick. `system/dashboard_publish.py::publish_dashboard_snapshot_global_edge()` now accepts an optional `RegimeState`, serializes the same explicit `regime.transition` block as the D015 path, and best-effort appends to `regime_transition.shadow_history`. `system/trading_loop/loop.py::_run_global_edge_tick()` now computes the shadow regime state with `compute_regime_state_async()` before publishing the global-edge snapshot, stamps demand diagnostics into its metadata, and passes it through to the dashboard publisher; failures are debug-only and cannot block allocator execution. Verification: `python -m py_compile system/dashboard_publish.py system/trading_loop/loop.py`; `python -m pytest tests/test_dashboard_publish_serializers.py tests/test_phase_c_regime_transition.py tests/test_loop_status_strategies.py -q` (`17 passed`). Restarted the supervised `run.py` child; `/system/status` is `state=running`, `pipeline_running=true`, `capital_pct=1.0`, active brokers `alpaca/binance/bybit/ibkr/kraken`. Immediate runtime proof of a history row is still pending because the restarted child had not completed a fresh post-D093 loop iteration at the verification point (`iterations=0`, no `last_error`); the next completed global-edge tick should now write Phase C transition history.
- Last completed task: **D092 — Phase C calibration evaluator scaffold added**. Added read-only `scripts/evaluate_phase_c_transition_history.py`, which reads `regime_transition.shadow_history`, looks forward over a configurable cross-asset panel horizon, labels realised panel stress (`future_panel_return <= --stress-return`, default `-0.004` over `4h`), and reports TP/FP/TN/FN plus precision, recall, and accuracy for the shadow detector. The evaluator defaults to the same broad panel used by the Phase C trainer (`SPY`, `QQQ`, `IWM`, `TLT`, `HYG`, `GLD`, major crypto, mega-cap tech, and FX anchors), is configurable by timeframe/horizon/symbol list, and is strictly read-only. Added unit coverage for the confusion-matrix scorer in `tests/test_phase_c_regime_transition.py`. Verification: `python -m py_compile scripts/evaluate_phase_c_transition_history.py tests/test_phase_c_regime_transition.py`; `python -m pytest tests/test_phase_c_regime_transition.py tests/test_dashboard_publish_serializers.py -q` (`11 passed`). Ran `python scripts/evaluate_phase_c_transition_history.py`: history is currently empty (`history_rows=0`, `evaluated=0`) because no post-D091 full allocator snapshot has been written yet; the evaluator is ready to produce real calibration once Phase C shadow rows accumulate.
- Last completed task: **D091 — Phase C shadow history persistence added**. Added bounded rolling persistence for Phase C transition shadow rows in `system/dashboard_publish.py` under control-state key `regime_transition.shadow_history`. Full D015 dashboard snapshots now append one row per allocator tick containing timestamp, loop iteration, path, base regime label, market/breadth scores, transition used/shadow flags, label, probability, threshold, model version, and unavailable reason when present. The append is best-effort and guarded so a history write can never break the allocator/control path. Extended `scripts/report_phase_c_transition.py` to print recent shadow-history rows in addition to config and latest snapshot. Verification: `python -m py_compile system/dashboard_publish.py scripts/report_phase_c_transition.py tests/test_dashboard_publish_serializers.py tests/test_phase_c_regime_transition.py`; `python -m pytest tests/test_dashboard_publish_serializers.py tests/test_phase_c_regime_transition.py -q` (`10 passed`). Restarted the supervised `run.py` child; `/system/status` returned `state=running`, `pipeline_running=true`, `capital_pct=1.0`, active brokers `alpaca/binance/bybit/ibkr/kraken`. Immediate post-restart report still shows no history rows because the live app has not yet completed a fresh full allocator iteration (`iterations=0`) and the latest dashboard snapshot remains the pre-restart heartbeat; the writer is loaded and will populate on the next full D015 snapshot.
- Last completed task: **D090 — Phase C shadow observability added**. Added an explicit `transition` block to `system/dashboard_publish.py::serialize_regime_state()` so dashboard snapshots expose Phase C fields (`used`, `shadow_only`, probability, label, threshold, model version, or unavailable reason) without requiring the UI/API to scrape raw `metadata`; the original `regime_transition_*` metadata remains intact for backward compatibility. Added read-only operator script `scripts/report_phase_c_transition.py`, which loads `config/regime_models.yaml`, prints the configured Phase C artefact/threshold/feature count, and reads the latest `dashboard.snapshot` from `control_state` to show the most recent transition prediction when a full allocator snapshot exists. Verification: `python -m py_compile system/dashboard_publish.py scripts/report_phase_c_transition.py tests/test_dashboard_publish_serializers.py tests/test_phase_c_regime_transition.py`; `python -m pytest tests/test_dashboard_publish_serializers.py tests/test_phase_c_regime_transition.py -q` (`9 passed`). Ran `python scripts/report_phase_c_transition.py`: config reports Phase C enabled/shadow-only at threshold `0.45` with `9` features; latest snapshot was still a heartbeat with no regime transition block yet, expected until the next full allocator tick. Restarted the supervised `run.py` child; `/system/status` returned `state=running`, `pipeline_running=true`, `capital_pct=1.0`, active brokers `alpaca/binance/bybit/ibkr/kraken`.
- Last completed task: **D089 — Phase C regime-transition detector added and shadow-enabled**. Added `risk/regime_transition.py`, a deterministic binary transition detector with save/load artefacts, train-only feature scaling, sklearn LogisticRegression when available, and a numpy fallback. Added `scripts/train_regime_transition.py`, which builds a cross-asset h4-style transition dataset from `feature_snapshots` using SPY/QQQ/IWM/TLT/HYG/GLD, major crypto, mega-cap tech, and FX anchors, then writes model artefacts plus JSON/CSV audit reports under `reports/models/phase_c_regime_transition/`. Wired `risk/regime_state.py` so `config/regime_models.yaml::transition_detector` can attach `regime_transition_*` metadata in shadow mode only; it does **not** override heuristic/HMM regime labels or allocation behavior. Trained three governed candidates: threshold `0.60` was too conservative, `0.35` too noisy, and the selected shadow candidate `phase_c_transition_h4_thr045_20260516` produced OOS ROC AUC `0.6118`, recall `0.792`, precision `0.1298`, and accuracy `0.3979` on `1,146` holdout rows. `config/regime_models.yaml` now enables `transition_detector` with `shadow_only: true`, threshold `0.45`, and artefact `artifacts/models/regime_transition/regime_transition-phase_c_transition_h4_thr045_20260516.pkl`. Local verification showed `regime_transition_used=True`, probability `0.5299`, label `stress_transition`, and `shadow_only=True` while the base regime label remained `mixed`. Verification: `python -m py_compile risk/regime_transition.py risk/regime_state.py scripts/train_regime_transition.py`; `python -m pytest tests/test_phase_c_regime_transition.py tests/test_wave4_wiring.py tests/test_wave4_regime_vol_cov.py tests/test_regime_state.py -q` (`30 passed`). Restarted the supervised `run.py` child so the long-running app loads Phase C; `/system/status` returned `state=running`, `pipeline_running=true`, `capital_pct=1.0` with no manual START.
- Last completed task: **D088 — Governed Phase B real-data TCN training run completed**. Added `scripts/train_phase_b_panel.py`, a research-only panel trainer that pulls `feature_snapshots` directly from Postgres, builds leakage-safe sequence datasets (`window=64`, configurable horizon), trains TCN candidates against the mandatory Ridge sequence baseline, writes JSON/CSV reports under `reports/models/phase_b_forecaster/`, and saves research artefacts under `artifacts/models/forecast/phase_b/<run_id>/` without registering or activating anything. Fixed the TCN training contract in `models/deep_sequence/train.py` / `models/deep_sequence/artefact.py`: TCN now uses train-only input feature scaling plus target scaling, and the packaged `TrainedSequenceForecast` stores/applies both scalers at inference. Tightened `models/deep_sequence/evaluate.py` so promotion now requires positive OOS cost-adjusted net P&L, not merely "less bad than baseline". Real-data results: `horizon=1` final gate rejected all 8 symbols (`BTC-USD`, `ETH-USD`, `SOL-USD`, `SPY`, `QQQ`, `AAPL`, `NVDA`, `USDCAD=X`) because every model was net negative after costs. `horizon=4` was more promising: seed 17 passed `ETH-USD`, `QQQ`, `NVDA`; seed 18 passed none; seed 19 passed `QQQ` and `NVDA`. Verdict: `QQQ`/`NVDA` are research candidates but not stable enough for activation; `ETH-USD` is weaker/seed-fragile; no model was registered in `config/forecast_models.yaml` and no trading behavior changed. Verification: `python -m py_compile models/deep_sequence/train.py models/deep_sequence/artefact.py scripts/train_phase_b_panel.py`; `python -m pytest tests/test_wave11_deep_sequence.py tests/test_phase_b_forecaster.py tests/test_phase_b_panel_training.py tests/test_training_universe_backfill.py -q` (`34 passed`, torch weight_norm deprecation warnings only). Runtime check: `/system/status` remains `state=running`, `pipeline_running=true`, `capital_pct=1.0`.
- Last completed task: **D087 — Phase B training-data backfill from live universe completed**. Added a governed universe-driven historical backfill path for the Phase B sequence forecaster: `config/data_pipeline.yaml` now has `historical_training_backfill` (`1h` / `730d`), `run_pipeline.py --training-backfill` selects symbols from `data/runtime/universe_tiers.json` (`core,scan` by default), skips news/FRED for pure price-feature history, supports `--dry-run`, `--universe-scope`, and `--max-symbols`, and `data/training_universe.py` normalizes residual broker-style universe symbols such as `RENDER/USD`, `PEOPLEUSDT`, `VIX`, and `DXY` into yfinance-compatible training symbols. Fixed `data/persist.py` feature-snapshot upserts to chunk large hourly histories below asyncpg's 32,767-argument limit. Ran the full 300-symbol universe training backfill plus a targeted residual pass: `feature_snapshots` now has `1,655,362` `1h` rows across `4,562` symbols; of the selected 300 training symbols, `298` have data, `154` have at least `5,000` bars, `245` have at least `1,000` bars, and only `ENJU` / `TAOJPY` still have no yfinance history. Verification: `python -m py_compile run_pipeline.py data/pipeline.py data/persist.py data/training_universe.py`; `python -m pytest tests/test_training_universe_backfill.py tests/test_pipeline_integration.py tests/test_phase_b_forecaster.py -q` (`11 passed, 1 skipped`, torch weight_norm deprecation warnings only). Runtime check: `/system/status` remains `state=running`, `pipeline_running=true`, `capital_pct=1.0`.
- Last completed task: **D086 — IBKR curated universe and contract-qualification gate added**. Implemented the agreed split where Alpaca remains the broad US equity/ETF discovery catalog while IBKR has an explicit, config-backed priority universe plus a qualification gate before any IBKR order. Added `config/ibkr_universe.yaml` (`61` curated IBKR priority instruments, including extra broad-market/bond ETFs and FX pairs), `brokers/ibkr/universe.py` loader/fallback, and wired `IBKRAdapter.get_supported_symbols()` to that config instead of hard-coded legacy symbols. Added `brokers/ibkr/qualification.py` with persisted records in `data/runtime/ibkr_contract_qualifications.json`; `brokers/ibkr/adapter.py` now maps dotted FX broker symbols such as `EUR.USD` correctly, records successful/failed contract qualifications, and rejects `place_order()` before submission if IBKR returns no qualified contract. Added `scripts/qualify_ibkr_universe.py` as a no-order cache warmer/diagnostic for the curated IBKR list. `universe/snapshot_service.py` now uses the IBKR universe config for catalog names too, so new seed entries do not show up anonymous. Verification: `python -m py_compile scripts/qualify_ibkr_universe.py brokers/ibkr/adapter.py brokers/ibkr/universe.py brokers/ibkr/qualification.py universe/snapshot_service.py`; `python -m pytest tests/test_ibkr_universe_qualification.py tests/test_universe_intelligence.py tests/test_universe.py tests/test_asset_class_routing.py tests/test_ibkr_forex_rounding.py -q` (`51 passed`). Restarted the supervisor/run.py process and started paper mode again at existing `capital_pct=1.0`; trading loop is running with Alpaca/Binance/Bybit/Kraken connected, but IBKR is currently excluded because Gateway is listening while the API is not responding (`possible zombie/paper disclaimer prompt`). Live `/intelligence/universe` therefore currently reports `broker_listing_count=16362` without IBKR; local config check returns `61` IBKR seed symbols and they will appear in coverage once Gateway accepts API connections.
- Last completed task: **D085 — Universe Intelligence coverage semantics and IBKR seed fixed**. Audited the Universe tab end-to-end after the operator challenge around `16,362 sourced · 300 watched · 32 active`, `12 promoted < 32 active`, and `ibkr: 0`. `brokers/ibkr/adapter.py` now returns the curated IBKR tradable seed (`55` symbols) from `get_supported_symbols()` instead of an empty list, with forex broker symbols normalized through `data/universe_builder.py` to yfinance forms such as `EURUSD=X`. `api/server.py` now passes broker symbol lists, not just counts, into `universe/snapshot_service.py`; the snapshot reports coverage metadata with raw broker listings, unique normalized candidates, caps, per-broker raw/normalized counts, and an explicit `curated_seed` note for IBKR. Fixed funnel semantics so source means broker listings, eligible/scored means the scored candidate set, watched remains core+scan, promoted is recent temporary promotions, and active is the actual correlation representative count rather than a capped cosmetic `32`; UI labels/headline now show listings vs unique normalized counts and active reps. `data/universe_builder.py` now uses deterministic broker-stratified sampling with curated anchors pinned before filling the 400 candidate cap, instead of one global random sample. Verification: `python -m py_compile data/universe_builder.py universe/snapshot_service.py brokers/ibkr/adapter.py api/server.py`; `python -m pytest tests/test_universe_intelligence.py tests/test_universe_tiers.py tests/test_universe.py tests/test_data_pipeline.py tests/test_asset_class_routing.py -q` (`55 passed`); UI `npm run build`. Direct adapter check returned `55` IBKR curated symbols. Restarted `python run.py`, started the system back in paper mode with `capital_pct=1.0`, and verified all five brokers connected with loop iteration advancing; live `/intelligence/universe` now reports `broker_listing_count=16417`, `unique_normalized=14737`, `scored=400`, `watching=300`, `active_reps=93`, and `ibkr raw=55 normalized=55 source=curated_seed`.
- Last completed task: **D084 — Live safety blockers B1/B2/B4 and churn/cancel guards fixed**. Closed the pre-live NO-GO items around restart safety and live arming. `risk/engine.py` now persists kill-switch, disabled-broker, loss, and cooldown runtime state to `data/runtime/risk_state.json`; startup no longer clears a latched kill in `system/trading_loop/loop.py` or `system/orchestrator.py`, and stale persisted snapshots cannot clear a kill except through deliberate `reset_kill`. Added `system/live_arming.py` and wired it through `system/broker_manager.py` plus `brokers/ibkr/adapter.py`: `APP_ENV=live` now requires explicit arming phrases, IBKR live requires a separate arming phrase, and `APP_ENV=paper` refuses `IBKR_PORT=7496`. Hardened `run.py` so live startup refuses API port takeover/alternate-port restart instead of killing an existing process; added read-only `scripts/diagnose_run_supervisors.py`, which found Claude/Git-Bash wrapper processes launching `python run.py` and no Windows Task Scheduler/Startup-folder respawner. Fixed stale-order age-out in `execution/engine.py` so live local status becomes `cancelled` only after broker-confirmed terminal cancel/reject; cancel/fill races stay non-terminal to preserve dedup. Added 900s anti-churn cooldowns for adaptive shed and capital recycle in `config/global_edge.yaml` / `portfolio/global_edge_coordinator.py`, with `system/trading_loop/loop.py` recording trim actions into replacement context. Verification: `python -m py_compile risk/engine.py execution/engine.py portfolio/global_edge_coordinator.py system/broker_manager.py system/live_arming.py brokers/ibkr/adapter.py system/trading_loop/loop.py system/orchestrator.py run.py scripts/diagnose_run_supervisors.py`; `python -m pytest tests/test_risk_engine.py tests/test_live_arming.py tests/test_execution_engine.py tests/test_global_edge_coordinator.py tests/test_broker_balance_ready.py -q` (`120 passed, 1 skipped`).
- Last completed task: **D083 — Observability fixes + capital recycle when book is full**. Fixed strategy/universe/dashboard mismatches: `/status` derives `active_strategies` from orchestrator `loaded_strategies`; wave13 notes distinguish configured vs loop-loaded families; Strategy Mix marks factor/stat-arb/options as configured-only; universe intelligence now derives real promotion rows/stream from scored conviction (not fabricated indices). Added `global_edge.capital_recycle` take-profit trims and dead-edge culls via `propose_capital_recycle_actions()` wired in the trading loop so a full book can free cash without rotation edge. Verification: `python -m pytest tests/test_universe_intelligence.py tests/test_wave13_dashboard.py tests/test_global_edge_coordinator.py -q` (`35+ passed`); UI `npm run build`.
- Last completed task: **D082 — Wave 9 profitability gate tightened after fee-dominated soak**. Continued Hunter paper testing with profitability/stability as the priority. A natural 50% Hunter soak showed the basket could be slightly gross-positive before costs but still net-negative after fees/spread/slippage, exposing that score-only allocator edge was too optimistic for execution gating. Fixed `execution/engine.py` so all `Signal.metadata` is preserved on `Order.instrument_metadata` for audit, not only option metadata. Tightened Wave 9 in `execution/scheduler.py` / `execution/wave9_runtime.py`: `edge_to_cost_safety` now means edge must cover a multiple of expected cost, score-only allocator fallback is capped at 25 bps instead of inflated 100 bps, `config/execution_models.yaml` now requires a 2x edge/cost cushion, and missing daily-volume/liquidity input adds a 5 bps unknown-liquidity penalty plus `wave9_liquidity_known` metadata. Added tests for metadata preservation, bounded score fallback, score-only cost blocking, and missing-liquidity penalty. Also improved `/intelligence/signals` news attribution in `api/server.py` by parsing numeric metadata robustly, stripping broker prefixes for news lookup, and widening attribution windows so older AI batches are not hidden from newer signal rows. Verification: `python -m py_compile execution/scheduler.py execution/wave9_runtime.py execution/engine.py`; focused Wave 9/execution/news tests (`28 passed`); broader suite `python -m pytest tests/test_wave9_wiring.py tests/test_execution_engine.py tests/test_profit_harvest.py tests/test_adaptive_sizing.py tests/test_global_edge_coordinator.py tests/test_risk_engine.py tests/test_api_dashboard_extras.py tests/test_api_signal_news_attribution.py -q` (`168 passed, 2 skipped`, one existing async mock warning). Restarted `python run.py`: a 5% probe before the liquidity penalty opened 11 small IBKR equity positions with score-only edge `14.7–19.3 bps`; after the penalty/restart, the same 5% probe produced `0` orders and `0` positions, confirming the system stayed defensive when liquidity data was missing. Final state: running paper mode, all five brokers connected, `capital_pct=0.0`, `/positions` empty, `/diagnostics/accounting ok=true`, `fee_delta=0`, with only expected tombstone-shock warnings from deliberate test flattening.
- Last completed task: **D081 — Long Hunter paper soak and fee/profit controls tightened**. Ran the requested longer Hunter paper test after D080. First 8-iteration 50% soak plus shed/top-up/flatten proved mechanics worked (opens, rotations, shed, top-up, flatten, accounting) but did **not** prove profitability: the test ended about `-840.75` net, with `536.26` fees and the rest spread/slippage/mark movement; open P&L briefly peaked near `+995` but profit harvest did not bank it because Hunter thresholds were too wide. Tightened `config/risk_limits.yaml::profit_harvest` for fast Hunter paper cycles (lower min profit / NAV floor, lower peak-lock floor, lower giveback, Hunter `threshold_mult=0.50`, `giveback_mult=0.50`, full trim fraction) and raised `config/global_edge.yaml::rotation` fee-adjusted hurdle (`hunter min_edge_advantage=0.03`, round-trip fee bps `40`, multiplier `1.5`). Also updated `system/trading_loop/loop.py` so normal allocator opens mark symbols in `ReplacementContext.last_event_at_by_symbol`, preventing immediate fee-only rotations of newly opened names. Verification: `python -m py_compile system/trading_loop/loop.py portfolio/global_edge_coordinator.py risk/profit_harvest.py`; `python -m pytest tests/test_profit_harvest.py tests/test_global_edge_coordinator.py tests/test_adaptive_sizing.py -q` (`64 passed`, one existing async mock warning); focused execution/profit checks (`12 passed, 32 deselected`). Restarted `python run.py` and reran Hunter at 50%: second retest loss improved to about `-317.46`; after setting a more realistic liquid-paper cost model (`paper_fee_bps=0.5`, `paper_slippage_bps=0.5`) and restarting, final 4-iteration open/flatten check ended about `-76.11` net with `53.57` modeled costs. Conclusion: strategy plumbing and accounting are healthy, but short forced round trips still lose unless price movement exceeds costs; this cannot honestly be called profitable yet. System left running in paper mode, Hunter mode, all five brokers connected, `capital_pct=0.0`, `/positions` empty, `/diagnostics/accounting ok=true` with expected tombstone-shock warning from deliberate flattening.
- Last completed task: **D080 — Hunter scaling stress-test fixes**. Fixed the paper stress-test findings where Hunter respected some allocation levels but ignored 25%/50% top-ups and only jumped at 100%. Root cause: the trading loop filtered out same-direction held symbols before adaptive sizing, and the coordinator required a fresh same-symbol opportunity on each later tick, so an already-good holding could not be topped up from 25% to 50%/100%. `system/trading_loop/loop.py` now leaves same-direction opportunities available to adaptive sizing; `portfolio/global_edge_coordinator.py` now emits allocator-marked top-ups for same-side held symbols and can top up from held remaining-edge state when no fresh same-symbol signal appears; `risk/engine.py` permits only those allocator-marked top-ups through `theme_uniqueness`. Reduced synthetic paper fee drag from 10 bps to 2 bps, loosened Hunter-only mean-reversion/volume-flow thresholds to broaden activity, added allocator `scanned_no_action` diagnostics so Hunter shows when it scanned but found no deployable edge, and added `/orders.filled_price` as an alias of `avg_fill_price` for audit/display. Verification: `python -m py_compile portfolio/global_edge_coordinator.py system/trading_loop/loop.py risk/engine.py api/server.py`; `python -m pytest tests/test_adaptive_sizing.py tests/test_global_edge_coordinator.py tests/test_risk_engine.py tests/test_api_dashboard_extras.py -q` (`99 passed, 2 skipped`); additional focused execution/loop checks (`4 passed, 44 deselected`). Restarted `python run.py`; live paper stress after the fix scaled cleanly: 25% → `cash_deployed_pct=0.24953`, 50% → `0.49937`, 100% → `0.99966`, then 0% → flat. Recent `/orders` rows now include non-null `filled_price`; `/diagnostics/accounting ok=true` with only expected tombstone-shock warnings from deliberate flattening. System left running in paper mode with all five brokers connected, `capital_pct=0.0`, and `/positions` empty.
- Last completed task: **D079 — Universe Intelligence made live instead of static fallback**. Investigated why `/universe` appeared frozen at `16,362 sourced · 300 watched · 7 active`: `config/universe_selection.yaml` had intelligence disabled, so `/intelligence/universe` was deliberately serving a fallback view from `data_pipeline.yaml + universe_tiers.json` with capped/formula stages, no clusters, and no real promotion stream. Enabled universe intelligence, extracted the manual clustering script into reusable `universe/intelligence_builder.py`, and wired `system/orchestrator.py` so the data pipeline builds `data/runtime/universe_intelligence.json` automatically after broker-derived tier rebuilds. The API now reports honest build states (`fresh` / `stale` / `missing`) and fallback metadata instead of marking missing intelligence as fresh; the UI shows a status badge and no longer hides fallback mode. Removed fabricated promotion stream rows when there are no real promotions. Verification: `python -m py_compile universe/intelligence_builder.py universe/snapshot_service.py scripts/build_universe_tiers.py system/orchestrator.py`; `python -m pytest tests/test_universe_intelligence.py tests/test_universe_tiers.py tests/test_universe.py tests/test_scanner.py tests/test_orchestrator_stop.py -q` (`25 passed`); UI `npm run build`. Restarted `python run.py`; live `/intelligence/universe` now reports `enabled=true`, `fallback=null`, `build_state=fresh`, `clusters=95`, `promoted=0`, `stream=0`; system running in paper mode with all five brokers connected, `capital_pct=0.0`, and `/positions` empty.
- Last completed task: **D078 — Paper close fills now use market marks for realised P&L**. Investigated the operator challenge that recent close rows showed exactly `0` gross realised P&L even though open-position MTM was visibly non-zero. Root cause: `ExecutionEngine._simulate_fill()` in paper mode preferred `signal.suggested_price` / order limit before any live mark, and reduce-only trim/close signals can carry the stale entry or allocator price. That meant the local paper ledger could "close" at entry, producing `realised_pnl_gross=0` and net P&L equal only to the close fee, while `/positions` was correctly marking the still-open book with live prices. Fixed reduce-only / close-only / `trim_symbol` paper fills in `execution/engine.py` so they prefer the broker's current `get_last_price()` (then current-price metadata) before falling back to stale signal/limit prices, and skip normal limit-price bounding for reduce-only paper closes so stale limits cannot force entry-price exits. Added regression coverage in `tests/test_execution_engine.py::test_paper_reduce_only_close_uses_market_price_not_stale_signal_price`. Verification: `python -m py_compile execution/engine.py`; `python -m pytest tests/test_execution_engine.py tests/test_global_edge_coordinator.py tests/test_api_dashboard_extras.py tests/test_run_m5.py -q -k "paper_simulate_fill or reduce_only_close or rotation or order_fill or estimate_realized"` (`13 passed, 77 deselected`). Restarted `python run.py`; `/system/status` confirms system running, `capital_pct=1.0`, all five brokers connected. Historical order rows with already-recorded stale fills remain audit history and were not rewritten.
- Last completed task: **D077 — Hunter rotation anti-churn guard**. Follow-up to D076 after the Book showed near-identical `-$473.xx` P&L on repeated ADPT/BGC rows. Confirmed the figures were mechanically accurate under the current paper model: every rotation leg was about `$473k` notional at a fixed ~10 bps fee, and fills were at the same entry/exit price, so gross close P&L was `0` and net P&L was just the platform fee. This exposed a trading flaw: Hunter rotation was ping-ponging symbols at target allocation and manufacturing fees. Tightened `config/global_edge.yaml::rotation`: Hunter now allows only one replacement per tick, records replacement events, applies a 15-minute `symbol_cooldown_sec` / `min_hold_sec`, and uses stronger churn penalties. `GlobalEdgeCoordinator.propose_rotation_actions()` now skips recently touched close/replacement symbols, blocks repeated reversal pairs, mutates `ReplacementContext` with global-edge rotation events, and the trading loop persists that context after coordinator actions. Verification: `python -m py_compile portfolio/global_edge_coordinator.py system/trading_loop/loop.py api/server.py`; `python -m pytest tests/test_global_edge_coordinator.py tests/test_api_dashboard_extras.py tests/test_run_m5.py -q -k "rotation or order_fill or estimate_realized"` (`8 passed, 47 deselected`). Restarted `python run.py`; after one boot-time replacement seeded the context, the next loop interval produced no new ADPT/BGC flip, confirming the cooldown held. System running with all five brokers connected, `capital_pct=1.0`, loop iteration advancing.
- Last completed task: **D076 — Live unrealised mark stability + fee-net P&L rollups**. Investigated the Book Totals unrealised figure appearing static. Live sampling showed it was updating, but the mark-to-market path could snap a position back to entry/zero unrealised because `_live_broker_prices()` accepted the first non-zero broker quote in a race; changed it to gather connected adapter quotes per symbol and choose a deterministic median positive quote, reducing stale/default quote flicker. Added server-side P&L annotations to `/orders`: every filled row now has `trade_pnl_net` (opens/adds = negative platform fee; closes/reduces = realised gross minus the close fee), while close rows also expose `closes_position`, `realised_pnl_net`, gross P&L, allocated fee, and closed quantity for both long sells and short buys. Updated the Book “Recent position changes” table to include a `p&l` column from `trade_pnl_net`. Fixed realised rollup estimation to use the pre-trade book, handle broker-symbol position keys, and normalise side casing; then changed `/pnl` today/week/month/YTD realised totals to derive directly from filled-order net P&L rather than stale `daily_pnl.realised_pnl`, and made the dashboard use those API rollups instead of NAV-history deltas that hid paper fees. Verification: `python -m py_compile api/server.py system/trading_loop/loop.py run_m5.py`; `python -m pytest tests/test_api_dashboard_extras.py tests/test_run_m5.py -q` (`22 passed, 1 skipped`); UI `npm run build`. Restarted `python run.py` and started the system at `capital_pct=1.0`; all five brokers reconnected after startup warm-up. Live checks: `/orders?limit=3` shows opening `BGC` with `trade_pnl_net=-473.71936759` and closing `ADPT` with `trade_pnl_net=-473.72310646`; `/pnl` now reports fee-net realised rollups (`today_realised=-10423.9738906547561748`, `week_realised=-13911.3558401447561748`, `month_realised=-15893.33525653709407062231973`, `ytd_realised=-41418.26130087689882227158340`).
- Last completed task: **D075 — Hunter mode fee-aware rotation pulse**. Addressed the operator complaint that Hunter felt static after reaching target allocation. Root cause: adaptive global-edge intentionally stopped opening once cash deployed was at target, and there was no at-target replacement path; additionally one slow broker/NAV await could leave the trading loop stuck inside an iteration with no fresh heartbeat. Added `rotation` config in `config/global_edge.yaml` (`max_replacements_per_tick`, `min_edge_advantage`, estimated round-trip fee bps) and implemented `GlobalEdgeCoordinator.propose_rotation_actions()`: when at/near target, it compares strongest fresh opportunity vs weakest held edge, subtracts fee-adjusted required advantage, then emits paired `trim_symbol` + `open_strategy` actions that redeploy the same cash budget. Wired `system/trading_loop/loop.py` so within target tolerance Hunter rotates instead of trying tiny top-ups, and added `asyncio.wait_for` guards around live NAV, treasury refresh, and broker-position merge so a slow broker call cannot freeze the loop. Tests: `python -m py_compile portfolio/global_edge_coordinator.py system/trading_loop/loop.py`; `python -m pytest tests/test_global_edge_coordinator.py tests/test_adaptive_sizing.py tests/test_loop_status_strategies.py -q` (`59 passed`); `python -m pytest tests/test_global_edge_coordinator.py tests/test_adaptive_sizing.py tests/test_risk_engine.py tests/test_execution_engine.py -q` (`113 passed, 1 skipped`). Restarted `python run.py` and started the system at `capital_pct=1.0`; live verification: all five brokers connected, dashboard cash deployed about `99.99%`, first fresh Hunter rotation closed `ADPT` (`global_edge_rotation`) and opened `kraken:ETH-USD` around `$473.6k`, `/positions` now shows `ETH-USD` and no open `ADPT`, `/diagnostics/accounting ok=true` with fees matching filled orders (`fee_delta=0`).
- Last completed task: **D074 — Capital allocation made authoritative again**. Responded to the 2026-05-08 finding that the system sat around `19%` cash deployed despite `capital_pct=1.0`: the loop was healthy, but newly-added fixed per-order/exposure caps were vetoing allocator-selected actions (`max_order_notional`) and Hunter adaptive `gross_fraction=0.70` meant 100% slider could never mean 100% deployment. Changed the contract so the operator allocation slider is the deployment target: `config/global_edge.yaml` now uses `gross_fraction: 1.00` for hunter/trader/defender; `config/risk_limits.yaml` disables static order/exposure/asset-class caps by default via `enforce_static_order_caps=false` and `enforce_static_exposure_caps=false`; `risk/engine.py` keeps those checks available only as explicit legacy opt-ins; `system/trading_loop/loop.py` no longer passes coordinator per-position notional clipping unless static caps are enabled; and `execution/engine.py` disables the absolute fallback sizing cap and metadata hard-cap enforcement unless env vars opt them back in. Preserved non-static controls such as kill switch, broker disable, options policy, live M8 micro-live rules, minimum order sanity, drawdown/daily-loss gates, confidence/theme/catalyst/quality checks, and execution sizing-vs-intent consistency. Restarted `python run.py`, started the system at persisted `capital_pct=1.0`, and verified after the first fresh loop that all five brokers connected and cash deployed moved to `0.9992` of NAV (`cash_deployed=1072477.63`, NAV `1073323.76`) with new paper positions including `ADPT`, `BGC`, `BRC`, and `EURUSD`. Focused verification: `python -m py_compile risk/engine.py system/trading_loop/loop.py execution/engine.py`; `python -m pytest tests/test_risk_engine.py tests/test_adaptive_sizing.py tests/test_global_edge_coordinator.py tests/test_execution_engine.py -q` (`111 passed, 1 skipped`).
- Last completed task: **D073 — Full post-incident paper-cycle test and stopped-book visibility fix**. Ran the requested full validation after the USDCHF/CHRD guardrail repairs. Full suite now passes: `python -m pytest -q` (`905 passed, 3 skipped`, one existing async-mock warning in `tests/test_profit_harvest.py`). Removed the pre-fix oversize local paper book with `python -m scripts.flatten_local_paper_book --apply`, closing `ibkr:CHRD` (`$536,591.78`) and `ibkr:USDCHF` (`$1,072,795.66`). Restarted `python run.py`, set capital allocation to `5%`, started the system, and let it place a controlled fresh paper book; hard risk capped a large `ADPT` action with `max_order_notional`, while approved small fills opened `ALNY`, `BIL`, `BPOP`, `BTSG`, `CEG`, `CNTA`, `IWM`, `QQQ`, plus `kraken:XRP-USD`, about `$37.2k` total exposure against about `$1.073m` NAV. Stopped the loop after the test so no further positions accumulate; `capital_pct=0.05` remains persisted and `/diagnostics/accounting` returns `ok=true` with only the expected tombstone-shock warning from the forced CHRD/USDCHF flatten and the historical fee-drift warning. Fixed `api/server.py` so a stopped paper system with no connected brokers still shows the local paper ledger on `/positions` instead of falsely returning an empty book; added regression coverage in `tests/test_live_portfolio_value.py`. Live verification after restart: `/system/status state=off`, `/positions` returns the 9-position paper book, `/diagnostics/accounting open_positions.count=9`.
- Last completed task: **D072 — Trading business-risk guardrails after USDCHF oversizing audit**. Investigated the 2026-05-07 paper incident where Hunter mode at `capital_pct=1.0` opened `CHRD` around `$830k` and `USDCHF` around `$1.07m` notional from weak/moderate `volatility_regime` signals. Root cause: D015/global-edge mode skipped core risk vetoes (`position_size`, `max_exposure`, `theme_uniqueness`, quality/catalyst), Hunter risk overrides allowed 100% single-name/concentration/gross exposure, forex cash factor inflated notional 20x, and execution fallback cap had been loosened to `$10m`. Fixed `risk/engine.py` so D015 no longer bypasses hard risk checks, added paper/live hard per-order notional caps and allocator-amplification rejection, added forex/future asset-class caps, tightened Hunter config caps, reduced Hunter adaptive concentration/gross fraction, changed default/config forex cash factor from `0.05` to `0.20`, restored execution runaway cap default to `$100k`, and added accounting diagnostics for filled-order fee vs `daily_pnl.total_fees` drift. Also fixed the global-edge execution path to pass fill fees into `_upsert_daily_pnl`. Stopped the running system via `/system/stop`, restarted `python run.py` so the fixed code is loaded, and left trading OFF; `/diagnostics/accounting` now returns `ok=true` with the expected `daily_pnl_fees_differ_from_filled_orders` warning (`fee_delta=1903.17939089`) for today's pre-fix paper fills. Verification: `python -m py_compile risk/engine.py portfolio/global_edge_coordinator.py system/trading_loop/loop.py system/trading_loop/helpers.py api/server.py execution/engine.py`; `python -m pytest tests/test_risk_engine.py tests/test_adaptive_sizing.py tests/test_global_edge_coordinator.py tests/test_signal_engine_coordinator_sizing.py tests/test_execution_engine.py -q -k "sizing or risk_notional or target_notional or boundary or d015 or exposure or order_notional or amplification"` (`41 passed, 73 deselected`); `python -m pytest tests/test_api_dashboard_extras.py tests/test_global_edge_coordinator.py tests/test_execution_engine.py tests/test_orchestrator_stop.py -q` (`77 passed, 1 skipped`); `python -m pytest tests/test_api_dashboard_extras.py -q -k diagnostics` (`2 passed, 11 deselected`).
- Last completed task: **D071 — Zero-allocation stale-loop flatten watchdog**. Added `system/local_paper_flatten.py` as the reusable paper-only local ledger flatten helper and refactored `scripts/flatten_local_paper_book.py` to call it. Wired a `zero-alloc-flatten-watchdog` background task in `system/orchestrator.py`: in paper mode only, when `capital_pct=0` and the trading loop is stopped or stale (`ZERO_ALLOC_FLATTEN_STALE_SEC`, default max 180s / 2.5x loop interval), it writes filled reduce-only close `OrderLog` rows plus zero-quantity `PositionLog` tombstones directly with `flatten_reason=zero_allocation_watchdog`, then wakes the loop to republish a flat dashboard. The watchdog refuses live mode, has interval/cooldown/env disable knobs, and is cancelled on orchestrator stop. Restarted `python run.py` with fresh logs and started the system so the watchdog code is loaded; final verification: all five brokers connected, trading loop running, `capital_pct=0.0`, `/positions` empty, `/diagnostics/accounting ok=true` with only the intentional D070 tombstone-shock warning, and `python -m scripts.flatten_local_paper_book` reports already flat. Tests: `python -m py_compile system/orchestrator.py system/local_paper_flatten.py scripts/flatten_local_paper_book.py`; `python -m pytest tests/test_zero_alloc_flatten_watchdog.py tests/test_orchestrator_stop.py -q` (`6 passed`).
- Last completed task: **D070 — Local paper flatten recovery after wedged loop**. Investigated the 2026-05-04 slider-at-0 state where the UI still showed about `$1.08m` at work. Backend `capital_pct` was correctly `0.0`, but the trading loop had stopped advancing (`iterations=26`, `last_iteration_at=13:49:20`) and did not consume the capital-allocation wake, so the normal zero-allocation `global_edge_flatten` path never emitted close actions. IBKR itself had no native paper positions to close (`scripts.flatten_ibkr_paper` dry run found `submitted=0`), proving the exposure was only the local paper `PositionLog` ledger. Added `scripts/flatten_local_paper_book.py`, a paper-only dry-run-first repair tool that writes filled reduce-only close `OrderLog` rows plus zero-quantity `PositionLog` tombstones. Applied it to flatten `alpaca:CARG`, `alpaca:CART`, `ibkr:ALNY`, `ibkr:AZO`, and `ibkr:BE`, then restarted the system cleanly. Verification: `python -m py_compile scripts/flatten_local_paper_book.py`; `/positions` empty; `/diagnostics/accounting ok=true` with only intentional tombstone-shock warnings; all five brokers connected after restart; trading loop running with `capital_pct=0.0`.
- Last completed task: **D069 — IBKR connector failure diagnosed + read-only gateway diagnostic added**. Investigated the 2026-05-04 IBKR Gateway failure. Gateway logs show it logged in and published GBP NetLiquidation internally, but rejected third-party API sockets with `Paper trading disclaimer must first be accepted for API connection`, leaving Gateway UI `API Client: disconnected`. A live read-only probe from Python later found `127.0.0.1:7497`, `localhost:7497`, and `::1:7497` refusing API connections, matching `/system/status` where `ibkr` is excluded and risk-disabled while Alpaca/Binance/Bybit/Kraken remain connected. Added `scripts/diagnose_ibkr_gateway.py`, a no-order read-only diagnostic that classifies common IBKR API failures (paper disclaimer, duplicate client id, port refused, timeout), and `docs/IBKR_CONNECTIVITY.md` documenting why retail IBKR cannot be made into a normal direct API-key connector like Kraken/Binance: TWS API requires authenticated TWS/IB Gateway, Client Portal/Web API usually requires a local Java gateway for individual users, and direct OAuth/FIX are separate onboarding/connectivity tracks. Verification: `python -m py_compile scripts/diagnose_ibkr_gateway.py`; `python scripts/diagnose_ibkr_gateway.py` correctly reported `port_refused_or_api_listener_not_accepting` for the current local Gateway state.
- Last completed task: **D068 — Partial broker coverage accounting made basis-consistent**. Fixed the dashboard state where an IBKR outage could show a partial `$96k` NAV while still comparing against full-book history/positions, producing fictional `-$1m` week/month/YTD moves and `1000%+` leverage. `/positions`, live MTM, paper synthetic position overlays, and dashboard exposure now filter to the current connected/balance-ready NAV broker set whenever broker coverage is partial. `/pnl` now marks `nav_status.coverage_full=false`, preserves partial coverage metadata, and suppresses whole-book realised/period/all-time/drawdown aggregates instead of applying them to a partial NAV denominator. The redesign UI now treats partial broker coverage as a changed accounting basis: hero day-open uses current-basis P&L only, and week/month/YTD display `0` rather than history deltas from full-book NAV. Verification: `python -m py_compile api/server.py`; `python -m pytest tests/test_live_portfolio_value.py tests/test_api_dashboard_extras.py -q -k "pnl or portfolio or partial or dashboard or filters"` (`30 passed, 1 skipped`); UI `npm run build`.
- Last completed task: **D067 — Profit-harvest monitor added**. Added first-class profit banking via `risk/profit_harvest.py` and `profit_harvest` config in `config/risk_limits.yaml`: partial take-profit, full take-profit, and trailing profit-lock decisions are pure/evaluable and runtime emits reduce-only signals through normal risk/execution (`strategy=profit_harvest_monitor`) with audit metadata (`profit_harvest_reason`, profit %, peak, giveback, reduce fraction). Wired a dedicated `profit-harvest-monitor` background task in `system/orchestrator.py` using the local paper ledger as book of record so it works across IBKR/Alpaca/Kraken/Binance/Bybit paper positions. Tests: `python -m py_compile risk/profit_harvest.py system/orchestrator.py`; `python -m pytest tests/test_profit_harvest.py tests/test_stop_loss_monitor.py -q` (`7 passed`). After restart, startup briefly restored a stale `100%` allocation and broker truth resurfaced old paper positions; immediately persisted allocation back to `0%`, and the existing zero-allocation flatten closed all resurfaced IBKR/Kraken paper rows. Final verification: all five brokers connected, `capital_pct=0.0`, `/positions` empty, DB latest-open rows `0`, working orders `0`, dashboard gross/net/cash deployed `0`, accounting diagnostics `ok=true` with tombstone audit warning only.
- Prior: **D066 — Paper book reset and dashboard/broker alignment**. With operator approval, stopped the system, persisted `system.capital_allocation=0`, inserted zero-quantity tombstones for every latest open paper row (`ALNY`, `ARKM/USD`, `AVT`, `AXS`, `AZN`, `BR`, `ETH-USD`), and reset today's `daily_pnl.unrealised_pnl`/fees to zero. Fixed `api/server.py` so `/dashboard/snapshot` overlays zero exposure even when `/positions` is empty, clears position-derived `weakest_by_hold_score` / `highest_exit_pressure`, rebuilds `global_edge.held_edges` from the current `/positions` book, normalizes zero Decimals to `"0"`, and makes `/pnl` use persisted `system.capital_allocation` instead of a restarted orchestrator default. Restarted `python run.py`, then started the system at `0%` allocation. Verification: all five brokers connected (`alpaca`, `binance`, `bybit`, `ibkr`, `kraken`), trading loop running with `capital_pct=0.0`, `/positions` empty, DB latest-open rows `0`, working orders `0`, dashboard gross/net/cash deployed all `0`, held edges `0`, `/pnl` tradable capital `0`, and `/diagnostics/accounting ok=true` with only the intentional tombstone audit warning. Tests: `python -m py_compile api/server.py`; `python -m pytest tests/test_api_dashboard_extras.py tests/test_execution_engine.py -q -k "pnl or diagnostics or reconcile"` (`9 passed, 1 skipped`).
- Prior: **D065 — Accounting self-diagnostics + paper restore tool**. Added `GET /diagnostics/accounting` in `api/server.py` so the system can explicitly report open-position counts/exposure by broker, open-book unrealised PnL vs `daily_pnl`, and recent position tombstone shocks instead of requiring manual dashboard forensics. Added `scripts/restore_paper_positions_before_tombstone.py`, a dry-run-first repair tool that can reinsert latest pre-tombstone paper ledger rows for a broker after operator review. Dry-run against `ibkr` at `2026-05-01T12:23:17Z` found 8 restorable rows (`ARKM-USD`, `ATRA`, `AZN`, `AZTA`, `BBDC`, `CARE`, `CNTA`, `USDCHF`) with about `$1.902m` gross exposure, so it was deliberately **not applied** on top of the newly rebuilt `$1.08m` book. Verification: `python -m py_compile api/server.py execution/engine.py scripts/restore_paper_positions_before_tombstone.py`; `python -m pytest tests/test_api_dashboard_extras.py tests/test_execution_engine.py -q -k "diagnostics or reconcile"` (`9 passed`).
- Prior: **D064 — IBKR paper-ledger tombstone guard**. Investigated the 2026-05-01 Book screenshot showing only Alpaca/Kraken rows. `/positions?limit=200` and DB latest-position queries both showed no open `ibkr` rows; the latest IBKR ledger entries were zero-quantity tombstones at `2026-05-01 12:23:17 UTC`, while there were no same-day IBKR close orders. The pre-tombstone IBKR paper rows were local simulated fills (`ARKM-USD`, `ATRA`, `AZN`, `AZTA`, `BBDC`, `CARE`, `CNTA`, `USDCHF`) from 2026-04-30. Root cause: local paper reconciliation could tombstone a broker's simulated positions if that broker was absent from the execution broker map during startup/reconnect. Fixed `execution/engine.py` so, when `APP_ENV=paper` and `EXECUTION_PAPER_USE_BROKER_ORDERS` is not enabled, all latest local paper rows are seeded as reconciliation truth before broker availability is considered. Added `test_paper_reconcile_preserves_local_position_when_broker_absent`. Verification: `python -m py_compile execution/engine.py`; `python -m pytest tests/test_execution_engine.py -q -k "reconcile"` (`6 passed`). Full `tests/test_execution_engine.py -q` currently has one unrelated sizing-boundary expectation failure.
- Prior: **D063 — Dashboard accounting bases aligned**. Audited the 2026-04-30 screenshots where NAV/tradable/Book totals looked misaligned: the values were mixing raw notional exposure (`gross`, `net`, notional-free) with cash/margin deployed (`capital at work`). Fixed `api/server.py` so `/dashboard/snapshot` overlays `portfolio.cash_deployed` and `cash_deployed_pct` from the same full `/positions` book used by the Book card, including asset-class cash factors. Renamed the right-rail exposure label from `cash` to `gross free` so it no longer reads as deployable cash. Preserved raw notional exposure in the top strip and kept cash-at-work as the slider-matching metric. Also fixed `/pnl` in paper mode to use the paper ledger as book of record instead of occasionally taking a broker-native unrealised total while `/positions` used the ledger. Live verification after restart: all five brokers connected, `capital_pct=0.5`, no open/pending orders, NAV about `$1.0728m`, gross notional about `$705.9k` (`65.8%`), net notional about `$307.9k` (`28.7%`), and cash deployed about `$538.9k` (`50.2%`). Tests: `python -m py_compile api/server.py`; `python -m pytest tests/test_live_broker_prices.py tests/test_adaptive_sizing.py tests/test_global_edge_coordinator.py tests/test_wave2_wiring.py -q` (`69 passed`); UI `npm run build`.
- Prior: **D062 — Slider cash-at-work exactness and UI accounting fixed**. Continued the 2026-04-30 capital deployment audit after adaptive sizing reached the target internally but the UI/debug view still appeared stuck around `36.7%`. Root cause: the dashboard/debug calculation summed only the truncated top-12 `global_edge.held_edges`, and the redesign `mapPositions()` sliced the full `/positions` book to 24 rows before `capitalAtWork()`; the UI also used raw notional rather than cash-deployed semantics, so forex/crypto margin treatment diverged from the allocator. Fixed the redesign to keep the full polled position set, carry `assetClass`, infer forex/crypto/futures from symbols, and calculate capital-at-work as cash used (`notional * cash_factor`) for both filled positions and pending exposure-increasing orders. Dashboard snapshots now publish `portfolio.cash_deployed` and `cash_deployed_pct` from the full `PortfolioState`. Tightened adaptive slider behavior: moved adaptive mode/factor/min-order knobs into `config/global_edge.yaml`, prevented fallback to legacy opening when the adaptive book is already at/above target, added configurable `adaptive.target_tolerance_pct`, and made shed-to-target emit partial reduce-only trims for the exact excess instead of whole-position closes. Live verification after restart: all five brokers connected, `capital_pct=0.5`, `portfolio.cash_deployed=536432.17` on `NAV=1072799.89`, `cash_deployed_pct=0.499984`, and no further open/trim actions were emitted because the book was within tolerance. Tests: `python -m py_compile system/dashboard_publish.py system/trading_loop/loop.py portfolio/global_edge_coordinator.py signals/arb_bridge.py`; `python -m pytest tests/test_adaptive_sizing.py tests/test_global_edge_coordinator.py tests/test_wave2_wiring.py -q` (`59 passed`); UI `npm run build`.
- Prior: **D061 — Hunter paper sizing unclamped from live broker cash**. Investigated slow/undersized deployment on 2026-04-30 after D060: candidate logs showed Hunter strategies now generating opens, but global-edge still capped every action around `$4.8k` because `system/trading_loop/loop.py` fed coordinator sizing from `live_portfolio_value()` broker cash (`~$96.6k * 50%`) instead of the paper ledger NAV (`~$1.07m * 50%`). Changed the global-edge handoff to use `_load_portfolio_state()`'s `portfolio_value` and `tradable_capital` for coordinator sizing. Also made Hunter paper profiles more assertive: mode-aware base notionals across momentum/mean/event/pairs/regime/volume/volatility strategies, trained meta Hunter threshold `0.35`, heuristic meta Hunter threshold `0.40`, and `global_edge.max_actions_per_tick.hunter=20`. Restarted `python run.py` and started the system. Live verification: all five brokers connected; next cycle selected/executed Hunter-scale actions (`COSO` `$42.9k`, `ALGT` `$41.3k`, `BSCX` `$40.4k`, short `CARG` `$40.8k`, `USDCAD` `$27.1k`), dashboard gross exposure rose to about `$192.4k` on `$1.07m` NAV, roughly `36%` of the 50% working-capital sleeve after one cycle. Tests: `python -m py_compile system/trading_loop/loop.py strategies/momentum.py strategies/mean_reversion.py strategies/event_driven.py strategies/pairs_trading.py strategies/regime_rotation.py`; `python -m pytest tests/test_strategies.py tests/test_global_edge_coordinator.py tests/test_wave2_wiring.py -q` (`50 passed`).
- Prior: **D060 — Futures data-only candidates stopped blocking the paper book**. Investigated the empty Book on 2026-04-30: the loop was healthy at 50% capital in Hunter, but global-edge was selecting continuous futures (`ES=F`, `NQ=F`) while `FUTURES_EXECUTION_ENABLED=0`, so they were persisted as signals with `execution_gated=futures_disabled` and never reached risk/orders; a prior cycle also showed approved Alpaca opens returning generic `execution_no_result`. Changed `portfolio/global_edge_coordinator.py` so continuous futures are excluded from opportunity ranking unless futures execution is explicitly enabled, preserving the existing `SKIP_CONTINUOUS_FUTURES` override. Added execution skip reasons in `execution/engine.py` and surfaced them in `system/trading_loop/loop.py` candidate logs; paper-local fills now ignore broker balance-exhausted cooldowns when native paper orders are not being used. Restarted `python run.py` and started the system. Live verification: all five brokers connected; the next cycle selected executable `volume_flow` equities and filled seven IBKR paper positions (`COSO`, `ALGT`, `BSCX`, `AMTM`, short `CARG`, short `CALM`, `CHDN`); `/positions` returned all seven open rows and dashboard gross exposure was about `$33.6k`. Tests: `python -m py_compile portfolio/global_edge_coordinator.py execution/engine.py system/trading_loop/loop.py`; `python -m pytest tests/test_strategies.py tests/test_wave2_wiring.py -q` (`21 passed`).
- Prior: **D059 — Hunter mode candidate gates repaired**. Diagnosed the “Hunter not adding positions” state on 2026-04-29: runtime control had `strategy.enabled.momentum_breakout=false`, while the loop was awake at 50% capital but publishing `no_batch_candidates`; candidate logs showed volume/volatility strategies using trader-level thresholds in Hunter and many generated raw candidates disappearing inside the trained meta/news gate without diagnostic metadata. Added profile-mode-aware strategy config overlays via `Strategy.effective_config()`, wired the active profile mode into each loop cycle, added Hunter relaxations for `volume_flow` and `volatility_regime` in `config/strategies.yaml`, passed `profile_mode` into D015 `raw_to_signal_candidate`, and preserved meta-label details on `filtered_signal_engine` candidate-log rows. Re-enabled `momentum_breakout` in `control_state`, restarted the app, and started the system. Live verification: all five brokers connected, loaded strategies show momentum enabled, 22:40 local cycle filled new paper orders (`ALGT`, `BRBR`, `CSIQ`) and trimmed stale holdings (`AMTM`, `AXTA`, `CWK`); `/positions` returned open `ALGT`, `BRBR`, `CSIQ`. Tests: `python -m py_compile strategies/base.py strategies/volume_flow.py strategies/volatility_regime.py signals/engine.py system/trading_loop/loop.py`; `python -m pytest tests/test_strategies.py tests/test_wave2_wiring.py -q` (`21 passed`).
- Prior: **D058 — Fixed-height Book panels with local scrolling**. Updated `ui/src/app/redesign/screens.tsx` so the Book main column has stable rows (`Open Positions` plus fixed `Recent position changes`) and each panel owns its own vertical scroller. Open positions no longer resize/push the recent-changes panel when more positions arrive, and recent fills scroll locally inside their fixed card. Also made Book row keys broker-symbol scoped. Verification: UI `npm run build`.
- Prior: **D057 — Book disappearance audit + recent fills visibility**. After restart on 2026-04-29 the open book was correctly three latest non-zero rows, but the set changed again while the loop kept running: `21:17 UTC` sold `ALGT` and bought `CWK`, leaving `AMTM`, `AXTA`, and `CWK`. Added a Book-side “Recent position changes” table directly under Open Positions in `ui/src/app/redesign/screens.tsx` so the operator can see fills in the same main panel instead of hunting in the right rail/orders view. Verification: UI `npm run build`; backend focused tests `36 passed, 1 skipped`; `/dashboard/snapshot` positions_sample confirmed `AMTM`, `AXTA`, `CWK`.
- Prior: **D056 — Open-position ledger fallback fixed**. Diagnosed the Book showing only three positions on 2026-04-29. Live DB showed only three latest non-zero broker-symbol rows (`ATRC` short Alpaca, `AXTA` long Alpaca, `CWK` long IBKR); the rest were present as zero-quantity tombstones after flatten/reconciliation. Fixed `api/server.py` so `/positions`, dashboard portfolio exposure, and PnL MTM fallback use latest row per `(broker, symbol)` with tombstones suppressing stale older rows, then return open rows only. This removes zero tombstone leakage from API payloads and avoids the fragile single-global-max-timestamp slice. Verification against local DB returned exactly the three open rows; focused tests `36 passed, 1 skipped`; `python -m py_compile api/server.py`.
- Prior: **D055 — API DB/command-bus late-bind recovery**. Diagnosed a stuck dashboard NAV state on 2026-04-29 where the API process started before Docker/Postgres was available: `/healthz` was OK, but `/readyz` reported `db:false` and `/system/status` returned `503 {"detail":"Command bus unavailable"}` while the UI still showed cached broker coverage (`5 of 5 configured brokers ready`). Added lazy API database/CommandBus rebind in `api/server.py` so dashboard/control dependencies retry `init_async_database()` after Postgres comes online instead of requiring a full code-path restart after late Docker startup. Kept pytest no-DB semantics intact. Verification: `python -m py_compile api/server.py`; focused tests `49 passed, 1 skipped`.
- Prior: **D054 — Responsive capital slider open/close contract**. Fixed the capital allocation slider as an active deployment ceiling in both directions. Raising the slider now wakes the trading loop immediately instead of waiting for the normal cadence, preserves allocator-selected opportunity confidence/edge through the coordinator-to-signal handoff, avoids duplicate AI/meta gates after global-edge has already selected an open, and in paper mode uses deterministic local fills after risk/execution checks instead of parking IBKR paper orders indefinitely. Fixed paper-mode NAV selection so the paper ledger, not small real broker cash balances, drives risk/dashboard NAV and does not create fake drawdown rejections. Lowering to `0%` now cancels DB-tracked working orders before flattening filled exposure, forces the zero-allocation flatten path even when the loop is in fallback mode, blocks stale in-flight open actions before risk/execution if the slider drops to zero mid-iteration, and treats `PositionLog` as the paper-mode book of record for every broker so live adapter overlays cannot erase offline/native-empty paper positions before flattening. Dashboard/positions endpoints now include paper-ledger positions in snapshots, and paper reconciliation preserves the local simulated ledger for all brokers. Live verification on 2026-04-28: `0%` flattened to nonzero positions `0`, working orders `0`, dashboard gross `0`; moving back to `50%` woke the loop and produced a filled paper `AVTR` position with working orders `0`, dashboard source `position_log`, persisted/status capital `0.5`, and no trading error.
- Last completed task: **D053 — Reconciliation tombstones stale broker positions**. Fixed the post-flatten “positions reappeared” bug: `ExecutionEngine._reconcile_positions_internal()` now loads latest local position rows per broker-symbol and writes explicit zero-quantity tombstones when broker truth no longer contains a local non-zero position. This prevents stale `positions` rows from resurrecting in the Book/dashboard after IBKR/Alpaca closes or after a broker returns an empty position list. Added `test_reconcile_tombstones_local_position_missing_from_broker`. Live verification on 2026-04-28 after restart and multiple loop cycles: `/dashboard/snapshot` reported gross `0`, net `0`, positions `0`, held_edges `0`; direct DB latest non-zero position query returned none; all brokers active and `capital_pct=0.0`.
- Last completed task: **D052 — Zero-allocation flatten repair**. Fixed the capital slider at `0%` so it persists across restart and actively emits risk-checked reduce-only flatten actions instead of only blocking new buys. Flatten actions now use market orders, bypass signal/meta filters while still passing risk, replace stale in-flight flatten orders, run even when no new candidates exist, and prioritize no-native-paper crypto venues. Fixed paper crypto accounting: Kraken/Binance/Bybit live adapter positions no longer overwrite simulated paper books, position snapshots now load latest rows per broker-symbol, write zero tombstones on closes, and preserve duplicate symbols held on multiple brokers. Live verification on 2026-04-28: Kraken/Binance crypto positions flattened to zero; Alpaca equity market close orders are open after-hours; IBKR remains broker/session dependent.
- Last completed task: **D051 — Universe Intelligence Layer**. Added a correlation/factor-aware universe selection package (`universe/`) with eligibility filtering, return/correlation graph construction, connected-component clustering, representative selection, anomaly promotion/expiry, and a tier builder that writes the existing `data/runtime/universe_tiers.json` contract plus rich `data/runtime/universe_intelligence.json`. Added `config/universe_selection.yaml`, `scripts/build_universe_tiers.py`, `docs/UNIVERSE_INTELLIGENCE.md`, and `tests/test_universe_intelligence.py`. Smoke builder over local DB worked; default filters pruned aggressively. No trading/risk/execution path was changed.
- Last completed task: **D050 — Research/paper automatic model training conductor**. Added `config/auto_training.yaml`, `scripts/auto_train_models.py`, `scripts/install_auto_training_task.ps1`, and `docs/MODEL_TRAINING_RUNBOOK.md`. The conductor runs existing trainers one by one for meta-label, forecast, regime, and microstructure families, writes JSON run reports under `reports/models/auto_training/`, skips missing/insufficient data instead of forcing bad models, and refuses any `allow_live_promotion` setting. Plan-mode smoke found meta-label retraining available, forecast jobs skipped safely because local SPY 1h history had only 67 rows vs the 300-row threshold, and regime/microstructure skipped because input CSVs are absent. Tests: `tests/test_auto_training.py`.
- Last completed task: **D049.1 — Paper strategy sleeves enabled by default**. Because the Strategy dashboard is a paper-mode control surface, the advanced strategy sleeves now default to paper-on rather than disabled: `factor_sleeve.enabled=true`, `stat_arb_pairs.enabled=true`, and both `options_directional.enabled=true` / `options_hedging.enabled=true` with `paper_only=true`. The redesign fallback roster also marks these cards enabled so the dashboard no longer shows disabled badges before `/system/status` refreshes. `Idle` remains meaningful: enabled sleeves can still show idle when no setup/eligible holding/chain/proposal/feature data exists.
- Last completed task: **D049 — Strategy dashboard roster hygiene + advanced sleeve cards**. Redesign Strategy screen now seeds the full operator-facing alpha roster, including `factor_sleeve`, `stat_arb_pairs`, `options_long_call`, `options_long_put`, `options_protective_put`, and `options_covered_call`, with family pills and disabled/idle state when the strategy is present but not enabled. Internal allocator maintenance actions (`global_edge_trim`, `trim_symbol`) are filtered out of the Strategy grid so they stay in allocation/execution observability instead of appearing as alpha strategies. Added `tests/test_ui_strategy_roster.py`; focused checks passed and UI production build passed.
- Last completed task: **D048 — First paper meta-labeler trained, registered, and soak-ready**. Built leakage-safe meta-label dataset from local audit DB with `scripts/build_meta_label_dataset.py` (3,679 rows, 1h horizon 10 bars), trained `mytbot_meta_labeler@0.1.0` logistic/Platt baseline (`artifacts/models/meta_label/mytbot_meta_labeler-0.1.0.pkl`, feature hash `8615c3cf26f8d684df3ebd90411af60039c380bd7ef154fd2b61be0cee1ab955`), registered it in `config/model_registry.yaml` and DB `model_versions` as `approval_status=paper`, enabled paper-only trained meta-labeling in `config/meta_labeler.yaml` + `config/strategies.yaml`, generated initial validation/model/paper-soak reports, and updated docs to reflect paper-enabled but not micro-live/live-approved status. Full suite: 830 passed, 3 skipped.
- Last completed task: **D047 — Broker reconnect readiness bypass + immediate late-broker ingestion**. BrokerManager now keeps automatic reconnect in place for every configured registered broker and adds an IBKR readiness edge: cheap TCP/API probes continue while disconnected, and a transition from not-ready to ready bypasses any outstanding full-connect exponential backoff so launching/restarting TWS/Gateway is picked up on the next health poll. TradingLoop now runs a `broker-join-poll` (`BROKER_JOIN_POLL_SEC`, default 2s) so newly connected adapters are added to router/execution without waiting for the next strategy iteration. Investigation on 2026-04-27 found Gateway logged in with API Server connected but API Client disconnected because "Paper trading disclaimer must first be accepted for API connection"; status text now points operators to accept the disclaimer/API prompt before treating it as a generic zombie/restart case.
- Last completed task: **D045 — Trading-engine audit fixes (global-edge sizing, crypto venue routing, trim exits)**. Fixed coordinator/D015 price fallback so missing prices no longer fabricate quantity from NAV; global-edge opportunities now preserve resolved price/side for SignalEngine sizing; spot crypto routes to dedicated venues instead of Alpaca by default, with fiat `*-USD` pairs pinned to Kraken unless explicitly converted to stablecoin venues; global-edge now emits reduce-only `trim_symbol` actions through normal risk/execution so held positions can be rotated/realised. Live verification after restart produced normal-sized Alpaca sell trims and no new 55k-unit crypto orders.
- Last completed task: **D043 — Strategy mix default roster + live sparklines from `/intelligence/signals`**. Redesign Strategy screen always shows the full signal + arb taxonomy; sparklines use recent DB signal confidences per strategy; `loaded_strategies` cleared when absent from `/system/status`.
- Last completed task: **D042 — Wave 9 (fee-prior + online fusion, slippage p50/p90, broker comparison)**. Router blends fee prior with learned quality for ranking (`fused_routing_score`), tracks rolling slippage |bps| and fill counts per broker-symbol, persists `broker_comparison` + `exec_metrics` in `routing.quality.state`; `GET /diagnostics/routing-quality` returns full state including `quality_stats`; Risk diagnostics adds a broker comparison table and uses fused scores for best-venue display.
- Last completed task: **D041 — Wave 8 (routing confidence intervals + adaptive decay + trajectory sparklines)**. Router now exports per-broker CI95 stats and uses adaptive decay driven by sample/activity/staleness proxies; loop feeds turnover/liquidity hints into routing feedback; diagnostics contract includes `quality_stats`; redesign Risk diagnostics adds routing trajectory mini-sparklines with CI display.
- Last completed task: **D040 — Wave 7 (persistent routing trajectories + mode-adaptive alerts + diagnostics endpoint)**. Routing learning now persists in control-state and reloads on startup; decay policy runs per configured cadence; demand alert history is mode-threshold driven; new `/diagnostics/routing-quality` endpoint and redesign diagnostics panel show per-symbol routing quality trajectories.
- Last completed task: **D039 — Wave 6 (learned routing feedback + mode-adaptive demand thresholds + UI diagnostics)**. Added learned broker-symbol routing feedback in `execution/router.py`; loop now records execution outcomes into router quality; demand alert thresholds are mode-aware with alert history telemetry; redesign Risk screen now shows demand/meta diagnostics from snapshot/runtime heartbeat.
- Last completed task: **D038 — Wave 5 (demand-aware routing + adaptive meta priors + demand alerts)**. Added metadata-aware routing preferences in `execution/router.py`; added online strategy-bias calibration hooks in `signals/meta_adaptation.py` and loop integration; added demand-regime alert payloads to heartbeat/dashboard snapshots.
- Last completed task: **D037 — Wave 4 (demand telemetry + mode-calibrated meta-labeling + demand-conditioned planner urgency)**. Added demand sections to dashboard/global-edge snapshots and heartbeat, added mode-specific meta-label calibration (`defender/trader/hunter`) in strategy config + filter logic, and applied demand-alignment urgency multipliers in `execution/planner.py`.
- Last completed task: **D036 — Wave 3 (cross-asset demand graph + volatility overlay + ML-style meta-labeling)**. Added `system/cross_asset_demand_graph.py`; upgraded `system/demand_engine.py` to include graph outputs (`market_volatility`, coverage); upgraded `signals/meta_labeler.py` to probability-scored filtering; added allocator gross-exposure volatility overlay in `portfolio/allocation_engine.py`; tests in `tests/test_demand_meta.py` and `tests/test_allocation_vol_overlay.py`.
- Last completed task: **D035 — Demand-engine + volatility/regime/meta wave**. Added `system/demand_engine.py`, `signals/meta_labeler.py`, `strategies/volatility_regime.py`, `strategies/regime_rotation.py`; wired demand score + meta filtering into `system/trading_loop/loop.py`, plus demand-aware weighting in `signals/opportunity_engine.py` and `portfolio/global_edge_coordinator.py`.
- Last completed task: **D034 — Strategy expansion wave (event-driven, pairs, volume-flow)**. Added `strategies/event_driven.py`, `strategies/pairs_trading.py`, `strategies/volume_flow.py`; wired into `system/trading_loop/loop.py` for both legacy and batch candidate paths; added YAML gates in `config/strategies.yaml`; tests in `tests/test_strategies.py` (all green).
- Last completed task: **D033 — Post-open stop-loss monitor wired at runtime**. `Orchestrator` now starts a dedicated `stop-loss-monitor` background task that evaluates live positions with `risk.stop_loss.evaluate_stop_loss` every `STOP_LOSS_MONITOR_INTERVAL_SEC` (default 15s, min 5s). Breached positions are closed via normal risk+execution flow (`RiskEngine.evaluate_and_persist` then `ExecutionEngine.execute`) using `strategy=stop_loss_monitor` and `metadata.reduce_only=true`; no risk bypass. Added close-throttle `STOP_LOSS_CLOSE_COOLDOWN_SEC` (default 60s) per broker/symbol/direction to avoid repeated close spam while fills/reconciliation settle. Files: `system/orchestrator.py`, `tests/test_stop_loss_monitor.py`, `docs/DECISIONS.md` D033, `docs/ARCHITECTURE.md`. **Full suite: 382 passed, 3 skipped.**
- Prior: **D030 — Hunter must hunt** — mode-aware `max_notional_fraction_per_action` (hunter 1.00 / trader 0.50 / defender 0.15) + reconciliation persists broker-truth unconditionally.
- Prior: **`docs/NEW_MACHINE_SETUP.md`** + `scripts/setup_new_machine.{ps1,sh}` — full new-PC migration (requirements, Docker, Ollama models per `config/ai.yaml`, FinBERT, UI, `.env`); README + `Codex` key files + `.env.example` pointer.
- Prior: **Docs sync (audit D020–D023)** — `docs/BUILD_PLAN.md` **G6**; `docs/ARCHITECTURE.md` orchestrator/contracts. Prior implementation: DECISIONS D023.
- Prior: **D023 Operational hardening** — pytest `PYTEST_API_DISABLE_READ_MIDDLEWARE` + conftest default; Docker compose up retries (`DOCKER_COMPOSE_UP_ATTEMPTS`); orchestrator pipeline chunked sleep + `trading.orchestrator_starting`; IBKR `place_order` retry jitter (`IBKR_PLACE_ORDER_RETRY_JITTER_SEC`). Docs: `docs/DECISIONS.md` D023.
- Prior: **D022 Late brokers + AI status** — `ExecutionEngine(broker_manager=...)`, `_get_broker` prefers `broker_manager.adapters`, `add_allowed_broker` from `TradingLoop._check_late_brokers`; heartbeat `ai` via `runtime_ai_status()` (`AIRouter` / `NewsClassifier`); `GET /system/status` merges `trading.ai` from `runtime.heartbeat`. Docs: `docs/DECISIONS.md` D022.
- Prior: **D021 Audit hardening** — shared `bind_app_database` / trading loop reuses API pool (no second engine); `ExecutionEngine.__init__` registers `set_execution_engine`; `SignalEngine` Decimal veto + dual-AI gate; IBKR `str(strike)`; orchestrator `last_start_error`; LiveStrip first-cycle + error copy; `snapshotFetchFailed` clears when not running. Docs: `docs/DECISIONS.md` D021.
- Prior: **D020 (amend)** — `GET /system/status` **`trading.snapshot_published_at`**; UI stale clock; heartbeat + `SignalBrain`. Docs: `docs/DECISIONS.md` D020.
- Prior: **D019 Dashboard V2 control tower** — `signals/accumulator.py` `dashboard_snapshot()`, `system/dashboard_publish.py` persists `dashboard.snapshot` to `ControlState` from D015 + global-edge ticks; `GET /dashboard/snapshot`, `GET /pnl` week/month + metrics, WebSocket `payload.dashboard` hint; React `ui/` layout (`LiveStrip`, `SignalBrain`, `AllocationCenter`, `RiskGate`, `PerformancePanel`) with top `NewsTicker` + bottom `OpportunityTicker` preserved. Docs: `docs/ARCHITECTURE.md`, `docs/DECISIONS.md` D019.
- Prior: **D017 Stateful signal accumulation** — `signals/accumulator.py` (`SignalAccumulator`, half-life decay, alignment/conflict), `SignalEngine` optional integration + metadata (`accumulator_*`), `feed_ai_pipeline_result` after AI compute in `system/trading_loop.py`, `run_m3.py`, `run_m5.py`; `config/strategies.yaml` `signal_engine.use_signal_accumulator` (default on). Docs: `docs/ARCHITECTURE.md`, `docs/BUILD_PLAN.md` G5, `docs/DECISIONS.md` D017.
- Prior: **D016 IBKR single-leg options** — `core/instruments.OptionContractSpec`, optional `instrument_metadata` on `Order`/`Position` in `brokers/base.py` (serialization only; defaults preserve existing adapters), `IBKRAdapter.get_option_chain` / `qualify_option_contract` / `get_option_market_data`, `ExecutionEngine` passes option payload into `Order`, `RiskEngine._check_options_trading_policy` + `config/risk_limits.yaml` `options_trading` + `risk/options_env.py`, portfolio `option_premium_exposure` in `run_m3._load_portfolio_state`, Alembic `c8f2a1d0e4aa`, smoke script `scripts/smoke_ibkr_options.py`. Default **options disabled** (`ENABLE_OPTIONS=false`).
- Prior: D015 **primary** operational path (allocator batch → risk → execution); see DECISIONS D004 amendment.
- Next task: Paper soak on primary path; monitor `trim_symbol` fills/cancels and clear old pending order backlog if needed; IBKR options paper validation with `scripts/smoke_ibkr_options.py` when enabled.
- Blockers: GPU server for faster inference; IBKR stream/order may still need local IB Gateway/TWS restart when the Gateway process is listening but API handshakes do not respond (observed 2026-04-25 and again as `CloseWait` socket buildup on 2026-04-27)
- Notes: .env not committed — use .env.example; `python run.py` is the ONLY command needed; Codex API disabled by default in config/ai.yaml; Ollama running on localhost:11434 with qwen2.5:7b + llama3.1:8b. **2026-04-12:** codebase assessment follow-up — IBKR `_remaining_safe` (NaN/stale `remaining`, PAXOS cash-qty fills), broker reconnect backoff jitter, `tests/test_ibkr_map_status.py` + execution `cancel_all` test, DECISIONS duplicate D012–D014 table. M2 already wraps yfinance/News/FRED in `_to_thread_with_retry`; Bollinger via `pandas_ta.bbands` in `data/features.py`.

## RULES Codex MUST FOLLOW IN THIS PROJECT
1. Never break `brokers/base.py` — the `BrokerAdapter` ABC and existing fields are frozen; backward-compatible optional dataclass fields (e.g. `instrument_metadata`) are allowed when all adapters default them to `None`
2. Never add a bypass to the risk engine
3. Decimal for all prices and quantities, never float
4. paper_mode=True is always the default
5. Every new broker = one new file + one line in registry.py, nothing else
6. Log every signal, risk decision, order, and fill
7. AI only scores and explains — never executes (local-first; paid API is optional fallback)
8. Check `docs/DECISIONS.md` before making architectural choices

## HOW TO USE THIS FILE
At the start of a Codex session, say:
"Here is my project context: [paste this file]
Current task: [describe what you want to work on]"

Codex will immediately understand the full architecture and constraints
without needing to re-explain everything.
