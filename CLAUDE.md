# CLAUDE.md
# ==========
# This file is for Claude (claude.ai).
# Paste the contents of this file at the start of any Claude conversation
# to instantly bring Claude up to speed on the project state.
#
# Update the CURRENT STATE section after each work session.

## PROJECT
`mytbot` — open-source autonomous multi-asset trading system under AGPL v3, published by Emfasys Labs, a division of Emfasys Ltd.
GitHub: https://github.com/emfasys-labs/mytbot.git
Owner: UK-based, trading stocks, bonds, ETFs, forex, crypto.
Primary broker: IBKR Pro. Crypto: Kraken + Binance.

## ARCHITECTURE IN ONE PARAGRAPH
**One-button system.** `python run.py` starts everything — orchestrator brings up
Docker (Postgres, Redis), auto-discovers available brokers (skipping unavailable),
runs the trading loop and data pipeline, and exposes an API+WebSocket for the UI.
The UI has a single ON/OFF button (`POST /system/start`, `POST /system/stop`).
All brokers implement a single abstract interface in `brokers/base.py`.
Signals pass through an optional **Signal Accumulation Engine** (`signals/accumulator.py`) that maintains time-decayed, multi-source conviction per asset (quant + news + macro) before the signal engine emits a unified Signal.
The signal engine aggregates strategy outputs into a Signal (with legacy point-in-time AI modifier and optional accumulated overlay).
Every Signal passes through the risk engine (unconditional veto power).
Approved signals go to the execution engine which routes to the best broker.
Everything is logged. Runtime broker permissions support graceful fallback routing.
Risk parameters are managed by `ParameterManager` with layered overrides
(regime > AI > defaults) and bounded, auditable changes.
AI is local-first (rules → FinBERT → local LLM → optional paid fallback) — never places orders.

## KEY FILES
- `run.py`                   — **THE entry point** (`python run.py` starts everything)
- `system/orchestrator.py`   — state machine: OFF → STARTING → RUNNING → STOPPING → OFF
- `system/dependency_manager.py` — auto-start Postgres/Redis via Docker
- `system/broker_manager.py` — auto-discover and connect available brokers
- `system/trading_loop/`    — controllable async trading loop package (`TradingLoop`, `helpers.py`)
- `brokers/base.py`          — the adapter interface (FROZEN: backward-compatible optional fields only)
- `brokers/registry.py`      — add new brokers here (one line)
- `brokers/ibkr/adapter.py`  — IBKR + single-leg options (chain / qualify / `Order.instrument_metadata`)
- `core/instruments.py`      — `OptionContractSpec` (options as structured instruments)
- `risk/options_env.py`      — `ENABLE_OPTIONS` / `OPTIONS_*` env merged into risk config
- `brokers/bybit/adapter.py` — Bybit V5 (spot / USDT linear)
- `brokers/_template/`       — copy this to add any new exchange
- `risk/engine.py`           — risk checks, kill switch, D115 FX + equity-index cluster caps
- `risk/intraday_derisk.py`  — D115 graduated portfolio-level derisk decision layer
- `risk/parameters.py`       — parameter manager (defaults + overrides + expiry)
- `signals/accumulator.py`   — stateful time-decayed conviction per symbol (optional; YAML-gated)
- `signals/anti_churn.py`    — D115 dedup / cross-strategy contradiction / post-fill cooldown gate
- `signals/engine.py`        — signal aggregation + accumulator integration + anti-churn wiring
- `strategies/momentum.py`   — first strategy (momentum breakout)
- `execution/engine.py`      — order placement
- `execution/router.py`      — smart order routing
- `storage/models.py`        — database schema
- `api/server.py`            — FastAPI: `/system/start`, `/system/stop`, `/system/status`, `/dashboard/snapshot`, `/pnl` (week/month)
- `api/pnl_periods.py`       — calendar week/month rollups over `daily_pnl` for `/pnl`
- `system/dashboard_publish.py` — persists allocator snapshot (`dashboard.snapshot` in `ControlState`) for the UI
- `config/risk_limits.yaml`  — all risk thresholds (editable without code change)
- `config/m8_micro_live.yaml` — optional micro-live profile (symbol/strategy/notional caps when `APP_ENV=live`)
- `config/fundamentals.yaml` — parameter defaults, absolute bounds, AI policy
- `config/broker_permissions.yaml` — runtime broker permission/fallback map
- `config/data_pipeline.yaml` — M2 symbols, intervals, News/FRED toggles
- `config/ai.yaml`          — local-first AI config: providers, escalation policy, no daily caps
- `config/profile_modes.yaml` — D015 mode coefficients + emergency safety_bounds (allocator)
- `config/allocation.yaml`  — D015 global opportunity replacement policy (validated by `config/models.py`)
- `config/loaders.py`       — `load_profile_modes()` / `load_allocation()`
- `core/models_runtime.py`  — runtime types: Opportunity, RegimeState, AllocationDecision, ExecutionPlan
- `signals/volume_anomaly.py` — D015 volume/flow features, detection vs scoring, YAML-weighted component
- `signals/opportunity_engine.py` — D015 opportunities; blends momentum proxy + volume component from M2 JSON + metadata
- `risk/regime_state.py` — D015 market/regime context for allocator (stub)
- `portfolio/allocation_engine.py` — D015 global replacement allocator (stub)
- `execution/planner.py` — D015 AllocationDecision → ExecutionPlan
- `data/regime_metrics.py` — cross-section feature fetch + aggregates for regime
- `data/feature_lookup.py` — latest `feature_snapshots.features` per symbol
- `portfolio/d015_hold_switch.py` — hold score + switching-cost penalty
- `signals/d015_weights.py` — profile/regime dynamic coefficient resolver
- `signals/opportunity_components.py` — momentum/news/liquidity/structure component scores
- `system/d015_shadow.py` — optional per-signal D015 vs legacy log (env `ALLOCATOR_D015_SHADOW`)
- `core/signal_math.py` — bounded_sigmoid, tanh_clip, normalize_zscore (Decimal)
- `run_pipeline.py`          — M2: yfinance → features → Postgres; NewsAPI + FRED
- `ai/router.py`             — local-first AI router (drop-in for NewsClassifier)
- `ai/providers/`            — provider implementations (rules, FinBERT, Ollama, Claude fallback)
- `ai/escalation.py`         — necessity-based escalation engine (no hard daily caps)
- `ai/schemas.py`            — shared AI types (ProviderResult, EscalationContext)
- `ai/news_classifier.py`    — legacy Claude classifier (kept for backward compat)
- `ai/pipeline.py`           — M6 orchestration: symbol news score + macro regime
- `docs/DECISIONS.md`        — architectural decision log
- `docs/BUILD_PLAN.md`       — milestones M1–M10 + task history
- `docs/NEW_MACHINE_SETUP.md` — new PC / full reinstall (Python, Docker, Ollama, UI, `.env`)
- `docs/M8_MICRO_LIVE.md`    — micro-live guardrails (`APP_ENV=live`, YAML profile)
- `alembic/`                 — DB migrations (URL from POSTGRES_* in env.py)
- `tests/`                   — pytest smoke tests
- `.cursorrules`             — Cursor AI alignment rules

## CURRENT STATE
<!-- Update this section after each work session -->
- Milestone: M10 — Local-First AI Architecture ✅
- Last completed task: **D171 — News signal quality: wire orchestrator news scores + tier-1 source selection**. Live audit: feeders + AI healthy but news felt weak because orchestrator passed `news_score=None`, aggregator publishers dominated the scoring budget, and many cycles had `0 non-zero` symbol matches. Fix: `system/trading_loop/loop.py` passes `ai_result.news_scores` into orchestrator/global-edge `process_coordinator_action` via `_news_score_for_symbol()`; `data/news_quality.py::select_news_rows_for_scoring()` + `config/ai.yaml::pipeline.news_source_selection` prefer tier-1 wire (Reuters/Bloomberg/CNBC) and cap tier-3 aggregators (TradingKey/Stock Titan); expanded forex/futures alias matching in `ai/pipeline.py`. Tests +28 in news/AI suites. **Requires `python run.py` restart.** See `docs/DECISIONS.md` D171.
- Last completed task: **D170 — Move the reasoning AI layer off the local GPU onto Gemini 2.5 Flash (free), evidence-validated**. Owner can't keep a machine on 24/7 and wants £0 hosting, so the bot must be host-portable (target: a free always-on cloud VM, e.g. Oracle Always-Free ARM). The only GPU-bound piece is the `local_reasoning` LLM — and it's not load-bearing (rules → FinBERT → local LLM → premium; AI only scores, never trades). Built read-only A/B harness `scripts/compare_ai_providers.py` (15 financial headlines → bias agreement / sentiment MAE / JSON validity / latency / accuracy vs analyst labels). **Result: local `gpt-oss:20b` ~60–100s/call (single probe 98.6s) and times out at its 20s prod limit → contributes ~nothing live; Gemini 2.5 Flash (free): ~3s/call, 100% JSON, 93–100% directional accuracy.** Switching is a strict upgrade and frees the GPU. Implementation: `ai/providers/local_reasoning_provider.py` already spoke OpenAI-compatible — added API-key auth (`api_key`/`api_key_env`/`LOCAL_REASONING_API_KEY` → `Authorization: Bearer`) + hosted model-trust (don't gate on Ollama-style installed-set check when keyed), fully backward-compatible (keyless Ollama unchanged). `config/ai.yaml::local_reasoning` → Gemini OpenAI endpoint, `gemini-2.5-flash`, `api_key_env: GEMINI_API_KEY`, `max_tokens: 2048` (2.5 thinking budget), `timeout 30`, `gpu_concurrency 2`; old Ollama block kept commented for instant revert. `.env` gained `GEMINI_API_KEY`. **Live after restart:** startup log `hosted model active | model=gemini-2.5-flash`; production scored real news via `local_reasoning(gemini-2.5-flash)` (KR 3.4s, MRNX 4.7s) where old Ollama logged `all models failed (20531ms)`. New tests `tests/test_local_reasoning_hosted.py` (6); AI suite (hosted+pipeline+escalation) **27 passed**. Caveats: free-tier RPM limits (fine for necessity-gated escalation; falls back on 429); supplied key has an `AQ.` prefix — if it's an ephemeral token (not a permanent `AIza…`) it'll expire and the chain falls back, replace with a permanent AI Studio key for 24/7. Fully reversible (uncomment Ollama block). Next: free cloud VM deploy (ARM Docker compose + IBC for IBKR). See `docs/DECISIONS.md` D170.
- Last completed task: **D168 + D169 — Phase 3.2 churn fix + Phase 4 brain re-admission (in shadow)**. After the D167 fixes were verified clean on a ~4h soak (proven longs fire, no new fractional futures, `USDJPY` round-trips), the soak exposed two next steps; both done, tested, deployed (full suite **1955 passed, 3 skipped**). **D168 (Phase 3.2) — close the last management-layer churn source.** D166 gave the stop-loss/derisk monitors a horizon-aware min-hold gate but left `profit_harvest_monitor` ungated; live evidence: `XLE` closed by profit-harvest at **−$138 just 32 min after open** (and `CL=F`/`USO`) because `trailing_profit_lock` fires *into the red* with only a 0.05%-NAV peak floor — pure churn on a young daily thesis. Fix: pure tested guard `risk/profit_harvest.should_suppress_harvest_for_horizon()` wired into `system/orchestrator._run_profit_harvest_tick()` (reusing D166's `_protective_exit_config` + `_fills_age_seconds_by_symbol`). Suppresses ONLY a trailing-lock realising a loss/immaterial gain on a position younger than `min_hold_sec` (3 days); always allows full/partial take-profit, a lock banking material (≥0.10% NAV) profit, matured positions, and unknown-age; safety stop/derisk paths untouched (rule 2 intact). **D169 (Phase 4) — re-admit the D163-shadowed trained meta-labeller, evidence-first.** New `signal_engine.trained_meta_labeler_shadow: true` (`signals/engine.py`): scores every signal and stamps would-keep/would-drop (`meta_label_shadow`/`meta_label_kept`/prob/threshold) but NEVER drops; distinct from `use_trained_meta_labeler` (hard enforce). Refactor makes it score allocator-selected/operator-close signals too (so the scoreboard sees the ones that become fills) while still never dropping them. New read-only `scripts/report_brain_shadow.py` joins shadow decisions vs the `fills` ledger: reconstructs `(broker,symbol)` round-trip streaks from `position_qty_after`, attributes each to the nearest shadow entry, buckets realised P&L into would-KEEP/would-DROP, verdict **RE-ADMIT / DO_NOT_ADMIT / INSUFFICIENT_DATA** (needs ≥ min_sample round-trips per side). Live: v0.2.0 model scores real ~0.13 probs and would-DROP exactly the churners (`USDJPY`/`CL=F`/`USO`/`XLE`); scorecard reads it end-to-end (currently INSUFFICIENT_DATA until round-trips mature). Only a RE-ADMIT verdict justifies flipping `use_trained_meta_labeler: true`. New tests: `tests/test_profit_harvest.py` (+6), `tests/test_brain_shadow.py` (8). Both fully reversible. **Requires `python run.py` restart (done; running, 5 brokers, `capital_pct=1.0`).** See `docs/DECISIONS.md` D168/D169.
- Last completed task: **D167.1 / D167.2 — Phase 3.1: anti-churn ordering bug + futures whole-contract enforcement**. A post-D167 soak (zero fills in ~3h despite the regime gate now open) surfaced two concrete defects, both fixed surgically + proven by tests (full suite **1941 passed, 3 skipped**). **Bug 1 (D167.1) — edge-gate-BLOCKED side suppressed a PROVEN side via anti-churn.** The D115 cross-strategy contradiction gate ran *before* the per-side edge gate (D161): on `USDJPY=X`, `mean_reversion`'s **short** (already `blocked`/toxic, PF<1 — can never trade) tombstoned the symbol and dropped `trend_breakout`'s **proven long** (PF 4.11) with `anti_churn:cross_strategy_contradiction` — the best weapon couldn't trade its only setup. Fix: new `TradingLoop._filter_edge_gate_blocked_raws()` drops edge-gate-BLOCKED `(strategy, side)` RAW candidates **before** anti-churn records them (both D015 + legacy paths, after `apply_regime_weighting`); `reduced`/`allowed`/unproven-reduce pass through; logged as `edge_gate_blocked`/`stage=pre_anti_churn`. Genuine 2-tradeable-side contradictions still caught; no-op when edge gate disabled. **Bug 2 (D167.2) — fractional-contract futures fills.** The signal engine rounds futures to whole contracts (D165), but a downstream notional clamp (risk single-name / crypto-venue room) recomputes `qty=clamped_notional/price` in raw units → fractional contract (live: `CL=F` mult 1000 filled **313 units = 0.313 contract** after a risk clamp, later a 2313-unit fractional book). Fix: `ExecutionEngine._normalize_order_for_broker()` (the single post-clamp chokepoint) now floors opening futures to a whole multiple of `futures_multiplier(symbol)` and skips sub-contract opens (qty→0 → existing guard); reduce-only closes pass through so fractional residuals can be flattened. Catches every clamp site at once; routing was already IBKR-only (D165). New tests: `test_futures_execution_normalize.py` (5) + `test_edge_gate_loop_filter.py` (+3). Reversible. **Requires `python run.py` restart (done; auto-resumed running, `capital_pct=1.0`, 5 brokers).** **Next: confirm scoreboard shows trend_breakout long round-trips + no new fractional-contract futures fills.** See `docs/DECISIONS.md` D167.1/D167.2.
- Last completed task: **D167 — Phase 3: Maximize edge expression (un-gate the proven weapons + backfill universe breadth)**. Phase 3 = "make the proven weapons actually FIRE and hold to thesis" (D166 did the *hold* half). A live read-only funnel trace (`strategy_candidate_log`, 6h) found two structural blockers starving the proven long-horizon weapons before risk/execution: **(1) THE BUG — stale macro-regime gate.** `config/ai.yaml::default_signal_strategies` (the anchor every `regime_strategy_gates[regime]` uses, D032) was never updated when D158 added the trend weapons, so `allowed_strategy_names()` omitted `trend_breakout`/`trend_following` and `filter_by_allowed_strategies` dropped **100%** of their signals as `filtered_regime`/`macro_regime_gate` — the trace caught the strongest weapon `trend_breakout` (PF **4.11**, `allowed`) getting all 22 long setups gated out. Fixed: added both to the list + extended the D032 coverage test so it can't silently recur. **(2) BREADTH — daily-history starvation.** trend_following (MA50/200→200 bars) and trend_breakout (L150→150 bars) need deep daily history, but only **23 symbols** had ≥200 daily bars (the D157.1 set) of 1,399 — so they could only fire on a tiny slice. Launched a one-time **2y daily backfill across the active universe** (`core,scan`=259 symbols) via existing `run_pipeline.py --backfill --symbols-from-universe` (idempotent, parallel to live loop, no code change). **(Part C investigated, left alone):** XLE `execution_precheck_rejected` are transient IBKR-paper delayed-data liquidity rejections that self-heal on retry (already mitigated by D140) — not an edge blocker, not destabilizing the working precheck. Full suite **1933 passed, 3 skipped** (no regressions). Reversible. Live after restart: 5 brokers, `capital_pct=1.0`. **Next: confirm on the scoreboard that trend_breakout now passes the gate + round-trips, then Phase 4 (re-admit shadowed brain vs scoreboard).** See `docs/DECISIONS.md` D167.
- Last completed task: **D166 — Phase 2: Cost & Validation Discipline (horizon-aware anti-churn + cost-model reconciliation + scoreboard-gated trust)**. The D163 scoreboard proved the entire realised loss lived in the *management* layers (`stop_loss_monitor`/`aggregate_derisk`/`intraday_derisk`) cutting **daily-horizon theses before they could mature** (median hold ~8.4h, 75% intraday) while the 4 proven daily weapons had **0 live closes**. Phase 2 stops the churn tax with 3 reversible pieces (no risk bypass, catastrophic protection intact): **(A) horizon-aware protective-exit gate** — new pure `risk/protective_exit_gate.py` suppresses *soft* stop/derisk exits for positions younger than `min_hold_sec` (**3 days**) but always allows catastrophic (≥4% NAV or ≥30% of cost basis), structural ATR (thesis invalidated), and most-severe aggregate tier (survival) cuts; position age derived from the `FillLog` open streak (no schema change); wired into all 3 orchestrator risk ticks. **(B) cost-model reconciliation** — `scripts/report_edge_scorecard.py` now loads backtest `fee_bps`/`slippage_bps` and flags `BACKTEST_TOO_KIND` when live fee-drag+slippage exceed the assumption, so `allowed`-but-live-negative weapons are caught. **(C) scoreboard-gated trust** — `_orchestrator_strategy_trust` only applies the live-P&L posterior after ≥`min_live_closes_for_posterior` (**8**) closes, and **caps a net-negative live strategy at neutral (1.0)** regardless of an optimistic backtest prior, so capital never flows toward a proven live loser. Config: `config/risk_limits.yaml::protective_exit_min_hold`, `config/strategies.yaml::portfolio_orchestrator.min_live_closes_for_posterior`. New tests: `test_protective_exit_gate.py`, `test_orchestrator_strategy_trust.py`, `test_edge_scorecard_cost.py` (+28). Full suite **1933 passed, 3 skipped**. Reversible (`protective_exit_min_hold.enabled:false` + `min_live_closes_for_posterior:0`). Live after restart: all 5 brokers connected, `capital_pct=1.0`. **Next: Phase 3 (maximise edge expression) after a soak shows the proven core round-tripping under the anti-churn gate.** See `docs/DECISIONS.md` D166.
- Last completed task: **D165 — Enable continuous-futures execution (front-month resolver + multiplier-aware boundary + IBKR-only routing)**. The proven daily weapons kept firing their strongest signals on continuous futures (`CL=F`, `ES=F`, `NQ=F`, `GC=F`) but futures execution was gated off, so those edges produced **0 live closes**. Owner chose to **enable real futures** (not exclude). Built the full safe path: (1) `core/instruments.py` `FuturesContractSpec` + `FUTURES_CONTRACT_SPECS` (root→exchange+multiplier; `=F` suffix REQUIRED so a futures root can't collide with an equity ticker — CL=Colgate); (2) IBKR front-month resolver `ContFuture→Future` (`_resolve_front_future`, cache + near-expiry rollover) — verified live qualifying `CLQ6` (Jul-2026 crude, multiplier 1000); (3) multiplier-aware accounting — internal qty stays in notional-consistent UNITS (`contracts×mult`), all contract↔unit conversion at the IBKR boundary (order build units→whole contracts, fills/positions contracts→units, `signals/engine.py` rounds to whole contracts), `broker_symbol_for` now PRESERVES `=F`; (4) **IBKR-only routing** — fixed a live bug where `CL=F` mis-routed to **Bybit** (a crypto-perp venue) producing a 671-fractional-barrel garbage fill: the router `asset_class=="future"` carve-out conflated CME futures with crypto perps; now `=F` futures pin to IBKR and **skip** (never crypto) if IBKR can't take them; enabled `ibkr.future` permission; (5) multiplier-aware liquidity check — IBKR reports futures depth in contracts, so `get_order_book` scales futures sizes by the multiplier (fixed a false `liquidity=444.4 min=222180` rejection). **Paper-validated end-to-end:** `CL=F` BUY → IBKR → `CLQ6` → `qty=3000` (3 contracts × 1000) → filled → position `ibkr CL=F qty=3000 ac=future avg=74.03` (~$222k notional). Files: `core/instruments.py`, `brokers/ibkr/adapter.py`, `execution/router.py`, `signals/engine.py`, `system/trading_loop/helpers.py`, `config/broker_permissions.yaml`, `.env`. Tests: new `test_futures_contract_spec.py` / `test_futures_sizing.py` / `test_ibkr_futures_execution.py` + futures-routing in `test_router_permissions.py` + multiplier-scaling in `test_ibkr_market_data_mode.py`. Full suite **1905 passed, 3 skipped**. Reversible (`FUTURES_EXECUTION_ENABLED=0` or `ibkr.future.enabled=false` → futures cleanly skip). Note: IBKR paper serves DELAYED futures data only, so paper fills are local-simulated at the resolved contract/price. See `docs/DECISIONS.md` D165.
- Last completed task: **D164 — Signal-derived sizing (edge-proportional Kelly trust + breadth-dynamic per-name cap)**. Owner's standing principle: "no hard-coded absolute numbers — sizing must depend on live signals." D163 set the right direction but with flat sizing constants; D164 makes the two sizing levers derive their operating point from signals, config numbers stay only as safety rails. (1) **A-priori edge-Kelly trust:** the edge gate's measured win-rate `W` + profit-factor `PF` collapse to a clean closed-form Kelly stake `f* = W·(PF−1)/PF` (`backtest/edge_gate.py::kelly_fraction`); new `EdgeGateRegistry.edge_kelly_trust()` normalises it onto the orchestrator trust band (strongest proven weapon → `max_trust`, weaker proven between neutral and max, blocked → `min_trust`). The loop fuses this **prior** with the existing recent-P&L **posterior** in `_orchestrator_strategy_trust(edge_prior=...)`: `trust = clamp(prior × posterior, min, max)`. Capital now flows to proven edge (trend_breakout PF 4.11 → top trust) instead of being flat; only re-weights between weapons (gross budgeted separately → can't inflate deployment; doesn't touch the confidence gate). (2) **Breadth-dynamic per-name cap:** `orchestrate()` derives the per-name cap as `gross / firing_edges` clamped to `[min_position_pct_of_nav 0.02, max_position_pct_of_nav 0.20]` — diversify when many edges fire, concentrate when few; config values are now the safety CEILING/FLOOR, not the operating point. New diagnostics (`firing_breadth`, `per_name_frac`, `strategy_trust`) make the derived values auditable. Files: `backtest/edge_gate.py`, `portfolio/portfolio_orchestrator.py`, `system/trading_loop/loop.py`, `config/strategies.yaml`. Tests +8; updated `test_wave13_dashboard.py` for the D163 trained-meta shadow. Full suite **1884 passed, 3 skipped**. Reversible. **Requires `python run.py` restart.** See `docs/DECISIONS.md` D164.
- Last completed task: **D163 — Profitability reset: the scoreboard + a clean, sanely-sized proven core**. Owner verdict: system "makes losses a lot, doesn't earn much" despite "a lot of brain." Implemented Phase 0 + Phase 1 of the audit plan. **Phase 0 (scoreboard):** new read-only `scripts/report_edge_scorecard.py` joins backtest edge (`edge_gate_verdicts.json`) vs live realised edge (`fills` ledger) and prints expectancy (bt vs live), a cost ledger (gross / ALL fees / NET / fee-drag bps), and churn (closes / median hold / intraday share). **The "before" it captured:** net realised **−$49,469**, the 4 proven daily weapons had **0 live closes** (never round-tripped), and the entire loss sat in management layers — `stop_loss −$21.8k`, `aggregate_derisk −$13.9k`, `intraday_derisk −$12.8k`, `orchestrator −$4.5k @ PF 0.14`, only `profit_harvest +$3.6k`; orchestrator opened big at low conviction (median hold ~8.4h, **75% intraday**) and the de-risk machinery realised the damage before any *daily* thesis matured → loss was sizing+churn driven, not edge driven. **Phase 1 (clean core, Kelly-rational sizing — reverses D159/D159.1, config-only):** `portfolio_orchestrator` entry_conviction 0.08→**0.15**, concentration_exponent 2.5→**1.5**, max_position_pct_of_nav 1.00→**0.20**, gross_target 0.70/1.10/1.50→**0.50/0.75/1.00** (unlevered), net_cap 1.00→**0.70**; `risk_limits` single_name 1.00→**0.20**, intraday_adds 1.00→**0.20**, min_signal_confidence 0.30→**0.45**, fx_cluster 1.00→**0.25**, equity_index 1.00→**0.30**, crypto 1.00→**0.25** (D160 consolidation stays: one theme = one bet, now capped); shadowed the trained meta-labeler (`use_trained_meta_labeler` true→**false**; heuristic remains). **Clean baseline:** wiped via `scripts/reset_trading_data.py --execute` (keeps features/prices/models + capital slider), restarted `python run.py` → state=running, pipeline=true, capital=1.0, all five brokers connected, ledger clean (0 live closes). Tests: `175 passed, 1 skipped` across risk/orchestrator/cluster/edge-gate/meta-label/derisk suites. Reversible by restoring D159 values + meta-labeler true. Next: soak, read the scoreboard, scale weapons with live_pf>1 & net>0. See `docs/DECISIONS.md` D163.
- Last completed task: **D158 Phase 3 — Multi-horizon trend snipers (weekly/monthly)**. Insight: a trend weapon's horizon = its lookback + hold, not the bar timeframe — a 150-day breakout held months is a "monthly sniper" running in the existing daily loop on existing data (no new pipeline/loop). Validated via `scripts/sweep_trend_horizons.py`: trend edge strengthens monotonically with horizon — trend_breakout L50→L150 gave PF 2.22→**4.15** (win 64%→73%); trend_following MA20/50→50/200 gave PF 1.23→2.46 (textbook long-horizon momentum). Retuned the live weapons: trend_breakout lookback 50→**150** (monthly sniper), trend_following MA→**50/200** (monthly trend); bumped `backtest.max_hold_bars` 20→60 (intraday-era cap truncated long trends). Re-ran edge gate: **all 4 core weapons now ALLOWED full-size** — trend_breakout (PF **4.02**, 73% win, +$829/trade, strongest), trend_following (PF 1.98, promoted from REDUCED), mean_reversion (PF 1.94), momentum_breakout (PF 1.65). Army spans cadences: momentum/MR (days) → trend_following (wks-months) → trend_breakout (months). Canonical daily verdicts updated. Full suite **1852 passed**. See `docs/DECISIONS.md` D158 Phase 3.
- Last completed task: **D161 — Per-side edge verdicts + short-replay margin guard (edge is side-specific)**. Owner's review-fix pass added short replay to the harness; running it showed edge is side-specific and shorts could blow up unbounded. Implemented: (1) **short margin guard** in `backtest/harness.py` (short gross ≤ equity, mirrors long `total_cost <= cash`; kills the −$1.3T garbage) + `allow_longs` for long-only/short-only/both replay; (2) **per-side verdicts** — gate certifies each strategy twice (canonical `name` = long, `name#short` = short); `EdgeGateRegistry` methods take `side=` (default long, back-compat); loop filter blocks/reduces per candidate entry side (closes unaffected); (3) **adverse-evidence rule** — thin sample + negative expectancy → blocked even under reduce policy (zero-trade cold start keeps reduce). **RESULT: every proven weapon is LONG-ONLY** — short sides: momentum PF 0.37 BLOCKED, mean_reversion PF 0.36 (−$124k) BLOCKED, trend_following PF 0.59 BLOCKED, trend_breakout PF 0.34/25 trades blocked-by-adverse-evidence. Long sides unchanged (trend_breakout PF 4.11). Canonical verdicts = per-side daily run. Live effect after restart: army takes long entries only until a short edge is proven (the prior short-USD forex book was mean_reversion sell-side — the toxic side). Tests: +10 (harness guard/sides, registry per-side, loop per-side, adverse-evidence). Full suite **1876 passed**. **Requires `python run.py` restart.** See `docs/DECISIONS.md` D161.
- Prior task: **D160 — Cluster-aware construction: one theme = one big bet (not N fragments)**. After D159 removed the cluster caps, the orchestrator opened 5 correlated forex positions (all "short USD") fragmenting capital + paying 5× spread for one economic bet. Fix is cluster *awareness* (not a cap): `portfolio/cluster_map.py` maps `(symbol,asset_class,side)`→`(cluster,theme_sign)` for `fx_usd` (USD direction), `crypto_beta`, `equity_index` (SPY/QQQ/… ETF set). `portfolio_orchestrator.consolidate_clusters()` runs before per-symbol netting: each multi-member theme sums signed conviction and emits ONE intent on the strongest member, sized by COMBINED conviction (cap `cluster_conviction_cap`=1.5); singles/non-clustered pass through; opposing nets express nothing. Verified: 5 short-USD pairs + GLD → one big EURUSD @ 100% NAV + GLD separate. Config `portfolio_orchestrator.cluster_consolidation: true`. CAPS stay removed (D159) — intelligence not limit; concentration uncapped. Tests `tests/test_cluster_consolidation.py` (9). Full suite **1863 passed**. **Requires `python run.py` restart.** See `docs/DECISIONS.md` D160.
- Prior task: **D159 — Predator mandate: unleash sizing (no per-name caps), aggressive deployment, drawdown breaker as the one floor**. Owner directive — system was too conservative ("dead"); make it proactively fill capital, size by conviction with NO fixed per-name caps, concentrate up to 100% NAV on a confirmed signal. Config-only (reversible): risk_limits `single_name_notional.max_pct_nav` 0.05→**1.00**, `intraday_symbol_adds` 0.10→1.00, `max_daily_loss_pct` 0.02→0.05, `max_drawdown_pct` 0.10→**0.20** (the survival breaker), `min_signal_confidence` 0.50→0.30 (gates size not pass/fail); orchestrator `max_position_pct_of_nav` 0.05→**1.00**, `gross_target_pct` →0.70/1.10/1.50, `net_cap_pct_of_gross` 0.60→**1.00**, `concentration_exponent` 1.5→**2.5**, `entry_conviction_threshold` 0.15→0.08. Verified: a 2-weapon-confirmed BTC signal sizes to **100% of NAV**, weak signals 1% slivers, ~101% deployed. **Deliberate call: KEEP winners (esp. PF-4.02 snipers) + add dominant bets via the levered budget + close losers — NOT force-close winners (that's the D156 0%-win bleed and less profitable).** Kept: edge gate (targeting) + drawdown breaker (survival). Caveat: aggression amplifies the modest proven edges both ways, bounded by the 20% breaker. Full suite **1854 passed**. **Requires `python run.py` restart.** See `docs/DECISIONS.md` D159.
- Prior task: **D158 Phase 3.2 — Orchestrator order price-sizing fix (post-wipe health check)**. After the clean-slate wipe + restart, a health check found the system fully alive (loop iterating, 4 weapons evaluated each tick, orchestrator netting, risk+execution gating) but **nothing trading** because every orchestrated order was being rejected. Root cause: `_run_orchestrated_tick` built its `CoordinatorAction` metadata WITHOUT a price, so the signal engine's `quantity = target_notional / price` sizing fell back to a wrong default price (~$1.60 vs GLD's real $398) → orders sized ~250× too many shares (a $98k notional cap became a $24.4M, 20×-NAV order) → correctly rejected by the D132 execution pre-check (safety net worked, no bad fills). Fix: inject the live price into each order's metadata (`md["close"]`) from the orchestrator book's `current_price`, else the resolved latest daily close (`_resolve_price_for_symbol`, now passed into the tick); skip orders with no resolvable price. Also confirmed the 0.50 `min_signal_confidence` floor correctly throttles the down-weighted mean_reversion knife (~0.41 in a fade regime) while strong trend signals pass — calibration, not a bug. Files: `system/trading_loop/loop.py` (`_run_orchestrated_tick` price map + call-site `resolve_price`). Full suite **1854 passed**. **Requires a `python run.py` restart to take effect.** See `docs/DECISIONS.md` D158.
- Prior task: **D158 Phase 3.1 — Trend-hold polish + multi-instance enabler**. Closed the 2 deferred refinements. (1) Added config-gated `hold_with_trend` continuation to `trend_breakout`; validated it makes more total profit but lower quality (PF 1.89/52% vs breakout-only **4.15/73%**) → kept **default OFF**. (2) The real live-hold fix went into the **orchestrator**: extended flat-desire edge-protection to also hold positions younger than `min_hold_sec_before_flip`, set to **3 days** (was 1800s intraday-era) so fresh snipers ride early noise — no whipsaw. (3) `strategies/base.py` honours optional `strategy_name` config override → same class can run as multiple horizon instances via config (enabler; not populated — multi-horizon already spans weapon types). Tests: 2 new orchestrator (young-flat hold/close). Full suite **1854 passed**. **The 4-weapon daily army (trend_breakout PF 4.02 sniper / trend_following / mean_reversion / momentum_breakout) is complete — next is paper-soak via `python run.py`.** See `docs/DECISIONS.md` D158 Phase 3.1.
- Prior task: **D158 Phase 2 — Heterogeneous risk modes (global throttle × per-weapon temperament)**. Redefined Defender/Trader/Hunter from one global switch into two composable axes in `portfolio/portfolio_orchestrator.py`: (1) **global threat throttle** — `system/adaptive_mode.py` mode → systemic threat scalar (`mode_threat`: hunter 0.0 / trader 0.35 / defender 0.75); (2) **per-weapon temperament** — each weapon's intrinsic style (`weapon_temperament`: trend_breakout=sniper, trend_following/momentum=shotgun, mean_reversion=knife) with `size_mult` + `defensive_cut`. Effective conviction = raw × edge_trust × `(size_mult × (1 − threat × defensive_cut))`. Calm → heterogeneous by style (sniper 1.30, shotgun 1.0, knife 0.75); danger → retreat together-but-uneven (sniper ~1.01, shotgun ~0.55, knife ~0.19, can bench itself). New `OrchestratorConfig` fields + `threat_for()`/`temperament_factor()` + diagnostics (`mode`/`threat_level`/`temperament_factors`); empty config = factors 1.0 (backward compat). Loop config rebuild switched to `dataclasses.replace` (was dropping new fields). Config: `config/strategies.yaml::portfolio_orchestrator.{temperaments,weapon_temperament,mode_threat}`. Tests: 7 new (24 in `tests/test_portfolio_orchestrator.py`). Full suite **1852 passed**. **Answers the user: the army does NOT all become defenders together — systemic danger throttles everyone but temperament/regime keep it heterogeneous; edge-health benches malfunctioning weapons independently. Next: Phase 3 — weekly/monthly snipers.** See `docs/DECISIONS.md` D158 Phase 2.
- Prior task: **D158 Phase 1 — Move the army to the daily horizon (live cutover)**. Switched the live system to trade daily, where the only proven edge exists. All config/wiring, fully reversible: `system/orchestrator.py` TIMEFRAME default `1h→1d`; `config/data_pipeline.yaml::incremental` interval `1h→1d` (period `1mo`); `config/strategies.yaml` — **enabled** the 4 proven weapons (trend_breakout, trend_following, mean_reversion, momentum_breakout), **parked** (enabled:false) the 5 unproven (volume_flow, volatility_regime, event_driven_news, pairs_trading, regime_rotation); turned **ON** `edge_gate.enabled` + `portfolio_orchestrator.enabled`; canonical `data/state/edge_gate_verdicts.json` = the daily run (matches live timeframe — safety rule). Wired the 2 new weapons into the loop (imports + instantiation + `strategies` dict + `collect_raw_signals_for_symbol` params/block + both call sites). End-to-end smoke test green; full suite **1845 passed**. To revert: TIMEFRAME=1h + incremental interval 1h + flip enabled flags + gates off. **Next: Phase 2 — redefine the 3 risk modes as global-throttle × per-weapon-temperament; Phase 3 — add weekly/monthly snipers.** See `docs/DECISIONS.md` D158 Phase 1.
- Prior task: **D158 — Forge + validate trend weapons (sniper/shotgun); confirm edge is daily-only**. Built 2 new daily-horizon, direction-agnostic weapons to grow the arsenal: `strategies/trend_breakout.py` (Donchian/Turtle "sniper", rides big moves) + `strategies/trend_following.py` (MA-cross "shotgun"). Both gated off pending proof, wired into the edge gate (added `--only` filter + force-enable so gated-off strategies are still testable). **Result: on DAILY they're the strongest in the book** — trend_breakout ALLOWED (162 trades, +$54.8k, PF **2.78**, 64% win), trend_following REDUCED (552 trades, +$84.6k, PF 1.99); with mean_reversion (PF 1.80) and momentum_breakout (now PF 1.33) that's **four proven, complementary daily edges** (trend rides / MR fades → uncorrelated). **On 1h ALL are blocked** (trend_breakout PF 0.61, trend_following PF 0.51) — same profitable-daily/catastrophic-1h inversion as mean-reversion. **CONCLUSION across 6 weapon types: no positive-expectancy edge exists at 1h after ~30bps costs; all real edge is daily. No viable "fast hunter" with these weapons + retail data.** Daily verdicts → `data/state/edge_gate_verdicts_1d.json`. Tests: `tests/test_trend_strategies.py` (9). Full suite **1845 passed**. **Open decision: live loop trades 1h (no edge); the 4 proven weapons need daily — move live cadence to daily, or run a per-horizon loop.** See `docs/DECISIONS.md` D158.
- Prior task: **D157.2 — 1h mean-reversion re-tune attempt (conclusive: not salvageable on 1h)**. User chose to exhaust 1h tuning before accepting a timeframe move. Fixed 2 harness bugs (walk-forward discarded the train window → now passes it as `warmup_bars` context; D141 dynamic RSI overrode static → tuner patches it off), added a config-gated `trend_filter` to mean_reversion (default off). Swept 3 lever families on 730d/23-symbol 1h data at realistic costs: **RSI extremity** (47/53→25/75) cut bleed ~1400× (−$839k→−$81k) but never positive; **trend filter** (MA100) cut to 78 trades/PF 0.57 but still −$4,426; **exit policy** (mean-TP, TP/SL, short hold) all negative — canonical MR exits made it *worse* (exit-at-mean PF 0.28). **CONCLUSIVE: no positive-expectancy 1h config exists after costs — price doesn't reliably revert on hourly bars. Edge is on daily (PF 2.30).** Harness warmup fix + trend filter kept (trend filter alone cuts 1h bleed ~190×, useful as defensive default). Full suite **1836 passed**. **Recommendation: run mean_reversion on daily; keep it edge-gate-blocked on 1h.** Files: `backtest/harness.py`, `strategies/mean_reversion.py`, `scripts/tune_mean_reversion.py`. See `docs/DECISIONS.md` D157.2.
- Prior task: **D157.1 — Historical backfill + first real edge-gate verdicts (decisive timeframe finding)**. Ran the existing pipeline backfill (`python run_pipeline.py --backfill` = 2y daily; `--training-backfill --symbols ...` = 730d hourly) — feature store went from ~5 days to **500–730 daily + 13k–17k hourly bars/symbol** (23 symbols). Re-ran the D157 edge gate. **DECISIVE RESULT, timeframe-dependent:** on **1d**, `mean_reversion` is genuinely profitable (36 trades, +$3,719, PF 2.30, 57% win → ALLOWED), momentum fires 0 trades. On **1h (the timeframe the live loop trades)**, it inverts brutally: `mean_reversion` **BLOCKED** (4,513 trades, **−$347,532**, PF 0.43, 32% win), `volume_flow` BLOCKED (−$6.6k), `volatility_regime` BLOCKED (−$33k, 13.6% win), momentum 0 trades. **ROOT CAUSE of the live bleed confirmed: the system trades 1h, where no strategy has edge and mean-reversion is actively destructive — but mean-reversion has real edge on the DAILY timeframe it is NOT run on.** Safety rule: the canonical `data/state/edge_gate_verdicts.json` MUST be generated on the live trading timeframe (a 1d verdict on a 1h book would have ALLOWED the −$347k strategy); it is now the 1h run. **DECISION NEEDED: (a) move mean-reversion to daily where it's proven, (b) re-tune the 1h strategies, or (c) enable the gate on 1h to halt the bleeders.** See `docs/DECISIONS.md` D157 / D157.1.
- Prior task: **D157 — Strategy edge gate (prove post-cost expectancy before capital)**. A strategy gets capital only after proving positive out-of-sample expectancy after costs. `backtest/edge_gate.py` aggregates the walk-forward harness across the universe → per-strategy verdict (`allowed`/`reduced`/`blocked`/`insufficient_data`) persisted to atomic JSON `data/state/edge_gate_verdicts.json` by `scripts/run_edge_gate.py`. Loop enforcement (`_apply_edge_gate_filter`): drops `blocked` strategies' candidates, scales non-blocked candidates' confidence by the verdict multiplier (propagates to sizing + D156 orchestrator). Verdict = a-priori trust; orchestrator's live-P&L trust = posterior. **Gated OFF** (`config/strategies.yaml::edge_gate.enabled`, default false); `unproven_policy` (default `reduce`) handles cold start. **CRITICAL FINDING:** running the gate on live data revealed the feature store holds only **~135 hourly bars/symbol across 23 symbols (~5 days)** — momentum/mean-reversion/volatility produced **zero** backtest round-trips, volume_flow 2 (both losing). **No strategy can currently demonstrate edge** because there's almost no history; live churn is machinery-driven, not edge-driven. Gate correctly returns `insufficient_data` for all → half-size under default policy. Tests: `tests/test_edge_gate.py` (13), `tests/test_edge_gate_loop_filter.py` (4). Full suite **1836 passed**. **Next: backfill months of historical features, re-run gate, then enable enforcement.** See `docs/DECISIONS.md` D157.
- Prior task: **D156 — Portfolio netting orchestrator (stop strategies cancelling each other's edge)**. Diagnosed the "we never grow" bleed with a new read-only tool `scripts/diagnose_strategy_interaction.py`: the live book was an accidental market-neutral basket (gross ~103%, **net only 13.5%**, 45 equal-weighted ~$60k names, 18 symbols with strategies on opposite sides — momentum buys breakouts while mean-reversion buys dips), and the realised bleed came from the *management* layers (`global_edge_rotation` closed 10 positions at **0% win**, `capital_recycle` −$733) force-closing maturing positions (median hold 71 min) to chase marginal opportunities, while the alpha that matured was positive (`profit_harvest_monitor` +$605/73%, `volume_flow` +$85/67%). Built `portfolio/portfolio_orchestrator.py` — a pure Decimal portfolio-construction layer: (1) **alpha combination** nets all strategy intents per symbol into one conviction-weighted target (no offsetting long+short pairs), (2) **portfolio construction** sizes by conviction (concentration exponent + per-name cap), scales to a mode gross budget, and manages net deliberately (|net| ≤ `net_cap_pct_of_gross`×gross when two-sided; one-sided book left fully directional), (3) **edge-protected rebalance** never flips/closes a profitable or <30-min position for a weak opposing signal, suppresses sub-band churn. Wired into `system/trading_loop/loop.py::_run_orchestrated_tick` (+ `_orchestrator_strategy_trust` down-weighting bleeders via recent FillLog P&L), routing the netted minimal order set through the existing `process_coordinator_action` → `_process_signal_global` (RiskEngine → ExecutionEngine) path and **skipping the global-edge rotation/recycle tick** when active. **All 7 strategies stay enabled; not all apply each tick — resolved at the portfolio level** (exactly the user's requirement). **Gated OFF** by `config/strategies.yaml::portfolio_orchestrator.enabled` (default false) → zero change until paper-validated. Risk veto preserved (rule 2). Tests: `tests/test_portfolio_orchestrator.py` (17). Dry-run on the real conflicted symbols collapsed 6 offsetting intents → 2 conviction-weighted positions (QQQ 0.5-vs-0.5 correctly opened nothing). Full suite **1819 passed, 3 skipped**. Also earlier this session: partial-coverage accounting fixes (`run_m3.py` FillLog-based realised, offline-exposure sidecar, HWM partial-coverage guard; `control/runtime.py` broker-scope helpers; `api/server.py` `active_exposure_pct`). See `docs/DECISIONS.md` D156. **Next: flip `portfolio_orchestrator.enabled: true`, paper-soak, compare net exposure + realised curve vs baseline.**
- Prior task: **D118 — Self-tuning priority pre-filter + 6-stage universe funnel**. Replaced stratified-random candidate selection with a deterministic, self-tuning rule that scores every `unique_normalized` symbol (microseconds per symbol; no I/O) and picks the top-N. Both the per-component weights and the scoring budget N are self-tuning, with **no operator-tunable numbers** — only master kill switches and fixed safety bounds. New modules: `data/universe_score_ages.py` (atomic JSON `symbol -> {last_scored_at, last_score, score_count, first_seen_at}` with deterministic LRU eviction), `data/universe_prefilter.py` (pure `compute_priority_scores()` over 6 bounded components — `liquidity_prior`, `anchor_pin`, `freshness_bonus`, `registry_availability`, `asset_class_balance`, `region_balance`), `data/universe_weight_learner.py` (online logistic regression with AdaGrad + EWMA decay + per-weight `[0.05, 0.50]` clamping + sum-to-1.0 re-normalisation; labels each picked symbol by whether it entered `watching`), `data/universe_budget_controller.py` (AIMD throughput + utility saturation; clamps to `[200, 800]`; exposes binding constraint), `data/universe_transitions.py` (ring buffer with `(ts, symbol, from_tier, to_tier, reason, score_delta)`). `data/universe_builder.py::build_tiered_universe` now accepts `priority_scores`, `target_budget`, `anchors`, and a `BuildTelemetry` collector; falls back to legacy stratified sampling when no scores are passed. `system/orchestrator.py::_pipeline_runner` orchestrates load → score → budget → build → post-cycle weight/budget update → transition diff, all with atomic persistence. `universe/snapshot_service.py` publishes a 6-stage funnel (`unique_normalized` → `priority_ranked` → `scored` → `watching` → `promoted` → `active_reps`; raw broker-listing count is a debug tooltip on `unique_normalized`, not a stage), plus `priority_rule` (live learned weights + sparkline history + budget state + binding constraint + score-age summary), `transitions`, and `asset_class_coverage`. Per-symbol rows now carry `last_scored_at`, `last_score`, `score_count`, and `priority_breakdown`. UI: extended `ui/src/app/lib/api.ts` (`UniversePriorityRule`, `UniverseTransition`, `UniverseAssetClassCoverage`); `ui/src/app/redesign/universe/UniverseScreen.tsx` renames funnel stages, adds `PriorityRuleCard` (weights bar chart + per-component sparklines + budget meter), new `CoverageTab`, new `TransitionsTab`, plus a `last scored` column with coloured age stripe + `priority_breakdown` tooltip on InstrumentsTab. Config surface is minimal — `config/data_pipeline.yaml::dynamic_universe.ranking.priority_score` has only `enabled`, `weight_learning_enabled`, `budget_self_tune_enabled`, `state_dir`. Tests: `tests/test_universe_score_ages.py`, `tests/test_universe_prefilter.py`, `tests/test_universe_weight_learner.py`, `tests/test_universe_budget_controller.py`, `tests/test_universe_transitions.py`, `tests/test_universe_builder_priority_selection.py`, `tests/test_universe_snapshot_d118.py`. `tests/test_universe_intelligence.py` updated for `active_reps` label. UI `npm run build` clean. See `docs/DECISIONS.md` D118.
- Last completed task: **D117 — Adaptive universe-tier sizing (regime + signal pressure + cluster-aware + anti-churn)**. Replaced the pre-D117 static `max_symbols=300 / core_max=50 / scan_max=250 / max_candidates_to_score=400` caps with a per-tick adaptive policy that widens the discovery funnel in `risk_on/volatile/trend_up` (multipliers `1.25/1.30/1.15`) and narrows it in `risk_off/crash/range` (`0.80/0.65/0.90`), plus a signal-pressure axis (`batch_candidate_count >= 8 → 1.20`, `<= 2 → 0.80`) and a cluster-aware floor `watching >= max(150, 3 * active_reps)`. All resolved caps are clamped to YAML bounds (`candidates 200–800`, `watching 150–600`, `core 25–100`, `scan 75–500`). Added anti-churn hysteresis: a symbol that was in the previous watchlist must miss `3` consecutive rebuilds before it actually drops to `light`. Files: `universe/adaptive_caps.py`, `universe/adaptive_context.py`, `universe/adaptive_state.py`, `config/data_pipeline.yaml::dynamic_universe.adaptive`, `data/universe_builder.py::UniverseBuilder.update_caps()`, `system/orchestrator.py::_pipeline_runner`, `universe/snapshot_service.py` (overlay + new `adaptive` block), `ui/src/app/lib/api.ts`, `ui/src/app/redesign/universe/UniverseScreen.tsx` (new `adaptive Nx ↑ widen / ↓ focus` badge). Risk / order routing untouched: this is a discovery-layer control only. Backward compatible: set `adaptive.enabled: false` and the builder uses static caps exactly like pre-D117. Verification: focused suite `python -m pytest tests/test_universe_adaptive_caps.py -q` (`26 passed`); regression `python -m pytest tests/test_universe_intelligence.py tests/test_universe_tiers.py tests/test_universe.py tests/test_universe_adaptive_caps.py tests/test_orchestrator_stop.py tests/test_api_dashboard_extras.py -q` (`71 passed, 1 skipped`).
- Last completed task: **D116.1 — Universe page wired to the D116 instrument registry**. Closed the "I don't see any changes" gap reported after D116 went live: the registry payload was populated, but the Universe page UI was still showing pre-D116 numbers because (a) the IBKR `ibkr_supported_symbols_use_registry` feature flag in `config/instrument_registry.yaml` was still off and (b) the redesign Universe screen did not render the new `registry_known_count` / `registry_covered_count` fields. Fixes: flipped `ibkr_supported_symbols_use_registry: true` (orders still call `qualifyContractsAsync` so trade safety is unchanged); updated `universe/snapshot_service.py::_build_coverage` to derive the IBKR `source` / `note` from real raw counts (`curated_seed` pre-flag, `curated_seed+registry` post-flag); extended `IntelligenceUniverseResponse.coverage.by_broker` typing in `ui/src/app/lib/api.ts` with `registry_known_count` / `registry_covered_count`; updated `ui/src/app/redesign/universe/UniverseScreen.tsx::OverviewTab` to (1) replace the stale `ibkr 61 curated` substring with a dynamic `curated` vs `curated+registry` tag plus a `registry NNN known / NNN covered` summary, and (2) render a per-broker chip row showing `<name> · <raw> listed · <covered>/<known> registry` colored green when fully covered, blue when partially covered, amber when zero covered. Verification: `python -m py_compile universe/snapshot_service.py`; `npm run build` in `ui/`; `python -m pytest tests/test_universe_intelligence.py tests/test_universe.py tests/test_universe_tiers.py tests/test_asset_class_routing.py tests/test_ibkr_universe_qualification.py tests/test_ibkr_supported_symbols_from_registry.py -q` (`55 passed`); full `python -m pytest -q` (`1323 passed, 3 skipped, 5 warnings`). Live `/intelligence/universe` after restart: broker listings `16,442` → `17,980`, unique normalized `14,828` → `15,319`, IBKR raw `61 (curated_seed)` → `1,599 (curated_seed+registry)`, active reps `88` → `92`. Per-broker registry coverage: `alpaca 899/903`, `ibkr 13/1615`, `binance 0/1003`, `kraken 0/1415`, `bybit 0/1615`. Live system: `state=running`, `pipeline_running=true`, `capital_pct=1.0`, all five brokers connected, `coverage.full=true`.
- Last completed task: **D116 — Instrument Registry + Cross-Broker Availability Resolver**. Replaced hand-maintained per-broker symbol lists (especially IBKR's 61-line curated YAML seed) with a self-updating master instrument registry. Schema: `instrument_registry` (canonical PK, asset class, region, exchange, currency, sector/industry, ISIN, FIGI, timestamps, retired_at, metadata) + `instrument_source_membership` + `instrument_broker_availability` (status enum `unknown/available/unavailable/requires_qualification/blocked`) + `instrument_source_runs` audit log; migration `alembic/versions/d116a1b2c3d4_instrument_registry.py`. Canonical symbol form in `instruments/canonical.py` (yfinance conventions: `AAPL`, `HSBA.L`, `7203.T`, `BTC-USD`, `EURUSD=X`, `ES=F`). Source adapters in `instruments/sources/`: `wikipedia.py` (~20 indices), `ishares.py` (~40 ETF holdings CSVs), `openfigi.py` (ISIN/FIGI bulk mapping), `static_fx.py`, `static_futures.py`, `broker_catalog.py` (wraps `BrokerAdapter.get_supported_symbols()` per connected broker). All HTTP through `instruments/sources/http.py` (User-Agent, per-host rate limit, ETag/Last-Modified, retry with jitter). `instruments/availability.py::resolve_broker_availability()` walks the registry, attempts `instruments.canonical.canonical_to_broker` translation, falls back to OpenFIGI alternates, marks IBKR-only unknowns as `requires_qualification`. `instruments/builder.py` orchestrates refresh + availability with retire policy (min 5 consecutive misses across min 2 independent sources; nothing ever hard-deleted). `instruments/scheduler.py` runs four background tasks (constituents refresh, broker availability, OpenFIGI enrichment, broker-connect consumer); `system/broker_manager.py::register_connect_callback()` triggers immediate re-evaluation on every (re)connect. `brokers/ibkr/adapter.py::get_supported_symbols()` now returns the union of curated YAML seed + qualification cache + registry rows behind `IBKR_SUPPORTED_SYMBOLS_USE_REGISTRY` env / YAML feature flag (default off until populated); failures fall back silently to curated YAML and `place_order()` still calls `qualifyContractsAsync` before submission. `universe/snapshot_service.py` exposes `registry_known_count` + `registry_covered_count` per broker. New API endpoints: `GET /intelligence/instruments`, `GET /intelligence/instruments/{canonical}`, `GET /intelligence/instrument-sources`. CLI: `python scripts/build_instrument_registry.py --sources=all --dry-run`, `python scripts/qualify_instrument_registry.py --broker=ibkr --limit=100`. Config: `config/instrument_registry.yaml`. Docs: `docs/INSTRUMENT_REGISTRY.md`, `docs/DECISIONS.md` D116, `docs/ARCHITECTURE.md`. What did NOT change: `brokers/base.py`, `risk/engine.py`, `execution/engine.py`, `signals/*`, `strategies/*`, `portfolio/*`. Tests (59 passing): `tests/test_instruments_canonical.py` (13), `tests/test_instruments_registry.py` (7), `tests/test_instruments_sources_wikipedia.py` (4), `tests/test_instruments_sources_ishares.py` (4), `tests/test_instruments_sources_openfigi.py` (4), `tests/test_instruments_sources_broker_catalog.py` (5), `tests/test_instruments_availability.py` (8), `tests/test_instruments_builder.py` (6), `tests/test_instruments_scheduler.py` (3), `tests/test_ibkr_supported_symbols_from_registry.py` (4). Added `lxml>=4.9,<7` to `requirements.txt` (required by `pandas.read_html`). Verification: `python -m py_compile` for every new module; `python -m pytest tests/test_instruments_*.py tests/test_ibkr_supported_symbols_from_registry.py -q` → `59 passed`.
- Last completed task: **D115 — Anti-churn + cluster-aware risk + intraday derisk + stale-price gate**. Five-tier rectification after the 2026-05-19 paper P&L bleed audit (224 fills / $6.0M turnover / $1.18M NAV / ~$5k drag / no individual stop fired). Tier 1: `signals/anti_churn.py::AntiChurnGate` wired into `SignalEngine.process()` + `raw_to_signal_candidate()` BEFORE meta-label; `record_fill()` invoked from `system/trading_loop/loop.py` on every confirmed fill — same-strategy 90s dedup, cross-strategy long/short contradiction with 5-min tombstone, mode-aware post-fill cooldown (hunter 120s / trader 180s / defender 600s). Tier 2: `risk/engine.py::_check_fx_cluster_exposure` (15% NAV signed-USD cap; xxxUSD vs USDxxx orientation) and `_check_equity_index_cluster_exposure` (20% NAV signed cap on SPY/QQQ/IWM/DIA/VTI/VOO/IVV/MDY/TQQQ/SQQQ/SPXL/SPXS); reduce-only and neutralising legs never blocked. Tier 3: `risk/intraday_derisk.py` graduated tiers (-0.5% → 20% trim of worst 2, -1.0% → 50% trim of worst 4, -1.5% → full close of worst 6) wired as `Orchestrator._intraday_derisk_loop`; sits before `max_daily_loss_pct`, all reduce-only, still passes through risk + execution. Profit-harvest peak persistence already in place via `Orchestrator._persist_profit_harvest_peaks`. Tier 4: `execution/engine.py::_simulate_fill` rejects opening paper fill when `signal.suggested_price` drifts against trade direction by more than `stale_price_gate.max_adverse_drift_bps` (default 25 bps); reduce-only exempt. Tier 5: `scripts/flatten_orphaned_remnants.py` paper-only housekeeping tool, dry-run-first, filters by notional ceiling / symbol / broker / loss-pct. Backtest harness disables anti-churn + stale-price gates for the duration of the run (wall-clock semantics inapplicable for replayed bars). Config keys: `config/strategies.yaml::signal_engine.anti_churn` + `config/risk_limits.yaml::fx_cluster|equity_index_cluster|intraday_derisk|stale_price_gate`. Verification: `python -m py_compile signals/anti_churn.py signals/engine.py risk/engine.py risk/intraday_derisk.py execution/engine.py system/orchestrator.py system/trading_loop/loop.py scripts/flatten_orphaned_remnants.py backtest/harness.py`; full `python -m pytest -q` → `1235 passed, 3 skipped, 1 warning` (pre-existing AsyncMock fixture leak in `test_profit_harvest.py`). New tests: `tests/test_anti_churn_gate.py` (17), `tests/test_fx_cluster_exposure.py` (9), `tests/test_equity_index_cluster.py` (6), `tests/test_intraday_derisk.py` (11), `tests/test_stale_price_gate.py` (7), `tests/test_flatten_orphaned_remnants.py` (5).
- Last completed task: **D043 — Strategy mix default roster + live sparklines from `/intelligence/signals`**. Redesign Strategy screen always shows the full signal + arb taxonomy; sparklines use recent DB signal confidences per strategy; `loaded_strategies` cleared when absent from `/system/status`.
- Last completed task: **D042 — Wave 9 (fee-prior + online fusion, slippage p50/p90, broker comparison)**. Router blends fee prior with learned quality for ranking (`fused_routing_score`), tracks rolling slippage |bps| and fill counts per broker-symbol, persists `broker_comparison` + `exec_metrics` in `routing.quality.state`; `GET /diagnostics/routing-quality` returns full state including `quality_stats`; Risk diagnostics adds a broker comparison table and uses fused scores for best-venue display.
- Last completed task: **D041 — Wave 8 (routing confidence intervals + adaptive decay + trajectory sparklines)**. Router now exports per-broker CI95 stats and uses adaptive decay driven by sample/activity/staleness proxies; loop feeds turnover/liquidity hints into routing feedback; diagnostics contract includes `quality_stats`; redesign Risk diagnostics adds routing trajectory mini-sparklines with CI display.
- Last completed task: **D040 — Wave 7 (persistent routing trajectories + mode-adaptive alerts + diagnostics endpoint)**. Routing learning now persists in control-state and reloads on startup; decay policy runs per configured cadence; demand alert history is mode-threshold driven; new `/diagnostics/routing-quality` endpoint and redesign diagnostics panel show per-symbol routing quality trajectories.
- Last completed task: **D039 — Wave 6 (learned routing feedback + mode-adaptive demand thresholds + UI diagnostics)**. Added learned broker-symbol routing feedback in `execution/router.py`; loop now records execution outcomes into router quality; demand alert thresholds are mode-aware with alert history telemetry; redesign Risk screen now shows demand/meta diagnostics from snapshot/runtime heartbeat.
- Last completed task: **D038 — Wave 5 (demand-aware routing + adaptive meta priors + demand alerts)**. Added metadata-aware routing preferences in `execution/router.py`; added online strategy-bias calibration hooks in `signals/meta_adaptation.py` and loop integration; added demand-regime alert payloads to heartbeat/dashboard snapshots.
- Last completed task: **D037 — Wave 4 (demand telemetry + mode-calibrated meta-labeling + demand-conditioned planner urgency)**. Added demand sections to dashboard/global-edge snapshots and heartbeat, added mode-specific meta-label calibration (`defender/trader/hunter`) in strategy config + filter logic, and applied demand-alignment urgency multipliers in `execution/planner.py`.
- Last completed task: **D036 — Wave 3 (cross-asset demand graph + volatility overlay + ML-style meta-labeling)**. Added `system/cross_asset_demand_graph.py`; upgraded `system/demand_engine.py` to include graph outputs (`market_volatility`, coverage); upgraded `signals/meta_labeler.py` to probability-scored filtering; added allocator gross-exposure volatility overlay in `portfolio/allocation_engine.py`; tests in `tests/test_demand_meta.py` and `tests/test_allocation_vol_overlay.py`.
- Last completed task: **D035 — Demand-engine + volatility/regime/meta wave**. Added `system/demand_engine.py`, `signals/meta_labeler.py`, `strategies/volatility_regime.py`, `strategies/regime_rotation.py`; wired demand score + meta filtering into `system/trading_loop/loop.py`, plus demand-aware weighting in `signals/opportunity_engine.py` and `portfolio/global_edge_coordinator.py`.
- Last completed task: **D034 — Strategy expansion wave (event-driven, pairs, volume-flow)**. Added `strategies/event_driven.py`, `strategies/pairs_trading.py`, `strategies/volume_flow.py`; wired into `system/trading_loop/loop.py` for both legacy and batch candidate paths; added YAML gates in `config/strategies.yaml`; tests in `tests/test_strategies.py` (all green).
- Last completed task: **D033 — Post-open stop-loss monitor wired at runtime**. `Orchestrator` now starts a dedicated `stop-loss-monitor` background task that evaluates live positions with `risk.stop_loss.evaluate_stop_loss` every `STOP_LOSS_MONITOR_INTERVAL_SEC` (default 15s, min 5s). Breached positions are closed via normal risk+execution flow (`RiskEngine.evaluate_and_persist` then `ExecutionEngine.execute`) using `strategy=stop_loss_monitor` and `metadata.reduce_only=true`; no risk bypass. Added close-throttle `STOP_LOSS_CLOSE_COOLDOWN_SEC` (default 60s) per broker/symbol/direction to avoid repeated close spam while fills/reconciliation settle. Files: `system/orchestrator.py`, `tests/test_stop_loss_monitor.py`, `docs/DECISIONS.md` D033, `docs/ARCHITECTURE.md`. **Full suite: 382 passed, 3 skipped.**
- Prior: **D030 — Hunter must hunt** — mode-aware `max_notional_fraction_per_action` (hunter 1.00 / trader 0.50 / defender 0.15) + reconciliation persists broker-truth unconditionally.
- Prior: **`docs/NEW_MACHINE_SETUP.md`** + `scripts/setup_new_machine.{ps1,sh}` — full new-PC migration (requirements, Docker, Ollama models per `config/ai.yaml`, FinBERT, UI, `.env`); README + `CLAUDE` key files + `.env.example` pointer.
- Prior: **Docs sync (audit D020–D023)** — `docs/BUILD_PLAN.md` **G6**; `docs/ARCHITECTURE.md` orchestrator/contracts. Prior implementation: DECISIONS D023.
- Prior: **D023 Operational hardening** — pytest `PYTEST_API_DISABLE_READ_MIDDLEWARE` + conftest default; Docker compose up retries (`DOCKER_COMPOSE_UP_ATTEMPTS`); orchestrator pipeline chunked sleep + `trading.orchestrator_starting`; IBKR `place_order` retry jitter (`IBKR_PLACE_ORDER_RETRY_JITTER_SEC`). Docs: `docs/DECISIONS.md` D023.
- Prior: **D022 Late brokers + AI status** — `ExecutionEngine(broker_manager=...)`, `_get_broker` prefers `broker_manager.adapters`, `add_allowed_broker` from `TradingLoop._check_late_brokers`; heartbeat `ai` via `runtime_ai_status()` (`AIRouter` / `NewsClassifier`); `GET /system/status` merges `trading.ai` from `runtime.heartbeat`. Docs: `docs/DECISIONS.md` D022.
- Prior: **D021 Audit hardening** — shared `bind_app_database` / trading loop reuses API pool (no second engine); `ExecutionEngine.__init__` registers `set_execution_engine`; `SignalEngine` Decimal veto + dual-AI gate; IBKR `str(strike)`; orchestrator `last_start_error`; LiveStrip first-cycle + error copy; `snapshotFetchFailed` clears when not running. Docs: `docs/DECISIONS.md` D021.
- Prior: **D020 (amend)** — `GET /system/status` **`trading.snapshot_published_at`**; UI stale clock; heartbeat + `SignalBrain`. Docs: `docs/DECISIONS.md` D020.
- Prior: **D019 Dashboard V2 control tower** — `signals/accumulator.py` `dashboard_snapshot()`, `system/dashboard_publish.py` persists `dashboard.snapshot` to `ControlState` from D015 + global-edge ticks; `GET /dashboard/snapshot`, `GET /pnl` week/month + metrics, WebSocket `payload.dashboard` hint; React `ui/` layout (`LiveStrip`, `SignalBrain`, `AllocationCenter`, `RiskGate`, `PerformancePanel`) with top `NewsTicker` + bottom `OpportunityTicker` preserved. Docs: `docs/ARCHITECTURE.md`, `docs/DECISIONS.md` D019.
- Prior: **D017 Stateful signal accumulation** — `signals/accumulator.py` (`SignalAccumulator`, half-life decay, alignment/conflict), `SignalEngine` optional integration + metadata (`accumulator_*`), `feed_ai_pipeline_result` after AI compute in `system/trading_loop.py`, `run_m3.py`, `run_m5.py`; `config/strategies.yaml` `signal_engine.use_signal_accumulator` (default on). Docs: `docs/ARCHITECTURE.md`, `docs/BUILD_PLAN.md` G5, `docs/DECISIONS.md` D017.
- Prior: **D016 IBKR single-leg options** — `core/instruments.OptionContractSpec`, optional `instrument_metadata` on `Order`/`Position` in `brokers/base.py` (serialization only; defaults preserve existing adapters), `IBKRAdapter.get_option_chain` / `qualify_option_contract` / `get_option_market_data`, `ExecutionEngine` passes option payload into `Order`, `RiskEngine._check_options_trading_policy` + `config/risk_limits.yaml` `options_trading` + `risk/options_env.py`, portfolio `option_premium_exposure` in `run_m3._load_portfolio_state`, Alembic `c8f2a1d0e4aa`, smoke script `scripts/smoke_ibkr_options.py`. Default **options disabled** (`ENABLE_OPTIONS=false`).
- Prior: D015 **primary** operational path (allocator batch → risk → execution); see DECISIONS D004 amendment.
- Next task: Paper soak on primary path; IBKR options paper validation with `scripts/smoke_ibkr_options.py` when enabled.
- Blockers: GPU server for faster inference; IBKR stream/order need local IB Gateway/TWS
- Notes: .env not committed — use .env.example; `python run.py` is the ONLY command needed; Claude API disabled by default in config/ai.yaml; Ollama running on localhost:11434 with qwen2.5:7b + llama3.1:8b. **2026-04-12:** codebase assessment follow-up — IBKR `_remaining_safe` (NaN/stale `remaining`, PAXOS cash-qty fills), broker reconnect backoff jitter, `tests/test_ibkr_map_status.py` + execution `cancel_all` test, DECISIONS duplicate D012–D014 table. M2 already wraps yfinance/News/FRED in `_to_thread_with_retry`; Bollinger via `pandas_ta.bbands` in `data/features.py`.

## RULES CLAUDE MUST FOLLOW IN THIS PROJECT
1. Never break `brokers/base.py` — the `BrokerAdapter` ABC and existing fields are frozen; backward-compatible optional dataclass fields (e.g. `instrument_metadata`) are allowed when all adapters default them to `None`
2. Never add a bypass to the risk engine
3. Decimal for all prices and quantities, never float
4. paper_mode=True is always the default
5. Every new broker = one new file + one line in registry.py, nothing else
6. Log every signal, risk decision, order, and fill
7. AI only scores and explains — never executes (local-first; paid API is optional fallback)
8. Check `docs/DECISIONS.md` before making architectural choices

## HOW TO USE THIS FILE
At the start of a Claude session, say:
"Here is my project context: [paste this file]
Current task: [describe what you want to work on]"

Claude will immediately understand the full architecture and constraints
without needing to re-explain everything.
